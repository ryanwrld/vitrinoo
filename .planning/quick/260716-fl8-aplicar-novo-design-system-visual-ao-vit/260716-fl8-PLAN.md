---
phase: quick-260716-fl8
plan: 1
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [QUICK-DESIGN-SYSTEM]
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/admin-sidebar.tsx
  - src/app/(admin)/login/page.tsx
  - src/app/(admin)/cadastro/page.tsx
  - src/app/(admin)/esqueci-senha/page.tsx
  - src/app/(admin)/redefinir-senha/page.tsx
  - src/app/(admin)/onboarding/onboarding-wizard.tsx
  - src/app/(admin)/(painel)/dashboard/page.tsx
  - src/app/(admin)/(painel)/produtos/page.tsx
  - src/app/(admin)/(painel)/produtos/novo/page.tsx
  - src/app/(admin)/(painel)/produtos/[id]/editar/page.tsx
  - src/app/(admin)/(painel)/produtos/product-list.tsx
  - src/app/(admin)/(painel)/produtos/product-toolbar.tsx
  - src/app/(admin)/(painel)/produtos/product-form.tsx
  - src/app/(admin)/(painel)/produtos/size-grid.tsx
  - src/app/(admin)/(painel)/produtos/photo-uploader.tsx
  - src/app/(admin)/(painel)/configuracoes/page.tsx
  - src/app/(admin)/(painel)/configuracoes/settings-form.tsx
  - src/app/(admin)/(painel)/configuracoes/slug-editor.tsx
  - src/app/(admin)/(painel)/configuracoes/qr-code-panel.tsx
  - src/app/loja/[slug]/page.tsx
  - src/app/loja/[slug]/store-hero.tsx
  - src/app/loja/[slug]/product-card.tsx
  - src/app/loja/[slug]/product-filters.tsx
  - src/app/loja/[slug]/pagination-numbered.tsx
  - src/app/loja/[slug]/load-more-button.tsx
  - src/app/loja/[slug]/image-with-fallback.tsx
  - src/app/loja/[slug]/[produto]/product-order-panel.tsx
  - src/app/loja/[slug]/[produto]/product-not-found-content.tsx

must_haves:
  truths:
    - "Zero classes Tailwind de cor arbitraria (padrao [#hex]) permanecem em src/components, src/app/(admin) e src/app/loja — todas convertidas para tokens do @theme"
    - "As fontes Manrope (display) e Inter (corpo) sao carregadas via next/font/google no layout.tsx e substituem o Arial de globals.css"
    - "A cor de marca #0D21A1 (blue-600/primary) e #000000 (gray-950) continuam sendo a base — nunca substituidas, so tokenizadas"
    - "npx tsc --noEmit nao introduz nenhum erro novo (arbitrary values e classes de token permanecem sintaticamente validos)"
    - "Nenhuma prop, Server Action, query Supabase, RLS, redirect, middleware, schema, rota ou copy foi alterada — so className, tokens e imports de fonte"
    - "Os defaults de dado accentColor '#0D21A1' (onboarding/settings) e o fallback inline store.accentColor ?? '#000000' (store-hero) permanecem intactos"
  artifacts:
    - "src/app/globals.css com bloco @theme (escala azul/neutros/semanticas/whatsapp + raio + sombra), aliases de fonte e keyframes de motion vt-fade-in/scale-in/slide-up; keyframe shake preservado"
    - "src/app/layout.tsx importando Manrope + Inter e expondo as variaveis --font-manrope/--font-inter no <html>"
    - "30 arquivos de apresentacao com os novos tokens e estados (hover/active/focus-visible/transition) aplicados"
  key_links:
    - "globals.css @theme define --color-primary (#0D21A1) / --color-gray-950 (#000000) — a marca continua sendo a cor primaria"
    - "layout.tsx expoe --font-manrope e --font-inter que o @theme mapeia para font-display e font-body/font-sans"
    - "CTA 'Pedir agora' em product-order-panel.tsx usa o token whatsapp (bg-whatsapp) — unica excecao verde do sistema"
---

<objective>
Migracao puramente visual: aplicar o novo Design System do Vitrinoo (tokens de cor, tipografia, raio, sombra, motion e estados de componente) sobre os ~30 arquivos de apresentacao do repositorio, SEM alterar nenhuma logica, rota, prop, Server Action, query, RLS, validacao, copy ou fluxo existente.

O trabalho tem duas camadas:
1. **Fundacao (Task 1):** adicionar os tokens ao `@theme` de `globals.css` e trocar as fontes em `layout.tsx`. Isso remapeia globalmente a escala de cor (blue/gray/semanticas), o raio (`rounded-sm/md/lg/xl`), a sombra (`shadow-sm/md/lg`) e as familias (`font-display`/`font-body`), habilitando as classes de token que as tasks seguintes usam.
2. **Aplicacao (Tasks 2-5):** em cada arquivo, trocar as classes de cor arbitraria `[#hex]` pelos tokens equivalentes e aplicar os estados de componente (hover/active/focus-visible/transition, badges, dialogos, campos e selects estilizados) conforme as receitas fixadas abaixo.

Restricao de marca (nao-negociavel): `#0D21A1` continua sendo `--color-primary` (a cor mais reconhecivel) e `#000000` continua sendo `--color-gray-950`. Podemos ampliar a paleta com as variacoes derivadas ja fornecidas, nunca substituir esses dois valores.

Purpose: entregar a nova identidade visual moderna de forma consistente e centralizada em tokens, mantendo intacto todo o comportamento validado do milestone v1.0.
Output: `globals.css` + `layout.tsx` com a fundacao de tokens/fontes/motion, e 28 arquivos `.tsx` de apresentacao re-estilizados; `grep` de cor arbitraria zerado nos tres diretorios de UI; `npx tsc --noEmit` sem erros novos.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Fonte de verdade do design (ler se houver divergencia; o `.css` real tem prioridade sobre a transcricao abaixo): o pacote em `/Users/ryanlucas/Downloads/# Vitrinoo Design System/` (note o `# ` literal no nome — quotar o path em qualquer comando shell). Os `tokens/*.css` desse pacote foram lidos no planejamento e conferem exatamente com os valores transcritos na secao "Tokens do Design System" abaixo. Os `.jsx`/`.html` do pacote sao REFERENCIA de design, nunca codigo para copiar — o alvo e recriar o visual com Tailwind v4 + `@theme` e os componentes `.tsx` ja existentes.

Inventario confirmado por grep no planejamento (para calibrar o escopo real):
- 27 arquivos sob `src/components`, `src/app/(admin)` e `src/app/loja` contem classes de cor arbitraria `[#hex]`; contagem por valor: `[#111111]` 76x, `[#E7F2FD]` 74x, `[#0D21A1]` 67x, `[#6B6B6B]` 63x, `[#000000]` 42x, `[#FF4D4D]` 30x.
- Mais 2 arquivos server (`produtos/novo/page.tsx`, `produtos/[id]/editar/page.tsx`) tambem tem `[#000000]` no `<h1>` e no botao "Voltar" — inclusos na Task 3 (a lista original da tarefa nao os citou, mas eles renderizam os mesmos elementos e nao podem ficar com hex velho).
- `store-hero.tsx` NAO tem cor arbitraria (so o `[#hex]` quotado do default de dado) — entra na Task 5 apenas para o toque de `font-display` no titulo.
- Tres `[#hex]` sao DADO, nunca estilo, e ficam INTOCADOS: `accentColor: "#0D21A1"` (onboarding-wizard.tsx e settings-form.tsx defaultValues) e `backgroundColor: store.accentColor ?? "#000000"` (store-hero.tsx inline style). Sao aspas, nao colchetes — o grep de verificacao (`[#hex]`) nao os alcanca.
- `src/app/page.tsx` (landing starter do Next) e `src/app/globals.css` ficam FORA do grep de verificacao dos tres diretorios de UI; `page.tsx` esta fora de escopo (nao mexer), `globals.css` legitimamente contem hex nos tokens.
</context>

## Tokens do Design System (conteudo exato para `globals.css`)

Adicionar ao `src/app/globals.css`, logo apos `@import "tailwindcss";`. Manter o keyframe `shake` / `.animate-shake` existente exatamente como esta (NAO tocar — e usado no aviso de tamanho nao selecionado). Substituir a regra `font-family: Arial, Helvetica, sans-serif;` do `body` por `var(--font-inter), ui-sans-serif, system-ui, sans-serif;`.

```css
@theme {
  /* Azul de marca — #0D21A1 e blue-600 (marca, nao alterar) */
  --color-blue-50:  #EEF0FC;
  --color-blue-100: #DCE0F8;
  --color-blue-200: #B9C1F1;
  --color-blue-300: #8D99E7;
  --color-blue-400: #5C6BDA;
  --color-blue-500: #3140C4;
  --color-blue-600: #0D21A1;
  --color-blue-700: #0A1A80;
  --color-blue-800: #081560;
  --color-blue-900: #060F45;
  --color-blue-950: #030821;

  /* Neutros azulados (nunca cinza quente) — gray-950 e #000000 (marca) */
  --color-gray-50:  #F7F8FB;
  --color-gray-100: #EEF0F6;
  --color-gray-200: #DFE2ED;
  --color-gray-300: #C5CADC;
  --color-gray-400: #9CA3BE;
  --color-gray-500: #767E9E;
  --color-gray-600: #575E7D;
  --color-gray-700: #40445C;
  --color-gray-800: #292C3D;
  --color-gray-900: #14151F;
  --color-gray-950: #000000;

  /* Aliases semanticos de marca */
  --color-primary: #0D21A1;
  --color-primary-hover: #0A1A80;
  --color-primary-active: #081560;
  --color-primary-subtle: #EEF0FC;
  --color-primary-border: #B9C1F1;

  /* Semanticas */
  --color-success-bg: #EBFAF1;
  --color-success-fg: #0B6238;
  --color-success-solid: #1FA860;
  --color-success-solid-hover: #128049;
  --color-error-bg: #FDECEE;
  --color-error-fg: #86172A;
  --color-error-solid: #D82A44;
  --color-error-solid-hover: #AE1F35;
  --color-warning-bg: #FDF3E4;
  --color-warning-fg: #7A4C14;
  --color-warning-solid: #C9821F;

  /* WhatsApp — excecao pontual, so no CTA "Pedir agora" */
  --color-whatsapp: #25D366;
  --color-whatsapp-hover: #1DA851;

  /* Raio (remapeia rounded-sm/md/lg/xl para a escala do DS) */
  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 14px;
  --radius-xl: 20px;

  /* Sombra rasa (remapeia shadow-sm/md/lg) */
  --shadow-xs: 0 1px 2px rgba(3,8,33,0.06);
  --shadow-sm: 0 1px 3px rgba(3,8,33,0.08), 0 1px 2px rgba(3,8,33,0.05);
  --shadow-md: 0 4px 12px rgba(3,8,33,0.10), 0 2px 4px rgba(3,8,33,0.06);
  --shadow-lg: 0 12px 28px rgba(3,8,33,0.14), 0 4px 8px rgba(3,8,33,0.06);
}

@theme inline {
  --font-display: var(--font-manrope), ui-sans-serif, system-ui, sans-serif;
  --font-body: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
  --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
}
```

Motion — adicionar tambem a `globals.css` (fora do `@theme`, junto ao shake existente):

```css
@keyframes vt-fade-in { from { opacity: 0 } to { opacity: 1 } }
@keyframes vt-scale-in { from { opacity: 0; transform: scale(.96) translateY(4px) } to { opacity: 1; transform: scale(1) translateY(0) } }
@keyframes vt-slide-up { from { opacity: 0; transform: translateY(8px) } to { opacity: 1; transform: translateY(0) } }
.animate-fade-in { animation: vt-fade-in 180ms cubic-bezier(.4,0,.2,1); }
.animate-scale-in { animation: vt-scale-in 280ms cubic-bezier(.2,0,0,1); }
.animate-slide-up { animation: vt-slide-up 280ms cubic-bezier(.2,0,0,1); }
```

Observacao Tailwind v4: definir `--color-blue-600` etc. no `@theme` SUBSTITUI apenas esses tons no default; cores como `white`/`black` seguem intactas. Remapear `--radius-*` e `--shadow-*` faz as classes existentes `rounded-lg`/`shadow-sm` ja adotarem a escala do DS sem edicao por arquivo — comportamento desejado.

## Fontes (`layout.tsx`)

Trocar `Geist`/`Geist_Mono` por `Manrope`/`Inter` de `next/font/google`:
- `const manrope = Manrope({ subsets: ["latin"], variable: "--font-manrope", weight: ["500","600","700","800"], display: "swap" });`
- `const inter = Inter({ subsets: ["latin"], variable: "--font-inter", weight: ["400","500","600","700"], display: "swap" });`
- No `<html>`, trocar `${geistSans.variable} ${geistMono.variable}` por `${manrope.variable} ${inter.variable}` (manter `h-full antialiased`).
- Manter `<Toaster richColors position="top-center" />`, `metadata`, estrutura do `<body>` — nada de logica muda.

## Receitas de classe (aplicar por estado de componente)

Aplicar SEMPRE via `className` Tailwind, sem novo componente/lib. Altura de toque minima 44px (`min-h-11`) em controles interativos no mobile.

- **Botao primario** (submit "Entrar"/"Criar"/"Salvar produto"/"Salvar alteracoes"/"Concluir", "Novo produto", "Salvar novo link", "Carregar mais", "Copiar" da URL/QR, "Enviar link"): `rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white transition-all duration-150 hover:bg-primary-hover active:bg-primary-active active:scale-[.98] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-400 focus-visible:ring-offset-2 disabled:bg-gray-100 disabled:text-gray-400 disabled:pointer-events-none`.
- **Botao secundario** (os antigos outline pretos: "Voltar ao painel", "Cancelar", "Baixar PNG", "Publicar"/"Voltar para rascunho"): `rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-semibold text-gray-900 transition-all duration-150 hover:bg-gray-100 active:bg-gray-200 active:scale-[.98] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-400 focus-visible:ring-offset-2 disabled:opacity-60`. (Substitui o padrao antigo `border-[#000000] text-[#000000] hover:bg-[#000000] hover:text-white` — o secundario do DS e outline cinza, nao preto.)
- **Botao destrutivo** ("Sim, excluir", "Sim, trocar o link"): `rounded-md bg-error-solid px-4 py-2 text-sm font-semibold text-white transition-all duration-150 hover:bg-error-solid-hover active:scale-[.98] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-error-bg focus-visible:ring-offset-2 disabled:opacity-60`.
- **CTA WhatsApp** (SO em product-order-panel.tsx, "Pedir agora"): mesma forma do primario mas cor whatsapp — trocar `bg-primary/hover:bg-primary-hover/active:bg-primary-active` por `bg-whatsapp hover:bg-whatsapp-hover` (sem active-shade proprio); manter texto branco e a logica de `animate-shake`/`key`/`href`/`onClick` intacta.
- **Campo (input de texto/tel/email/senha/color/file e textarea)**: `rounded-md border border-gray-300 bg-white px-3 h-11 text-base text-gray-900 outline-none transition-colors duration-150 focus:border-primary focus:ring-2 focus:ring-primary-subtle placeholder:text-gray-400` (textarea usa `py-2` no lugar de `h-11`; input `type=color`/`type=file` mantem os tamanhos atuais, so troca as cores de borda/foco). Estado de erro: adicionar `border-error-solid focus:border-error-solid focus:ring-error-bg`. Desabilitado: `bg-gray-100 text-gray-400`.
- **Label de campo**: `text-sm font-medium text-gray-700`. **Mensagem de erro de campo**: `text-sm text-error-solid`. **Texto auxiliar/previa/caption**: `text-xs text-gray-500`.
- **`<select>` nativo estilizado**: NAO trocar por componente custom (mantem acessibilidade nativa). Envolver o `<select>` num `<div className="relative">`; no `<select>` aplicar a receita de Campo + `appearance-none pr-9`; adicionar como irmao um chevron: `<ChevronDown className="pointer-events-none absolute right-3 top-1/2 h-4 w-4 -translate-y-1/2 text-gray-400" aria-hidden="true" />` (importar `ChevronDown` de `lucide-react`, ja dependencia). Preservar `{...register(...)}`, `value`, `onChange`, `aria-label` e todas as `<option>` sem mudanca.
- **`<dialog>` nativo** (drawer do admin-sidebar, confirmacao de exclusao em product-list, confirmacao de slug em slug-editor): manter `<dialog>` + `.showModal()` e toda a logica. Trocar o backdrop para `backdrop:bg-black/45 backdrop:backdrop-blur-[2px]` e adicionar `animate-scale-in` no conteudo do dialogo (no `<div>`/wrapper interno; nos dialogos de confirmacao que nao tem wrapper, adicionar um `<div className="animate-scale-in">` ao redor do conteudo OU aplicar a classe no proprio `<dialog>`). O `<dialog>` de confirmacao ganha tambem `rounded-lg` e `shadow-lg`.
- **Badge/status (pill)**: `inline-flex items-center gap-1.5 rounded-full px-2.5 py-0.5 text-xs font-semibold`, com variantes: publicado/disponivel `bg-success-bg text-success-fg`; rascunho `bg-gray-100 text-gray-600`; esgotado `bg-error-bg text-error-fg`.
- **Indicador inline de disponibilidade (dot + texto)** nas listas: disponivel -> texto `text-success-fg`, dot `bg-success-solid`; esgotado -> texto `text-gray-500`, dot `bg-gray-400`. Adicionar `transition-colors duration-150` onde alterna.
- **Chip de filtro** (product-filters do storefront e product-toolbar do admin): ativo `border-primary bg-primary text-white`; inativo `border-gray-300 bg-white text-gray-700 hover:border-primary hover:text-primary`; sempre com `rounded-full ... transition-colors duration-150`.
- **Pill de tamanho (grade)**: nao-incluido/disponivel-nao-selecionado `border-gray-300 bg-white text-gray-900 hover:border-primary`; incluido-esgotado `border-gray-200 bg-gray-100 text-gray-400 line-through`; incluido/selecionado-disponivel `border-primary bg-primary text-white`; adicionar `transition-colors duration-150`. Preservar `aria-pressed`, `tabIndex`, `pointer-events-none` e handlers.
- **Card** (stat cards do dashboard, linhas de lista, tile de produto): container `border border-gray-200` + `rounded-lg` (14px via token) + `shadow-sm`; empty-state tracejado usa `border-dashed border-gray-300`. Tile de produto do storefront (o `<Link>` do product-card) ganha `transition-all duration-150 hover:shadow-md hover:-translate-y-0.5`.
- **Tipografia**: titulos de pagina `<h1>` e subtitulos `<h2>` recebem `font-display`; nomes de produto recebem `font-display`; numeros dos stat cards `font-display font-extrabold text-3xl`. Preco do produto no storefront `font-display font-bold text-primary` (conforme ProductCard do pacote); preco no admin permanece `text-gray-900`.
- **Placeholder de imagem / thumbnail / fundo do QR / slot de upload**: fundo `bg-gray-100`; icone de fallback `text-gray-400`. Campo de URL read-only: fundo `bg-gray-50`.
- **Toast**: os toasts sao gerenciados pelo `sonner` (`<Toaster richColors position="top-center">`) — NAO reescrever para markup manual; manter como esta (o `richColors` ja colore sucesso/erro). As classes `.animate-slide-up` ficam disponiveis para uso pontual, mas nao ha troca obrigatoria aqui.
- **Skeleton**: nao ha spinner de LISTA no codigo atual (os `Loader2` existentes sao status inline de "Enviando…"/"Verificando…" e devem permanecer). Portanto nenhum swap de skeleton e necessario — nao inventar skeletons novos.

## Mapeamento hex -> token (camada mecanica base)

Aplicar a troca de cor em TODA classe `[#hex]` (colchete = estilo). Os tres `[#hex]` quotados de dado ficam intocados.

| Classe antiga (arbitraria) | Token novo |
|---|---|
| `bg-[#0D21A1]` (CTA/dot/badge azul) | `bg-primary` |
| `text-[#0D21A1]` | `text-primary` |
| `border-[#0D21A1]` | `border-primary` |
| `focus:border-[#0D21A1]` / `focus-within:border-[#0D21A1]` | `focus:border-primary` / `focus-within:border-primary` |
| `text-[#000000]` (headings) | `text-gray-900` + `font-display` no heading |
| padrao outline `border-[#000000] text-[#000000] hover:bg-[#000000] hover:text-white` | receita "Botao secundario" |
| `border-[#E7F2FD]` (borda solida) | `border-gray-200` |
| `border-dashed border-[#E7F2FD]` | `border-dashed border-gray-300` |
| `bg-[#E7F2FD]` (thumb/QR/slot/processing) | `bg-gray-100` |
| `bg-[#E7F2FD]` (campo URL read-only) | `bg-gray-50` |
| `text-[#6B6B6B]` | `text-gray-500` |
| `text-[#111111]` | `text-gray-900` |
| `text-[#FF4D4D]` (msg de erro) | `text-error-solid` |
| `bg-[#FF4D4D]` (botao destrutivo) | receita "Botao destrutivo" |
| `text-[#FF4D4D]` + `hover:bg-[#FF4D4D]/10` (icone destrutivo) | `text-error-solid hover:bg-error-bg` |
| `bg-[#111111]` (tooltip do order-panel) | `bg-gray-900` |
| status pill `bg-[#E7F2FD] text-[#6B6B6B]` | receita Badge (rascunho: `bg-gray-100 text-gray-600`) |
| indicador "Disponivel" azul (`text-[#0D21A1]`/dot `bg-[#0D21A1]`) | `text-success-fg` / dot `bg-success-solid` |
| indicador "Esgotado" cinza (`text-[#6B6B6B]`/dot `bg-[#6B6B6B]`) | `text-gray-500` / dot `bg-gray-400` |

<tasks>

<!-- planner-discipline-allow: 0D21A1 -->
<!-- planner-discipline-allow: E7F2FD -->
<!-- planner-discipline-allow: 6B6B6B -->
<!-- planner-discipline-allow: 111111 -->
<!-- planner-discipline-allow: FF4D4D -->
<!-- planner-discipline-allow: 000000 -->

<task type="auto">
  <name>Task 1: Fundacao — tokens no @theme, motion e fontes Manrope/Inter</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <action>
Estabelecer a fundacao de tokens que as Tasks 2-5 consomem. NAO tocar em nenhum outro arquivo nesta task.

1. `src/app/globals.css`: manter `@import "tailwindcss";` na primeira linha. Adicionar logo em seguida o bloco `@theme { … }` e o bloco `@theme inline { … }` exatamente como na secao "Tokens do Design System (conteudo exato para globals.css)" acima (escala blue/gray, aliases primary, semanticas success/error/warning, whatsapp, `--radius-*`, `--shadow-*`, e os aliases de fonte `--font-display`/`--font-body`/`--font-sans`). Adicionar tambem os `@keyframes vt-fade-in/vt-scale-in/vt-slide-up` e as classes `.animate-fade-in/.animate-scale-in/.animate-slide-up` da secao "Motion". Na regra `body`, trocar `font-family: Arial, Helvetica, sans-serif;` por `font-family: var(--font-inter), ui-sans-serif, system-ui, sans-serif;`. PRESERVAR intactos: o `@keyframes shake` e a classe `.animate-shake` existentes. Pode remover ou manter o `@theme inline` antigo de `--color-background`/`--color-foreground`/`--font-mono` (geist), mas NAO deixar referencia a `--font-geist-sans`/`--font-geist-mono` que deixarao de existir — se mantiver `--font-mono`, aponte para uma stack monospace generica.

2. `src/app/layout.tsx`: trocar `import { Geist, Geist_Mono } from "next/font/google";` por `import { Manrope, Inter } from "next/font/google";`. Substituir as duas constantes `geistSans`/`geistMono` por `manrope` e `inter` conforme a secao "Fontes" (variaveis `--font-manrope` e `--font-inter`, pesos Manrope 500/600/700/800 e Inter 400/500/600/700, `display: "swap"`). No `<html>`, trocar `${geistSans.variable} ${geistMono.variable}` por `${manrope.variable} ${inter.variable}`, mantendo `h-full antialiased`. NAO alterar `metadata`, `<Toaster>` nem a estrutura do `<body>`.

Guard-rail: nenhuma logica muda; e adicao de tokens CSS + troca de import de fonte.
  </action>
  <verify>
    <automated>cd /Users/ryanlucas/Downloads/VITRINOO && grep -q "\-\-color-primary: #0D21A1" src/app/globals.css && grep -q "vt-scale-in" src/app/globals.css && grep -q "animate-shake" src/app/globals.css && grep -q "Manrope" src/app/layout.tsx && grep -q "font-manrope" src/app/layout.tsx && ! grep -q "font-geist" src/app/layout.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>`globals.css` tem o `@theme` com `--color-primary: #0D21A1` e demais tokens, os keyframes `vt-*` de motion, e ainda o `.animate-shake` original; `body` usa `var(--font-inter)`. `layout.tsx` importa `Manrope`/`Inter`, expoe `--font-manrope`/`--font-inter` no `<html>` e nao referencia mais `geist`. `npx tsc --noEmit` sem erro novo.</done>
</task>

<task type="auto">
  <name>Task 2: Shell admin + paginas de auth + onboarding</name>
  <files>src/components/admin-sidebar.tsx, src/app/(admin)/login/page.tsx, src/app/(admin)/cadastro/page.tsx, src/app/(admin)/esqueci-senha/page.tsx, src/app/(admin)/redefinir-senha/page.tsx, src/app/(admin)/onboarding/onboarding-wizard.tsx</files>
  <action>
Aplicar o mapeamento hex->token e as receitas de classe. So `className` e (no sidebar) o import do chevron mudam — nenhuma prop/action/logica.

- `admin-sidebar.tsx` (Nav item, guard-rail: NAO tocar na logica do `<dialog>`/`useEffect`/`matchMedia`): trocar o item ativo `border-[#0D21A1] ... text-[#0D21A1]` por `border-primary text-primary` e o inativo `text-[#6B6B6B]` por `text-gray-500 hover:text-gray-900`, adicionando `transition-colors duration-150` em ambos; manter a estrutura de `border-l-2 ... pl-3 -ml-3`. Bordas/divisores `border-[#E7F2FD]` -> `border-gray-200`. Botao "Sair da conta" texto `text-[#6B6B6B]` -> `text-gray-500 hover:text-gray-900 transition-colors duration-150`. No `<dialog>` do drawer: backdrop -> `backdrop:bg-black/45 backdrop:backdrop-blur-[2px]`, e adicionar `animate-scale-in` no `<div>` interno de conteudo.
- `login/page.tsx`, `cadastro/page.tsx`, `esqueci-senha/page.tsx`, `redefinir-senha/page.tsx`: `<h1>` -> `font-display text-gray-900` (trocando `text-[#000000]`); subtitulos/labels/erros/campos/botao submit via receitas (Campo, Label, Mensagem de erro, Botao primario); links de rodape (`text-[#000000] underline`) -> `text-primary hover:text-primary-hover font-medium`. Manter o layout single-column atual (nao inventar split-screen; se quiser o painel azul opcional do desktop nao e obrigatorio — priorizar consistencia com as receitas).
- `onboarding-wizard.tsx` (guard-rail: NAO alterar o default `accentColor: "#0D21A1"` nem a logica de `AsYouType`/`saveOnboarding`): `<h1>` -> `font-display text-gray-900`; todos os campos/labels/erros/previa e o botao "Concluir e ver minha vitrine" via receitas (Campo, Label, texto auxiliar `text-gray-500`, Botao primario). O input `type=color` mantem `h-10 w-20 p-1`, so troca a borda para `border-gray-300`. Se o wizard tiver barra de progresso, passos concluidos `bg-primary` e pendentes `bg-gray-100` (o wizard atual e tela unica sem stepper — se nao houver barra, ignorar este item).

Converter TODA classe `[#hex]` restante nestes arquivos conforme o mapeamento (incl. `text-[#111111]` -> `text-gray-900`, `border-[#E7F2FD]` -> `border-gray-200`, `text-[#FF4D4D]` -> `text-error-solid`).
  </action>
  <verify>
    <automated>cd /Users/ryanlucas/Downloads/VITRINOO && test "$(grep -rhoE '\[#[0-9A-Fa-f]{3,8}\]' src/components/admin-sidebar.tsx 'src/app/(admin)/login/page.tsx' 'src/app/(admin)/cadastro/page.tsx' 'src/app/(admin)/esqueci-senha/page.tsx' 'src/app/(admin)/redefinir-senha/page.tsx' 'src/app/(admin)/onboarding/onboarding-wizard.tsx' | wc -l | tr -d ' ')" = "0" && grep -q 'accentColor: "#0D21A1"' 'src/app/(admin)/onboarding/onboarding-wizard.tsx' && npx tsc --noEmit</automated>
  </verify>
  <done>Zero classes `[#hex]` nos 6 arquivos; nav ativo usa `border-primary text-primary`; auth/onboarding com receitas de campo/botao/tipografia aplicadas; default `accentColor: "#0D21A1"` preservado; `<dialog>` do drawer intacto na logica com backdrop novo; `npx tsc --noEmit` sem erro novo.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard + Produtos (lista, toolbar, form, tamanhos, fotos, novo/editar)</name>
  <files>src/app/(admin)/(painel)/dashboard/page.tsx, src/app/(admin)/(painel)/produtos/page.tsx, src/app/(admin)/(painel)/produtos/novo/page.tsx, src/app/(admin)/(painel)/produtos/[id]/editar/page.tsx, src/app/(admin)/(painel)/produtos/product-list.tsx, src/app/(admin)/(painel)/produtos/product-toolbar.tsx, src/app/(admin)/(painel)/produtos/product-form.tsx, src/app/(admin)/(painel)/produtos/size-grid.tsx, src/app/(admin)/(painel)/produtos/photo-uploader.tsx</files>
  <action>
Aplicar mapeamento + receitas. Nenhuma query/action/prop muda.

- `dashboard/page.tsx`: `<h1>`/`<h2>` -> `font-display text-gray-900`. Os 4 stat cards: container `rounded-lg border border-gray-200 p-5` (era `rounded-lg border border-[#E7F2FD] p-4`); label `text-gray-500`; icone `text-gray-400`; numero `font-display font-extrabold text-3xl text-gray-900` (simplificar os arrays `iconClass`/`numberClass` para valores uniformes — presentacional, nao muda os dados calculados). Linhas de produto (recentes/mais-vistos/cliques): receita Card de linha (`border-gray-200`), nome `font-display text-gray-900`, secundario `text-gray-500`, preco `text-gray-900`, indicador de disponibilidade via receita success/gray; empty-states tracejados `border-dashed border-gray-300` com botao primario "Novo produto".
- `produtos/page.tsx`, `produtos/novo/page.tsx`, `produtos/[id]/editar/page.tsx`: `<h1>` -> `font-display text-gray-900`; botao "Novo produto" (primario); botao "Voltar ao painel"/"Voltar" (secundario). Converter os `[#000000]`/`[#E7F2FD]`/`[#6B6B6B]`/`[#111111]` desses tres arquivos.
- `product-list.tsx`: linhas Card `border-gray-200`; thumb `bg-gray-100` + fallback `text-gray-400`; nome `font-display text-gray-900`; secundario `text-gray-500`; preco `text-gray-900`; indicador disponibilidade success/gray; a pill de status (Publicado/Rascunho) via receita Badge (publicado `bg-success-bg text-success-fg`, rascunho `bg-gray-100 text-gray-600`); botao editar (icone) `text-gray-500 hover:bg-gray-100 hover:text-gray-900`; botao excluir (icone) `text-error-solid hover:bg-error-bg`; `<dialog>` de confirmacao: backdrop `backdrop:bg-black/45 backdrop:backdrop-blur-[2px]`, `rounded-lg shadow-lg animate-scale-in`, "Cancelar" (secundario), "Sim, excluir" (destrutivo). NAO alterar `deleteProduct`/estado/`showModal`.
- `product-toolbar.tsx`: campo de busca (receita Campo, wrapper com foco `focus-within:border-primary focus:ring-2 focus:ring-primary-subtle`); os 4 `<select>` viram selects estilizados (receita `<select>` nativo estilizado, com `ChevronDown`). Preservar todos os `value`/`onChange`/`aria-label`/`<option>` e o debounce.
- `product-form.tsx`: `<h2>` de secao -> `font-display text-gray-900`; todos os campos/labels/erros via receitas; os `<select>` (marca, solado, categoria, modalidade, visibilidade) estilizados com chevron; wrapper de preco (`focus-within:border-primary`); botao "Salvar produto" (primario), "Publicar"/"Voltar para rascunho" (secundario). Preservar toda a logica de `useForm`/`useTransition`/`saveProduct`/`updateProduct`/`publishProduct`.
- `size-grid.tsx`: pills via receita "Pill de tamanho" (nao-incluido `border-gray-300 text-gray-500`... etc.); `<h2>` `font-display text-gray-900`, texto auxiliar `text-gray-500`; botao "Marcar tudo como esgotado" (secundario, tamanho `text-sm`). Preservar `useFieldArray` e `markProductEsgotado`.
- `photo-uploader.tsx`: `<h2>` `font-display text-gray-900`, auxiliar `text-gray-500`; slots (`border-gray-200`), placeholder/processing `bg-gray-100`, empty tracejado `border-dashed border-gray-300`, icones `text-gray-400`; badge "Capa" `bg-primary text-white`; botao remover (o span interno) `text-error-solid`; grip mantem estilo neutro (`text-gray-900`). Preservar TODA a logica de dnd-kit/compressao/Server Actions.

Converter qualquer `[#hex]` remanescente conforme o mapeamento.
  </action>
  <verify>
    <automated>cd /Users/ryanlucas/Downloads/VITRINOO && test "$(grep -rhoE '\[#[0-9A-Fa-f]{3,8}\]' 'src/app/(admin)/(painel)/dashboard' 'src/app/(admin)/(painel)/produtos' | wc -l | tr -d ' ')" = "0" && grep -rq "ChevronDown" 'src/app/(admin)/(painel)/produtos/product-form.tsx' 'src/app/(admin)/(painel)/produtos/product-toolbar.tsx' && npx tsc --noEmit</automated>
  </verify>
  <done>Zero classes `[#hex]` em `dashboard/` e `produtos/`; stat cards com numero `font-display font-extrabold text-3xl`; pill de status via Badge (success/gray); selects com `ChevronDown` e `appearance-none`; `<dialog>` de exclusao com backdrop/animate novos e logica intacta; `npx tsc --noEmit` sem erro novo.</done>
</task>

<task type="auto">
  <name>Task 4: Configuracoes (pagina, form, slug-editor, QR)</name>
  <files>src/app/(admin)/(painel)/configuracoes/page.tsx, src/app/(admin)/(painel)/configuracoes/settings-form.tsx, src/app/(admin)/(painel)/configuracoes/slug-editor.tsx, src/app/(admin)/(painel)/configuracoes/qr-code-panel.tsx</files>
  <action>
Aplicar mapeamento + receitas. Guard-rail: NAO alterar o default `accentColor: store.accentColor ?? "#0D21A1"` em settings-form.tsx (linha de dado), nem a logica de `checkSlugAvailability`/`updateStoreSlug`/`saveStoreSettings`/`QRCode.toCanvas`.

- `configuracoes/page.tsx`: `<h1>`/`<h2>` -> `font-display text-gray-900`; botao "Voltar ao painel" (secundario).
- `settings-form.tsx`: `<h2>` de secao `font-display text-gray-900`; todos os campos/labels/erros/previa via receitas; os `<select>` (ocultar esgotados) estilizado com chevron; input `type=color`/`type=file` so troca borda para `border-gray-300`; botao "Salvar alteracoes" (primario). Preservar o default `accentColor` e `saveStoreSettings`.
- `slug-editor.tsx`: label/campo/`/loja/{slug}` (`text-gray-500`); `StatusPill` — "Verificando" `text-gray-500` (mantem `Loader2`), "Disponivel" `text-success-fg` (`Check`), "Este link ja esta em uso" `text-error-solid` (`X`); mensagem de erro de formato `text-error-solid`; botao "Salvar novo link" (primario); `<dialog>` de confirmacao: backdrop `backdrop:bg-black/45 backdrop:backdrop-blur-[2px]`, `rounded-lg shadow-lg animate-scale-in`, "Cancelar" (secundario), "Sim, trocar o link" (destrutivo). NAO alterar o fluxo de `needsCheck`/`useTransition`/`showModal`.
- `qr-code-panel.tsx`: `<h2>` `font-display text-gray-900`; moldura do canvas `bg-gray-100`; botao "Baixar PNG" (secundario, com icone); label/URL read-only (`bg-gray-50 border-gray-200 text-gray-900`); botao "Copiar" (primario, com icone). Preservar `canvasRef`/`toDataURL`/`copyText`.

Converter qualquer `[#hex]` remanescente conforme o mapeamento.
  </action>
  <verify>
    <automated>cd /Users/ryanlucas/Downloads/VITRINOO && test "$(grep -rhoE '\[#[0-9A-Fa-f]{3,8}\]' 'src/app/(admin)/(painel)/configuracoes' | wc -l | tr -d ' ')" = "0" && grep -q 'accentColor: store.accentColor ?? "#0D21A1"' 'src/app/(admin)/(painel)/configuracoes/settings-form.tsx' && npx tsc --noEmit</automated>
  </verify>
  <done>Zero classes `[#hex]` em `configuracoes/`; StatusPill do slug em success/gray/error; `<dialog>` de troca de link com backdrop/animate novos e logica intacta; default `accentColor` de dado preservado; `npx tsc --noEmit` sem erro novo.</done>
</task>

<task type="auto">
  <name>Task 5: Vitrine publica (loja + detalhe do produto)</name>
  <files>src/app/loja/[slug]/page.tsx, src/app/loja/[slug]/store-hero.tsx, src/app/loja/[slug]/product-card.tsx, src/app/loja/[slug]/product-filters.tsx, src/app/loja/[slug]/pagination-numbered.tsx, src/app/loja/[slug]/load-more-button.tsx, src/app/loja/[slug]/image-with-fallback.tsx, src/app/loja/[slug]/[produto]/product-order-panel.tsx, src/app/loja/[slug]/[produto]/product-not-found-content.tsx</files>
  <action>
Aplicar mapeamento + receitas. Guard-rails: NAO tocar na logica de `shakeKey`/`animate-shake`/`decideOrderAction`/`logOrderClick`/`buildWhatsAppUrl` do order-panel; NAO alterar o fallback inline `store.accentColor ?? "#000000"` em store-hero.tsx; manter o CTA "Pedir agora" como `<a href>` real (nunca `disabled`).

- `page.tsx` (loja): empty-states tracejados `border-dashed border-gray-300`, texto `text-gray-900`.
- `store-hero.tsx`: aplicar `font-display` no `<h1>` do nome da loja. Manter o `style={{ backgroundColor: store.accentColor ?? "#000000" }}` e `rounded-2xl` intactos (o hero usa a cor de destaque da loja, dado dinamico).
- `product-card.tsx`: moldura da imagem `bg-gray-100`; nome `font-display text-gray-900`; secundario `text-gray-500`; preco `font-display font-bold text-primary`; indicador disponibilidade success/gray; no `<Link>` container adicionar `transition-all duration-150 hover:shadow-md hover:-translate-y-0.5` (lift do card).
- `product-filters.tsx`: campo de busca (receita Campo com `focus-within:border-primary`); chips via receita "Chip de filtro" (ativo `border-primary bg-primary text-white`, inativo `border-gray-300 bg-white text-gray-700 hover:border-primary hover:text-primary`, `transition-colors duration-150`). Preservar `useDebouncedValue`/`navigate`/multi-select.
- `pagination-numbered.tsx`: "Anterior"/"Proxima" (secundario).
- `load-more-button.tsx`: botao "Carregar mais" (primario). Preservar `fetchNextPage`/estado.
- `image-with-fallback.tsx`: fundo do fallback `bg-gray-100`, icone `text-gray-400`. Preservar `onError`/estado.
- `product-order-panel.tsx`: "Voltar" (`text-gray-500`, mantem `ChevronLeft`); moldura das fotos `bg-gray-100`; contador de fotos `text-gray-500`; `<h1>` nome `font-display text-gray-900`, preco `font-display font-bold text-primary`; `<h2>` "Escolha o tamanho" `font-display text-gray-900`; pills de tamanho via receita "Pill de tamanho" (disponivel-nao-selecionado `border-gray-300 bg-white text-gray-900 hover:border-primary`, selecionado `border-primary bg-primary text-white`, esgotado `pointer-events-none border-gray-200 bg-gray-100 text-gray-400 line-through opacity-60`); tooltip "Selecione um tamanho" `bg-gray-900 text-white rounded-md`; CTA "Pedir agora" -> receita CTA WhatsApp (`bg-whatsapp hover:bg-whatsapp-hover text-white`) mantendo `key`/`href`/`target`/`rel`/`onClick` e a classe condicional `animate-shake`; "Copiar pedido" -> receita Botao secundario, mantendo `Copy`/`copyShakeKey`/`animate-shake`.
- `product-not-found-content.tsx`: `<h1>` `font-display text-gray-900`, paragrafo `text-gray-500`, link "Voltar para a loja" `text-primary hover:text-primary-hover font-medium`.

Converter qualquer `[#hex]` remanescente conforme o mapeamento.
  </action>
  <verify>
    <automated>cd /Users/ryanlucas/Downloads/VITRINOO && test "$(grep -rhoE '\[#[0-9A-Fa-f]{3,8}\]' 'src/app/loja' | wc -l | tr -d ' ')" = "0" && grep -q 'store.accentColor ?? "#000000"' 'src/app/loja/[slug]/store-hero.tsx' && grep -q "bg-whatsapp" 'src/app/loja/[slug]/[produto]/product-order-panel.tsx' && npx tsc --noEmit</automated>
  </verify>
  <done>Zero classes `[#hex]` em `src/app/loja`; CTA "Pedir agora" usa `bg-whatsapp` como `<a href>` real com `animate-shake` preservado; cards de produto com lift no hover e preco `text-primary`; fallback inline `#000000` de store-hero preservado; `npx tsc --noEmit` sem erro novo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhuma nova fronteira de confianca. A mudanca e puramente de apresentacao (classes Tailwind, tokens CSS, imports de fonte). Nao ha nova entrada de usuario, endpoint, dependencia nova (Manrope/Inter via `next/font/google` ja disponivel; `ChevronDown` do `lucide-react` ja instalado), nem alteracao de fluxo de dados, auth, RLS ou controle.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | Edicao de className atingir logica (prop/action/handler) por engano | low | mitigate | Cada task lista guard-rails explicitos (nao tocar em dialog/select nativo, defaults de dado, Server Actions, shakeKey); verificacao por `npx tsc --noEmit` e greps que confirmam preservacao dos defaults `accentColor`/`#000000` |
| T-quick-02 | Denial of Service | Remap de `--radius-*`/`--shadow-*` no @theme quebrar layout visual amplo | low | accept | Remap e intencional (adota a escala do DS); classes `white`/`black`/`rounded-full` seguem intactas; risco visual, nao funcional, e revisavel no checkpoint humano do /gsd-quick |
| T-quick-SC | Tampering | npm/pip/cargo installs | n/a | accept | Nenhuma instalacao de pacote — Manrope/Inter e ChevronDown ja fazem parte de dependencias existentes (next/font/google, lucide-react); sem superficie de supply-chain |
</threat_model>

<verification>
Gate final (rodar apos as 5 tasks, a partir de /Users/ryanlucas/Downloads/VITRINOO):

1. `grep -rhoE '\[#[0-9A-Fa-f]{3,8}\]' src/components "src/app/(admin)" "src/app/loja" | wc -l` — deve ser `0` (nenhuma classe de cor arbitraria remanescente nos tres diretorios de UI).
2. `grep -rnE '"#[0-9A-Fa-f]{6}"' src/components "src/app/(admin)" "src/app/loja"` — deve retornar exatamente as 3 linhas de DADO preservadas: `accentColor: "#0D21A1"` (onboarding-wizard.tsx e settings-form.tsx) e `store.accentColor ?? "#000000"` (store-hero.tsx).
3. `grep -q "\-\-color-primary: #0D21A1" src/app/globals.css` e `grep -q "\-\-color-gray-950: #000000" src/app/globals.css` — marca preservada como token primario/preto.
4. `grep -q "Manrope" src/app/layout.tsx && ! grep -q "font-geist" src/app/layout.tsx` — fontes trocadas.
5. `npx tsc --noEmit` — sem erros novos (o unico erro pre-existente conhecido e em `tests/**/server-cookies.test.ts`, ver STATE.md; nao introduzir outros).
6. (Manual, checkpoint humano do /gsd-quick) abrir a vitrine publica e o painel no mobile e conferir: CTA "Pedir agora" verde funcionando como link real, hover/foco dos botoes, dialogos com backdrop novo, badges de status.
</verification>

<success_criteria>
- Zero classes de cor arbitraria `[#hex]` em `src/components`, `src/app/(admin)` e `src/app/loja`.
- `#0D21A1` continua `--color-primary` e `#000000` continua `--color-gray-950` no `@theme` — marca nunca substituida.
- Manrope (display) + Inter (corpo) carregadas via `next/font/google`, Arial removido de `globals.css`.
- Estados de componente (hover/active/focus-visible/transition), badges success/gray/error, `<select>` estilizados com chevron, `<dialog>` com backdrop+scale-in, e CTA WhatsApp aplicados conforme as receitas.
- Nenhuma prop/Server Action/query/RLS/redirect/middleware/schema/rota/copy alterada; defaults de dado `accentColor`/`#000000` intactos.
- `npx tsc --noEmit` limpo (sem erros novos).
</success_criteria>

<output>
Create `.planning/quick/260716-fl8-aplicar-novo-design-system-visual-ao-vit/260716-fl8-SUMMARY.md` when done.
</output>
