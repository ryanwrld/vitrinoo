---
phase: quick-260715-du7
plan: 1
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [QUICK-REBRAND-COR]
files_modified:
  - src/app/(admin)/cadastro/page.tsx
  - src/app/(admin)/configuracoes/page.tsx
  - src/app/(admin)/configuracoes/qr-code-panel.tsx
  - src/app/(admin)/configuracoes/settings-form.tsx
  - src/app/(admin)/configuracoes/slug-editor.tsx
  - src/app/(admin)/dashboard/page.tsx
  - src/app/(admin)/esqueci-senha/page.tsx
  - src/app/(admin)/login/page.tsx
  - src/app/(admin)/onboarding/onboarding-wizard.tsx
  - src/app/(admin)/produtos/[id]/editar/page.tsx
  - src/app/(admin)/produtos/novo/page.tsx
  - src/app/(admin)/produtos/page.tsx
  - src/app/(admin)/produtos/photo-uploader.tsx
  - src/app/(admin)/produtos/product-form.tsx
  - src/app/(admin)/produtos/product-list.tsx
  - src/app/(admin)/produtos/product-toolbar.tsx
  - src/app/(admin)/produtos/size-grid.tsx
  - src/app/(admin)/redefinir-senha/page.tsx
  - src/app/loja/[slug]/[produto]/product-not-found-content.tsx
  - src/app/loja/[slug]/[produto]/product-order-panel.tsx
  - src/app/loja/[slug]/image-with-fallback.tsx
  - src/app/loja/[slug]/load-more-button.tsx
  - src/app/loja/[slug]/page.tsx
  - src/app/loja/[slug]/pagination-numbered.tsx
  - src/app/loja/[slug]/product-card.tsx
  - src/app/loja/[slug]/product-filters.tsx
  - src/app/loja/[slug]/store-hero.tsx
  - .planning/phases/02-link-compartilh-vel-da-vitrine/02-UI-SPEC.md
  - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md
  - .planning/phases/05-fluxo-de-pedido-no-whatsapp-cr-tico/05-UI-SPEC.md
  - .planning/phases/06-m-tricas-e-dashboard/06-UI-SPEC.md
  - .planning/PROJECT.md

must_haves:
  truths:
    - "Nenhuma ocorrencia ativa dos hex antigos (accent, tinta escura, fundo secundario) permanece em src/ nem nos 4 UI-SPEC nem em PROJECT.md"
    - "Vitrine publica e painel admin renderizam com a nova paleta azul: accent #0D21A1, tinta escura #000000, fundo secundario #E7F2FD"
    - "O seed default de accentColor no onboarding e nas configuracoes e #0D21A1"
    - "O fallback inline de cor de destaque da store-hero e #000000 quando accentColor e nulo"
    - "npx tsc --noEmit passa sem novos erros (arbitrary values do Tailwind seguem sintaticamente validos)"
  artifacts:
    - "27 arquivos .tsx de src/ com os novos hex"
    - ".planning/PROJECT.md com a linha canonica de paleta atualizada (hex + descricao em portugues)"
    - "4 UI-SPEC (02, 03, 05, 06) com hex e adjetivos de cor coerentes com a paleta azul"
  key_links:
    - "store-hero.tsx: fallback inline store.accentColor ?? cor-de-tinta-escura usa #000000"
    - "onboarding-wizard.tsx e settings-form.tsx: seed default accentColor usa #0D21A1"
---

<objective>
Rebrand de cores mecanico: substituir a paleta verde pela nova paleta azul em todo o app — codigo-fonte real (src/) e docs de planejamento (UI-SPEC + PROJECT.md).

Mapeamento de hex (FECHADO pelo usuario, NAO abrir para discussao):
- accent `#00C46A` (CTAs, botoes primarios, foco de input, badges disponivel/capa, indicador de nav ativo, dot de disponibilidade) -> `#0D21A1` (Vivid Royal)
- tinta escura `#0D3D2B` (headings h1, botoes outline/secundarios, hover de outline) -> `#000000` (preto puro)
- fundo secundario `#F5F5F3` (bordas, divisores, backgrounds de secao) -> `#E7F2FD` (Alice Blue)
- `#FFFFFF`, `#FF4D4D`, `#111111`, `#6B6B6B` -> SEM mudanca

Purpose: Aplicar a nova identidade visual azul de forma consistente antes de qualquer trabalho novo de UI, sem refatorar nada alem da troca de paleta.
Output: 27 arquivos .tsx atualizados, 4 UI-SPEC e PROJECT.md atualizados, typecheck limpo, greps de verificacao zerados para os hex antigos.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Notas de escopo confirmadas por grep durante o planejamento:
- Todas as ocorrencias em src/ estao em arquivos `.tsx` (nenhum `.css`/config), todas em MAIUSCULAS e sempre com prefixo `#` (173 tokens em 27 arquivos).
- A lista de arquivos mapeada por um explorador anterior estava incompleta: o grep encontrou 2 arquivos adicionais nao listados (`src/app/loja/[slug]/image-with-fallback.tsx` e `src/app/loja/[slug]/page.tsx`). Por isso o metodo abaixo coleta a lista real via grep em vez de usar uma lista fixa.
- As cores novas (`#0D21A1`, `#000000`, `#E7F2FD`) ainda nao aparecem em lugar nenhum (0 ocorrencias) — a troca e limpa, sem colisao.
</context>

<tasks>

<!-- Marcadores de allowlist para a disciplina de comment-text (#429): os literais abaixo aparecem nos <action> como lado de BUSCA das substituicoes (estao sendo REMOVIDOS, nao escritos), e tambem sao alvo de negative-grep nas verificacoes. -->
<!-- planner-discipline-allow: 00C46A -->
<!-- planner-discipline-allow: 0D3D2B -->
<!-- planner-discipline-allow: F5F5F3 -->
<!-- planner-discipline-allow: dark green -->
<!-- planner-discipline-allow: accent green -->
<!-- planner-discipline-allow: green dot -->

<task type="auto">
  <name>Task 1: Trocar os hex da paleta em todo o codigo-fonte (src/)</name>
  <files>Todos os arquivos .tsx sob src/ que contem os hex antigos (27 arquivos, coletados via grep em tempo de execucao)</files>
  <action>
Substituir mecanicamente os tres hex antigos pelos novos em todos os arquivos `.tsx` de `src/`. NAO editar arquivo por arquivo a partir de uma lista fixa — colete a lista real primeiro, porque ha ocorrencias fora da lista originalmente mapeada.

Passo 1 (coletar a lista real e conferir): rode `grep -rlE '#(00C46A|0D3D2B|F5F5F3)' src/` para listar os arquivos afetados. Deve incluir tambem `src/app/loja/[slug]/image-with-fallback.tsx` e `src/app/loja/[slug]/page.tsx`.

Passo 2 (aplicar a troca): rode `grep -rlZ -E '#(00C46A|0D3D2B|F5F5F3)' src/ | xargs -0 perl -pi -e 's/#00C46A/#0D21A1/gi; s/#0D3D2B/#000000/gi; s/#F5F5F3/#E7F2FD/gi'`.

Mapeamento FECHADO (nao abrir para discussao): accent `#00C46A`->`#0D21A1`; tinta escura `#0D3D2B`->`#000000`; fundo secundario `#F5F5F3`->`#E7F2FD`. A busca e case-insensitive (flag `i`) por seguranca, embora hoje todas as ocorrencias sejam maiusculas.

Isso cobre automaticamente, por serem apenas instancias dessas mesmas strings: o seed default `accentColor` `#00C46A`->`#0D21A1` em `onboarding-wizard.tsx` e `settings-form.tsx`, e o fallback inline `store.accentColor ?? "#0D3D2B"`->`"#000000"` em `store-hero.tsx`. NAO exige tratamento especial.

NAO alterar `#FFFFFF`, `#FF4D4D`, `#111111`, `#6B6B6B`. NAO refatorar, renomear ou tocar em qualquer outra coisa alem da string de cor.
  </action>
  <verify>
    <automated>test "$(grep -riE '00C46A|0D3D2B|F5F5F3' src/ | wc -l | tr -d ' ')" = "0" && npx tsc --noEmit</automated>
  </verify>
  <done>`grep -riE "00C46A|0D3D2B|F5F5F3" src/` retorna zero ocorrencias; `grep -riE "0D21A1|E7F2FD" src/` retorna >0 (novas cores presentes); `npx tsc --noEmit` termina sem novos erros. O seed default de accentColor em onboarding/configuracoes e `#0D21A1` e o fallback de store-hero e `#000000`.</done>
</task>

<task type="auto">
  <name>Task 2: Trocar os hex + adjetivos de cor nos docs de planejamento</name>
  <files>.planning/phases/02-link-compartilh-vel-da-vitrine/02-UI-SPEC.md, .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md, .planning/phases/05-fluxo-de-pedido-no-whatsapp-cr-tico/05-UI-SPEC.md, .planning/phases/06-m-tricas-e-dashboard/06-UI-SPEC.md, .planning/PROJECT.md</files>
  <action>
Passo 1 (troca mecanica de hex nos 5 docs): rode `grep -rlZ -E '#(00C46A|0D3D2B|F5F5F3)' .planning/phases/*/*-UI-SPEC.md .planning/PROJECT.md | xargs -0 perl -pi -e 's/#00C46A/#0D21A1/gi; s/#0D3D2B/#000000/gi; s/#F5F5F3/#E7F2FD/gi'`. Isso troca os hex nas tabelas de cor mantendo a estrutura e a semantica das tabelas intactas.

Passo 2 (linha canonica de paleta em PROJECT.md — mantendo a descricao em portugues verdadeira): edite a UNICA linha do PROJECT.md que comeca com `- Paleta:` (os hex ja foram trocados no passo 1). Ajuste os nomes de cor em portugues para nao ficarem falsos: `verde escuro`->`preto puro`, `verde vibrante`->`azul royal`, `cinza claro`->`azul claro`. Resultado esperado exato da linha: `- Paleta: preto puro `#000000` (fundo/destaque), azul royal `#0D21A1` (CTAs, badges disponivel), branco `#FFFFFF`, azul claro `#E7F2FD`, preto suave `#111111`, cinza medio `#6B6B6B`, vermelho suave `#FF4D4D` (badge esgotado)` (preservando os acentos originais do documento).

Passo 3 (adjetivos de cor nos 4 UI-SPEC — para nao restar texto tipo "dark green" descrevendo um token agora azul/preto): rode `grep -rlZ -iE 'dark green|accent green|green dot|solid green|existing green|red or green' .planning/phases/*/*-UI-SPEC.md | xargs -0 perl -pi -e 's/dark green/black/gi; s/accent green/accent blue/gi; s/green dot/blue dot/gi; s/solid green/solid blue/gi; s/existing green/existing blue/gi; s/red or green/red or blue/gi'`. Essas 6 frases cobrem exatamente as 12 ocorrencias de "green" mapeadas nos UI-SPEC (todas descrevem o token accent ou o token de tinta escura). NAO toque em nenhum outro texto e NAO reescreva a estrutura dos documentos.
  </action>
  <verify>
    <automated>test "$(grep -riE '00C46A|0D3D2B|F5F5F3' .planning/phases/*/*-UI-SPEC.md .planning/PROJECT.md | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>`grep -riE "00C46A|0D3D2B|F5F5F3" .planning/phases/*/*-UI-SPEC.md .planning/PROJECT.md` retorna zero; a linha `- Paleta:` do PROJECT.md descreve preto puro/azul royal/azul claro com os hex novos; nenhum adjetivo de cor nos UI-SPEC descreve mais um token azul/preto como "green" (grep por `dark green|accent green|green dot|solid green|existing green` nos UI-SPEC retorna zero).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhuma nova fronteira de confianca. A mudanca e uma substituicao de strings de cor literais (hex e adjetivos de cor); nao introduz entradas de usuario, dependencias, endpoints, nem altera fluxo de dados ou controle.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | Substituicao em massa via perl atingir string nao intencionada | low | mitigate | Padroes de busca sao ancorados em hex distintos de 6 digitos com prefixo `#` e frases de cor multi-palavra; verificacao final por grep confirma zero residuos e presenca das cores novas; `npx tsc --noEmit` garante integridade sintatica |
| T-quick-SC | Tampering | npm/pip/cargo installs | n/a | accept | Nenhuma instalacao de pacote nesta tarefa — sem superficie de supply-chain |
</threat_model>

<verification>
Gate final (rodar apos as duas tasks):

1. `grep -ri "00C46A\|0D3D2B" src/` — zero ocorrencias (criterio exato do pedido).
2. `grep -riE "00C46A|0D3D2B|F5F5F3" src/ .planning/phases/*/*-UI-SPEC.md .planning/PROJECT.md` — zero ocorrencias (inclui o fundo secundario e os docs).
3. `grep -riE "0D21A1|E7F2FD" src/` — >0 ocorrencias (sanity: as cores novas foram de fato aplicadas).
4. `npx tsc --noEmit` — sem novos erros (arbitrary values do Tailwind como `bg-[#0D21A1]` seguem validos).
5. `grep -riE "dark green|accent green|green dot|solid green|existing green" .planning/phases/*/*-UI-SPEC.md` — zero (adjetivos de cor coerentes com a paleta azul).
</verification>

<success_criteria>
- Zero ocorrencias ativas de `#00C46A`, `#0D3D2B` e `#F5F5F3` em src/ e nos docs alvo.
- Novas cores `#0D21A1` / `#000000` / `#E7F2FD` presentes onde as antigas estavam.
- `npx tsc --noEmit` limpo.
- Linha canonica de paleta do PROJECT.md descreve a nova paleta em portugues sem termos "verde".
- Nenhuma alteracao alem da troca de paleta (sem refatoracao).
</success_criteria>

<output>
Create `.planning/quick/260715-du7-rebrand-de-cores-substituir-a-paleta-ver/260715-du7-SUMMARY.md` when done.
</output>
