---
phase: 06-m-tricas-e-dashboard
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/lib/dashboard/metrics.ts
  - src/app/(admin)/dashboard/page.tsx
  - tests/dashboard/metrics-aggregation.test.ts
autonomous: true
requirements: [MTR-01, MTR-02]
must_haves:
  truths:
    - "O dashboard exibe 4 cards de resumo — Total de produtos, Disponíveis, Esgotados, Acessos — com números corretos (os 3 primeiros derivados de `queryProducts`, 'Acessos' = count de `pageviews` com `product_id` null)"
    - "O dashboard exibe uma lista de 'Produtos recentes' (5 mais recentes por created_at)"
    - "O dashboard exibe DUAS listas Top-10 separadas e paralelas: 'Mais visualizados' (de `product_pageview_counts`) e 'Cliques no WhatsApp' (de `product_order_click_counts`), nunca fundidas num único número (D-08/D-09)"
  artifacts:
    - src/lib/dashboard/metrics.ts
    - src/app/(admin)/dashboard/page.tsx
    - tests/dashboard/metrics-aggregation.test.ts
  key_links:
    - "dashboard mantém `requireCompletedOnboarding()` no topo (inalcançável sem sessão + onboarding)"
    - "Top-10: consulta a view + join em memória com `products` para os nomes — nomes NUNCA embutidos na view"
    - "`limit(10)` é constante fixa no código, nunca aceita via input do usuário (V5, previne enumeração/DoS)"
    - "'Acessos' conta só `product_id is null` (D-01) — visualização de produto não soma no contador geral"
---

<objective>
Construir a metade de EXIBIÇÃO da fatia de métricas: o módulo `src/lib/dashboard/metrics.ts` com as agregações (contagem de acessos + as duas listas Top-10 via views) e a reescrita completa de `src/app/(admin)/dashboard/page.tsx`, substituindo o placeholder da Fase 1 pelos 4 cards de resumo, a lista de produtos recentes e as duas listas Top-10 paralelas.

Purpose: entrega MTR-01 (acessos, mais visualizados, cliques WhatsApp por produto) e MTR-02 (resumo total/disponível/esgotado/acessos + recentes) na única página `/dashboard` (D-04). Todos os números são totais desde sempre — all-time, sem janela de tempo (D-03). Reaproveita `queryProducts` para tudo que é produto (contadores + recentes, zero SQL nova, per RESEARCH §Don't Hand-Roll) e consome as views de 06-01 para os rankings Top-10 (D-10). Roda em paralelo com a captura (06-02) — arquivos disjuntos.

Output: dashboard real e dinâmico exibindo as métricas agregadas, com empty states corretos para loja nova.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-m-tricas-e-dashboard/06-CONTEXT.md
@.planning/phases/06-m-tricas-e-dashboard/06-RESEARCH.md
@.planning/phases/06-m-tricas-e-dashboard/06-PATTERNS.md
@.planning/phases/06-m-tricas-e-dashboard/06-UI-SPEC.md
@.planning/phases/06-m-tricas-e-dashboard/06-VALIDATION.md

# Analogs a reaproveitar/espelhar
@src/lib/products/list.ts
@src/app/(admin)/dashboard/page.tsx
@src/app/(admin)/produtos/product-list.tsx
@src/lib/currency/brl.ts
@tests/products/list-filter-sort.test.ts
@tests/rls/order-clicks-rls.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [Wave 0] Teste de agregação de métricas (falhando)</name>
  <read_first>
    - tests/rls/order-clicks-rls.test.ts (padrão de seed com duas contas + anon insert)
    - tests/products/list-filter-sort.test.ts (estilo de asserção sobre queryProducts)
    - tests/setup/supabase-test.ts (helpers de seed)
    - .planning/phases/06-m-tricas-e-dashboard/06-VALIDATION.md (§Wave 0 — escopo exato deste teste)
  </read_first>
  <behavior>
    - Semeando N pageviews de produto (product_id preenchido) distribuídos entre produtos, `product_pageview_counts` retorna a contagem correta por `product_id`, ordenável desc por `views`, truncável em 10.
    - Semeando pageviews de grid (product_id null) + de produto, `queryAccessCount(supabase, storeId)` retorna SÓ a contagem das linhas com `product_id` null (D-01) — visualizações de produto não somam.
    - `queryTopViewedProducts`/`queryTopOrderClickProducts` retornam no máximo 10 itens, ordenados desc pela métrica, cada item com o nome do produto resolvido (join com products).
    - Isolamento: nenhuma dessas funções retorna dado de outra loja (reforço de 06-01, via storeId passado).
  </behavior>
  <action>
    Criar `tests/dashboard/metrics-aggregation.test.ts` (integração contra Supabase real, environment node). Seedar uma loja com produtos publicados; semear pageviews via `createAnonClient()` insert em `pageviews` (mix de `product_id: null` e `product_id` preenchido, quantidades desiguais entre produtos para provar ordenação) e `order_clicks` (para a segunda lista). Asserções:
    1. `queryAccessCount(supabase, storeId)` = número exato de linhas `product_id null` semeadas (não conta as de produto).
    2. `queryTopViewedProducts(supabase, storeId)` retorna itens ordenados desc por `views`, `length <= 10`, e o item de maior contagem corresponde ao produto mais semeado; cada item traz `name` (não vazio).
    3. `queryTopOrderClickProducts(supabase, storeId)` idem para `clicks` a partir de `order_clicks`.
    4. Semear >10 produtos com pageviews distintos e provar truncamento em exatamente 10.
    Usar o `supabase` autenticado do dono (`seedAuthenticatedAccount(...).client`) para chamar as funções de `metrics.ts`. `afterAll` deleta a store. Importar de `@/lib/dashboard/metrics`. O teste DEVE falhar agora (módulo inexistente) — Wave 0.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboard/metrics-aggregation.test.ts 2>&1 | tail -20; echo "esperado: FALHA (metrics.ts ainda não existe — Wave 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/dashboard/metrics-aggregation.test.ts` existe e importa `queryAccessCount`, `queryTopViewedProducts`, `queryTopOrderClickProducts` de `@/lib/dashboard/metrics`.
    - Cobre: acessos = só product_id null; Top-10 ordenado desc; truncamento em 10; nome do produto resolvido.
    - Roda e FALHA por módulo inexistente.
  </acceptance_criteria>
  <done>O teste de agregação existe, cobre contagem de acessos, ordenação e truncamento Top-10 e resolução de nomes; falha por falta de metrics.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Módulo metrics.ts (acessos + Top-10 via views + join de nomes)</name>
  <read_first>
    - src/lib/products/list.ts (`queryProducts` — assinatura de função pura recebendo `supabase` já autenticado; padrão 2 queries + join em memória, linhas 91-119)
    - .planning/phases/06-m-tricas-e-dashboard/06-PATTERNS.md (§metrics.ts — shapes exatos das queries de view, count e join)
    - src/lib/database.types.ts (confirmar tipos de `product_pageview_counts`/`product_order_click_counts`)
  </read_first>
  <behavior>
    - `queryAccessCount(supabase, storeId)` → `number`: `count:"exact",head:true` sobre `pageviews` com `.eq("store_id", storeId).is("product_id", null)`.
    - `queryTopViewedProducts(supabase, storeId)` → array (máx 10) de `{ productId, name, secondary, views }`, ordenado desc por `views`.
    - `queryTopOrderClickProducts(supabase, storeId)` → idem com `clicks`.
  </behavior>
  <action>
    Criar `src/lib/dashboard/metrics.ts`. Funções PURAS que recebem `supabase: SupabaseClient<Database>` já autenticado (nunca criam client próprio — mesma disciplina de `queryProducts`), importando os tipos de `@supabase/supabase-js` e `@/lib/database.types`.

    `queryAccessCount(supabase, storeId: string): Promise<number>` — `const { count } = await supabase.from("pageviews").select("id", { count: "exact", head: true }).eq("store_id", storeId).is("product_id", null)`; retornar `count ?? 0`. Só conta acessos ao grid (D-01).

    `queryTopViewedProducts(supabase, storeId: string)` — consultar `product_pageview_counts`: `.select("product_id, views").eq("store_id", storeId).order("views", { ascending: false }).limit(10)`. Depois, join em memória com `products`: `.select("id, name, brand, brand_other, line").in("id", ids)` — NUNCA embutir nome na view. Mapear cada linha ranqueada para `{ productId, name, secondary, views }`, preservando a ordem desc da view (montar um Map id→produto e iterar sobre o resultado da view). `secondary` = a linha secundária de marca/linha no mesmo espírito do `secondaryLine` de `product-list.tsx` (ex.: `brand_other ?? brand` + ` line` quando houver). `limit(10)` é constante fixa (D-10, Top-10) — NUNCA parametrizar por input do usuário.

    `queryTopOrderClickProducts(supabase, storeId: string)` — idêntico, contra `product_order_click_counts`, campo `clicks`, retornando `{ productId, name, secondary, clicks }`.

    Exportar tipos de retorno (`TopViewedProduct`, `TopOrderClickProduct`) para uso no page. NÃO reimplementar total/disponível/esgotado/recentes aqui — esses vêm de `queryProducts` chamado no page (RESEARCH §Don't Hand-Roll).
  </action>
  <verify>
    <automated>test -f src/lib/dashboard/metrics.ts && grep -q "limit(10)" src/lib/dashboard/metrics.ts && grep -q "product_pageview_counts" src/lib/dashboard/metrics.ts && grep -q "product_order_click_counts" src/lib/dashboard/metrics.ts && npx tsc --noEmit && npx vitest run tests/dashboard/metrics-aggregation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `metrics.ts` exporta `queryAccessCount`, `queryTopViewedProducts`, `queryTopOrderClickProducts`.
    - `queryAccessCount` filtra `.is("product_id", null)`.
    - Ambas as Top-10 usam `.limit(10)` literal e fazem join em memória com `products` (grep: sem `.select("*, products` embed/nesting).
    - `tests/dashboard/metrics-aggregation.test.ts` VERDE.
    - `npx tsc --noEmit` passa.
  </acceptance_criteria>
  <done>metrics.ts fornece a contagem de acessos (só grid) e as duas listas Top-10 (view + join de nomes em memória, limit fixo em 10), com o teste de agregação verde.</done>
</task>

<task type="auto">
  <name>Task 3: Reescrever dashboard/page.tsx (cards + recentes + 2 Top-10)</name>
  <read_first>
    - src/app/(admin)/dashboard/page.tsx (placeholder atual — preservar o gate `requireCompletedOnboarding()`)
    - src/app/(admin)/produtos/product-list.tsx (shell de row, thumbnail `coverUrl`/`ImageOff`, `secondaryLine`, dot de disponibilidade)
    - src/lib/products/list.ts (`queryProducts` — reusar para total/disponível/esgotado/recentes)
    - src/lib/currency/brl.ts (`formatBRLPrice` para preços na lista de recentes)
    - .planning/phases/06-m-tricas-e-dashboard/06-UI-SPEC.md (contrato completo: stat cards, Top-10 list, produtos recentes, cores, tipografia, copy dos empty states)
  </read_first>
  <action>
    Reescrever `src/app/(admin)/dashboard/page.tsx` do zero (Server Component async, totalmente dinâmico, sem `"use cache"`). Manter `await requireCompletedOnboarding()` como PRIMEIRA linha do corpo (inalterado). Resolver a store do dono (`supabase.auth.getUser()` → `stores.select("id").eq("owner_id", ...).single()`, mesmo padrão de `produtos/page.tsx`; `redirect("/onboarding")` se ausente).

    Dados: chamar `queryProducts(supabase, store.id, {})` UMA vez — derivar em memória `totalProdutos = produtos.length`, `disponiveis = produtos.filter(p => p.disponivel).length`, `esgotados = totalProdutos - disponiveis`, e `recentes = produtos.slice(0, 5)` (queryProducts já ordena por `created_at desc` por padrão). Chamar `queryAccessCount`, `queryTopViewedProducts`, `queryTopOrderClickProducts` de `@/lib/dashboard/metrics`. Para os thumbnails dos recentes, resolver `coverUrl` via `supabase.storage.from("product-images").getPublicUrl(coverPath)` (mesmo padrão de `produtos/page.tsx`).

    IMPORTANTE — manter o elemento raiz como `<main className="bg-white ...">` (o layout de sidebar ainda NÃO existe neste wave; a reorganização de rotas + troca de `<main>`→`<div>` acontece no plano 06-04). Isso mantém `tests/ui/dark-mode-contrast.test.ts` verde neste wave.

    UI conforme 06-UI-SPEC.md:
    - `<h1>` "Dashboard": `text-2xl font-bold text-[#000000]`.
    - 4 stat cards em `grid grid-cols-2 gap-3 md:grid-cols-4 md:gap-4`: "Total de produtos" (ícone `Package`, número `#111111`), "Disponíveis" (ícone+número `#0D21A1`), "Esgotados" (ícone `XCircle`, número `#6B6B6B` — muted, NUNCA vermelho), "Acessos" (ícone `Eye`, número `#111111`). Card = `rounded-lg border border-[#E7F2FD] bg-white p-4 flex flex-col gap-1`; número em `text-[28px] font-bold`; label em `text-sm text-[#6B6B6B]`. Cards sempre mostram `0` numérico em loja nova (nunca substituir por prosa).
    - Seção "Produtos recentes" (`text-xl font-medium`): 5 rows reusando o shell de `product-list.tsx` (thumbnail/`ImageOff`, nome, secundária de marca/linha, `formatBRLPrice(price)`, dot de status), cada row `<Link href={"/produtos/" + id + "/editar"}>`, sem botões editar/excluir. Empty state (0 produtos na loja): heading "Nenhum produto cadastrado ainda", body "Cadastre seu primeiro produto para começar a vender pelo WhatsApp." + botão "Novo produto" (`href="/produtos/novo"`) — copy verbatim de `produtos/page.tsx`.
    - Duas seções Top-10 SEPARADAS e paralelas (D-08/D-09, nunca fundidas): "Mais visualizados" (ícone `Eye`, contagem `views`) e "Cliques no WhatsApp" (ícone `MessageCircle`, contagem `clicks`). Cada row = rank `1`–`10` (`w-5 text-sm text-[#6B6B6B]`), nome (`truncate`, `#111111`), secundária opcional, contagem à direita com ícone `h-4 w-4 #6B6B6B` (nunca cor de acento). Cada row `<Link href={"/produtos/" + productId + "/editar"}>`, `min-h-11`. Empty states: "Mais visualizados" com 0 pageviews → heading "Ainda sem visualizações", body "Assim que sua vitrine receber acessos, os produtos mais vistos aparecem aqui."; "Cliques no WhatsApp" com 0 cliques → heading "Ainda sem cliques", body conforme 06-UI-SPEC.md.
    Novos ícones lucide-react: `Package`, `CheckCircle2`, `XCircle`, `Eye`, `MessageCircle`. Container interno pode usar largura maior que o `max-w-md` das páginas de formulário (o grid de 4 cards + listas pede mais largura) — escolha do executor conforme mobile-first.
  </action>
  <verify>
    <automated>grep -q "requireCompletedOnboarding" src/app/(admin)/dashboard/page.tsx && grep -q "queryTopViewedProducts" src/app/(admin)/dashboard/page.tsx && grep -q "queryTopOrderClickProducts" src/app/(admin)/dashboard/page.tsx && grep -q "<main" src/app/(admin)/dashboard/page.tsx && grep -q "bg-white" src/app/(admin)/dashboard/page.tsx && npx tsc --noEmit && npm run build 2>&1 | tail -5 && npx vitest run tests/ui/dark-mode-contrast.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/page.tsx` mantém `requireCompletedOnboarding()` e raiz `<main ... bg-white ...>` (dark-mode test permanece verde).
    - Consome `queryProducts` (contadores + recentes) e as 3 funções de `metrics.ts`.
    - Renderiza 4 stat cards, lista de 5 recentes, e DUAS listas Top-10 separadas (grep: "Mais visualizados" e "Cliques no WhatsApp" ambos presentes).
    - Empty states com a copy verbatim do 06-UI-SPEC.md.
    - `npx tsc --noEmit`, `npm run build` e `tests/ui/dark-mode-contrast.test.ts` verdes.
  </acceptance_criteria>
  <done>O dashboard exibe os 4 cards de resumo, a lista de recentes e as duas listas Top-10 paralelas com dados reais e empty states corretos, mantendo o gate de onboarding e o root `<main bg-white>`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessão autenticada (dono) → agregações | `storeId` é sempre derivado da sessão (`auth.getUser` → própria store), nunca de input; a RLS + `security_invoker` de 06-01 é a rede final |
| valor `limit` das listas Top-N | constante fixa no código, nunca aceita do usuário — evita enumeração/DoS por limite arbitrário |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Information Disclosure | `queryTopViewedProducts`/`queryTopOrderClickProducts`/`queryAccessCount` lendo métrica de outra loja | high | mitigate | todas as queries filtram `.eq("store_id", storeId)` com `storeId` derivado da sessão; views são `security_invoker=true` (06-01); provado pelo teste de agregação + isolamento de 06-01 |
| T-06-09 | Denial of Service | limite de linhas das listas Top-N | low | mitigate | `.limit(10)` é literal fixo (V5), nunca parametrizado por input — o app nunca busca mais de 10 linhas por lista |

Nota: nenhum pacote instalado nesta fase — sem threat de supply-chain.
</threat_model>

<verification>
- `npx vitest run tests/dashboard/metrics-aggregation.test.ts` verde.
- `npx vitest run tests/ui/dark-mode-contrast.test.ts` verde (root `<main bg-white>` preservado).
- `npm test` (suíte completa) verde.
- `npx tsc --noEmit` e `npm run build` limpos.
</verification>

<success_criteria>
`/dashboard` exibe, com dados reais e dinâmicos, os 4 cards de resumo (total/disponível/esgotado/acessos), a lista de 5 produtos recentes e as duas listas Top-10 paralelas (mais visualizados / cliques WhatsApp), com empty states corretos para loja nova — entregando MTR-01 e MTR-02 na página única (D-04).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 06-03)

- **Módulo:** `src/lib/dashboard/metrics.ts`
- **Funções:** `queryAccessCount(supabase, storeId): Promise<number>`, `queryTopViewedProducts(supabase, storeId)`, `queryTopOrderClickProducts(supabase, storeId)`
- **Tipos exportados:** `TopViewedProduct`, `TopOrderClickProduct`
- **Page reescrita:** `src/app/(admin)/dashboard/page.tsx` (cards de resumo, produtos recentes, 2 listas Top-10)
- **Teste:** `tests/dashboard/metrics-aggregation.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/06-m-tricas-e-dashboard/06-03-SUMMARY.md` when done
</output>
