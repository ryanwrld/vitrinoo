---
phase: 06-m-tricas-e-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0006_pageviews_and_metric_views.sql
  - src/lib/database.types.ts
  - tests/rls/pageviews-rls.test.ts
autonomous: false
requirements: [MTR-01, MTR-02]
must_haves:
  truths:
    - "Um visitante anônimo insere em `pageviews` um acesso ao grid (product_id null) e uma visualização de produto (product_id preenchido) contra uma loja com produto publicado; inserts com par product_id/store_id inconsistente ou para produto não publicado são REJEITADOS pelo WITH CHECK"
    - "O dono lê apenas os pageviews da própria loja; um segundo dono nunca lê os dados do primeiro (isolamento cross-tenant)"
    - "As views `product_pageview_counts` e `product_order_click_counts` retornam contagens agregadas isoladas por loja — um dono autenticado nunca lê agregados de outra loja (`security_invoker = true` respeita a RLS das tabelas base)"
  artifacts:
    - supabase/migrations/0006_pageviews_and_metric_views.sql
    - src/lib/database.types.ts
    - tests/rls/pageviews-rls.test.ts
  key_links:
    - "migration 0006 aplicada ao Postgres remoto via `supabase db push` — sem isso, build/typecheck passam (tipos vêm do arquivo, não do banco vivo) mas os testes de integração falham contra schema desatualizado"
    - "`security_invoker = true` presente em AMBAS as views — omiti-lo vaza agregados cross-tenant para qualquer sessão autenticada"
---

<objective>
Criar a fundação de dados de métricas desta fase: a migration `0006_pageviews_and_metric_views.sql` com a tabela nova `pageviews` (RLS espelhando `order_clicks`: anon só INSERE, dono lê só a própria loja) e as duas views agregadas Top-N (`product_pageview_counts`, `product_order_click_counts`, ambas `security_invoker = true`). Aplicar o [BLOCKING] push ao Postgres remoto, regenerar os tipos TypeScript, e provar com um teste de integração real o contrato multi-tenant completo: anon insere, dono lê escopado, e as views não vazam entre lojas.

Purpose: Nenhum mecanismo de pageview/acesso existe hoje no codebase (descoberta de escopo do 06-CONTEXT.md) — apesar do ROADMAP dizer `Depends on: Phase 4 (pageviews)`, a Fase 4 nunca implementou captura. Esta é a base de dados que a captura (06-02) e o dashboard (06-03) consomem. `order_clicks` (cliques WhatsApp) já existe desde a Fase 5 e só é agregado aqui via view, nunca recriado.

Output: tabela `pageviews`, duas views agregadas, tipos regenerados, teste de RLS/isolamento verde.

Nota de MVP mode: esta fase roda em modo vertical-slice. Este plano é a fundação da fatia "captura de métricas" — a razão de ser um plano próprio (e não fundido com a captura de 06-02) é o [BLOCKING] schema push, que por política é uma task separada e não pode compartilhar plano com a implementação que depende dos tipos regenerados. O incremento observável deste plano não é "só um CREATE TABLE": é o contrato multi-tenant de escrita anônima + leitura escopada + agregação isolada, provado ponta a ponta pelo teste de integração. As fatias verticais de captura e de exibição rodam em paralelo no Wave 2 (06-02 e 06-03), ambas dependentes só desta fundação.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-m-tricas-e-dashboard/06-CONTEXT.md
@.planning/phases/06-m-tricas-e-dashboard/06-RESEARCH.md
@.planning/phases/06-m-tricas-e-dashboard/06-PATTERNS.md
@.planning/phases/06-m-tricas-e-dashboard/06-VALIDATION.md

# Analogs a espelhar
@supabase/migrations/0005_order_clicks_and_public_whatsapp.sql
@tests/rls/order-clicks-rls.test.ts
@tests/setup/supabase-test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: [Wave 0] Teste de RLS + isolamento de views (falhando)</name>
  <read_first>
    - tests/rls/order-clicks-rls.test.ts (analog exato — copiar estrutura de seed, describe, casos)
    - tests/setup/supabase-test.ts (helpers `createAnonClient`, `seedAuthenticatedAccount`, `SeededAccount`)
    - .planning/phases/06-m-tricas-e-dashboard/06-RESEARCH.md (Pitfall 6 — teste de isolamento cross-tenant das views deve ser escrito com um SEGUNDO dono autenticado, nunca só com o client anon)
  </read_first>
  <action>
    Criar `tests/rls/pageviews-rls.test.ts` espelhando `tests/rls/order-clicks-rls.test.ts` linha a linha, adaptado para a tabela `pageviews` (product_id NULLABLE) e as duas views. Seedar duas contas reais (`seedAuthenticatedAccount("pageviews-loja-a"/"...-b")`), duas stores, e em cada store: um produto `published` e um `draft`. Casos obrigatórios, todos via `createAnonClient()` para os inserts anônimos:
    1. anon INSERE acesso ao grid válido: `{ store_id: storeAId, product_id: null }` contra loja com produto publicado → `error` null.
    2. anon INSERE visualização de produto válida: `{ store_id: storeAId, product_id: publishedProductAId }` → `error` null.
    3. anon INSERE visualização com par product_id/store_id inconsistente (`product_id` da loja B, `store_id` da loja A) → `error` NÃO null (rejeitado pelo WITH CHECK).
    4. anon INSERE visualização apontando para produto `draft` → `error` NÃO null.
    5. anon INSERE acesso ao grid (`product_id: null`) para uma store SEM nenhum produto publicado → `error` NÃO null (a defesa em profundidade exige ao menos um produto publicado, per D-01/Pattern 1).
    6. anon NUNCA lê nenhuma linha de `pageviews` (`.select("*")` retorna `[]`, sem policy SELECT para anon).
    7. dono da Loja A lê os próprios pageviews; os da Loja B retornam `[]` (isolamento).
    8. Isolamento das VIEWS (`security_invoker`): após seedar acessos/cliques em ambas as lojas, o dono da Loja A lendo `product_pageview_counts` e `product_order_click_counts` com `.eq("store_id", storeBId)` retorna `[]`, e `.eq("store_id", storeAId)` retorna só linhas da própria loja. Para cliques, seedar `order_clicks` via anon (tabela já existente).
    Fechar com `afterAll` deletando as duas stores (cascade limpa pageviews/order_clicks). O teste DEVE falhar agora (tabela/views inexistentes) — isso é esperado (Wave 0). NÃO usar `service_role`; seed via client autenticado do dono, mesma disciplina do analog.
  </action>
  <verify>
    <automated>npx vitest run tests/rls/pageviews-rls.test.ts 2>&1 | tail -20; echo "esperado: FALHA (tabela/views ainda não existem — Wave 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/rls/pageviews-rls.test.ts` existe e importa `createAnonClient`/`seedAuthenticatedAccount` de `../setup/supabase-test`.
    - Contém os 8 casos acima (grep: `product_pageview_counts`, `product_order_click_counts`, `product_id: null`, `draft`).
    - Roda e FALHA (relação `pageviews` inexistente) — comprovando que é um teste real, não vazio.
  </acceptance_criteria>
  <done>O arquivo de teste existe, cobre insert anônimo válido/inválido, ausência de leitura anônima, leitura escopada do dono e isolamento cross-tenant das duas views; falha por falta do schema.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0006 — tabela pageviews + RLS + 2 views agregadas</name>
  <read_first>
    - supabase/migrations/0005_order_clicks_and_public_whatsapp.sql (analog exato do bloco comentário + create table + enable RLS + policies anon/owner na mesma migration)
    - .planning/phases/06-m-tricas-e-dashboard/06-PATTERNS.md (§migration 0006 — SQL completo da tabela, índices e views)
    - .planning/phases/06-m-tricas-e-dashboard/06-RESEARCH.md (Pattern 1 e 2, Common Pitfall 6 — regra security_invoker)
  </read_first>
  <action>
    Criar `supabase/migrations/0006_pageviews_and_metric_views.sql`. Abrir com um bloco de comentário em prosa (convenção das migrations 0001–0005) explicando: o que muda, que `pageviews` espelha o padrão RLS de `order_clicks`, a distinção `product_id NULL` = acesso ao grid (D-01) vs. `product_id` preenchido = visualização de produto (alimenta D-08), e a nova regra não-negociável: toda `create view` de agregação leva `with (security_invoker = true)` na mesma declaração (análogo, para views, do `enable row level security` obrigatório em tabelas — Pitfall 6).

    Tabela: `create table pageviews` com colunas `id uuid primary key default gen_random_uuid()`, `store_id uuid not null references stores(id) on delete cascade`, `product_id uuid references products(id) on delete cascade` (NULLABLE), `created_at timestamptz not null default now()`. Índices: `pageviews_store_id_idx on (store_id)` e o índice parcial `pageviews_product_id_idx on (product_id) where product_id is not null`. Em seguida, na MESMA migration: `alter table pageviews enable row level security`.

    Policy de leitura do dono `owner_read_pageviews` (for select) usando `store_id in (select id from stores where owner_id = auth.uid())`. Policy de insert público `public_insert_pageviews` (for insert, to anon) com `with check` = ( `product_id is null and store_id in (select store_id from products where status = 'published')` ) OR ( `product_id in (select id from products where store_id = pageviews.store_id and status = 'published')` ). Sem policy SELECT para anon, por design.

    Duas views, cada uma com `with (security_invoker = true)`: `product_pageview_counts as select store_id, product_id, count(*) as views from pageviews where product_id is not null group by store_id, product_id`; e `product_order_click_counts as select store_id, product_id, count(*) as clicks from order_clicks group by store_id, product_id`. NÃO embutir nome de produto em nenhuma view — o join de nomes é feito na camada de app (06-03).

    NÃO rodar o push neste task — isso é a Task 3 [BLOCKING].
  </action>
  <verify>
    <automated>grep -c "security_invoker = true" supabase/migrations/0006_pageviews_and_metric_views.sql; test "$(grep -c 'security_invoker = true' supabase/migrations/0006_pageviews_and_metric_views.sql)" = "2" && echo "OK: 2 views com security_invoker" || echo "FALHA"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe; contém `create table pageviews`, `enable row level security`, `owner_read_pageviews`, `public_insert_pageviews`, e as duas views.
    - Exatamente 2 ocorrências de `security_invoker = true` (uma por view).
    - `product_id` da tabela é nullable (sem `not null`); índice parcial `where product_id is not null` presente.
    - Policy de insert é `to anon` e tem o ramo `product_id is null` E o ramo de produto publicado.
  </acceptance_criteria>
  <done>A migration cria a tabela com RLS anon-insert/owner-read e as duas views com security_invoker, na mesma migration, seguindo a convenção de 0005.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar migration ao Supabase remoto + regenerar tipos</name>
  <read_first>
    - .planning/phases/05-fluxo-de-pedido-no-whatsapp-cr-tico/05-01-PLAN.md (§Task 2 — padrão exato de `supabase db push` + `SUPABASE_ACCESS_TOKEN` + fallback manual + typegen, já usado 4 vezes neste projeto)
    - supabase/migrations/0006_pageviews_and_metric_views.sql (a migration a aplicar)
  </read_first>
  <action>
    [BLOCKING — rodar DEPOIS da Task 2 e ANTES de considerar a fase verificável.] Este é o schema push obrigatório: sem ele, build/typecheck passam (os tipos vêm de `src/lib/database.types.ts`, não do banco vivo), mas os testes de integração desta fase rodam contra o projeto Supabase remoto real (`tests/setup/supabase-test.ts` conecta via `NEXT_PUBLIC_SUPABASE_URL`/`ANON_KEY` de `.env.local`) — sem o push, o teste da Task 1 e o de 06-03 falhariam contra um schema desatualizado, um falso positivo de verificação.

    1. Aplicar a migration ao projeto remoto linkado: rodar `supabase db push` (o projeto já está linkado — migrations 0001–0005 já aplicadas). Se exigir auth em ambiente não-interativo, exportar `SUPABASE_ACCESS_TOKEN` primeiro. Se ainda assim exigir um prompt interativo impossível de suprimir, PARAR e sinalizar esta task para intervenção manual (este plano já é `autonomous: false` por esta razão). Fallback se a CLI falhar: aplicar o SQL de 0006 via SQL editor do Supabase Dashboard, depois ainda assim regenerar os tipos.
    2. Regenerar os tipos: `supabase gen types typescript --linked > src/lib/database.types.ts`. Os tipos DEVEM vir do schema vivo — nunca editar `database.types.ts` à mão. Confirmar que o arquivo regenerado contém `pageviews` em `Tables` e `product_pageview_counts`/`product_order_click_counts` em `Views`.
    3. Rodar o teste da Task 1 — agora deve ficar VERDE.
  </action>
  <verify>
    <automated>grep -q "pageviews" src/lib/database.types.ts && grep -q "product_pageview_counts" src/lib/database.types.ts && grep -q "product_order_click_counts" src/lib/database.types.ts && npx tsc --noEmit && npx vitest run tests/rls/pageviews-rls.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` aplicou 0006 ao banco remoto (ou fallback via Dashboard).
    - `src/lib/database.types.ts` regenerado contém `pageviews` (Tables) e as duas views (Views).
    - `npx tsc --noEmit` passa.
    - `tests/rls/pageviews-rls.test.ts` está VERDE (todos os 8 casos, incluindo isolamento das views).
    - Se o push exigiu prompt interativo impossível de suprimir, a task foi sinalizada para intervenção manual, não marcada concluída silenciosamente.
  </acceptance_criteria>
  <done>Schema aplicado ao remoto, tipos regenerados refletindo tabela+views, e o teste de RLS/isolamento verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente anônimo (`anon`) → Postgres (insert em `pageviews`) | A anon key é pública no bundle do cliente; a segurança real é a RLS `WITH CHECK`, nunca os argumentos enviados pela Server Action |
| sessão autenticada (dono) → views agregadas | Qualquer sessão `authenticated` poderia ler qualquer linha de uma view que rode com o papel do criador (bypass de RLS) se `security_invoker` estiver ausente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | views `product_pageview_counts`/`product_order_click_counts` | critical | mitigate | `with (security_invoker = true)` em ambas as views (Task 2); provado pelo caso 8 do teste (Task 1) — um dono nunca lê agregados de outra loja |
| T-06-02 | Spoofing/Tampering | policy `public_insert_pageviews` | high | mitigate | `WITH CHECK` cruza `product_id`/`store_id` e exige `status='published'`; rejeita par inconsistente e produto não publicado (casos 3/4 do teste) |
| T-06-03 | Information Disclosure | tentativa de pageview em URL de produto rascunho/oculto | medium | mitigate | mesmo `WITH CHECK` rejeita silenciosamente `product_id` que não seja produto publicado da loja — nenhum pageview é gravado para produto inexistente/rascunho |
| T-06-04 | Denial of Service / Repudiation | inflação de contadores via inserts anônimos repetidos | low | accept | MVP (D-03, "Analytics avançado" fora de escopo no PROJECT.md); consistente com o risco já aceito para `order_clicks` na Fase 5 — sem rate-limit/CAPTCHA nesta fase |

Nota: esta fase NÃO instala nenhum pacote (RESEARCH §Package Legitimacy Audit — "não aplicável"), portanto não há threat `T-06-SC` de supply-chain nem checkpoint de legitimidade.
</threat_model>

<verification>
- `npx vitest run tests/rls/pageviews-rls.test.ts` verde (8 casos).
- `src/lib/database.types.ts` contém `pageviews` + as duas views.
- `npx tsc --noEmit` limpo.
- `grep -c "security_invoker = true" supabase/migrations/0006_pageviews_and_metric_views.sql` = 2.
</verification>

<success_criteria>
Tabela `pageviews` e as duas views agregadas existem no banco remoto com RLS/security_invoker corretos, os tipos refletem o schema vivo, e o teste de integração prova o contrato anon-insert/owner-read/isolamento-de-view ponta a ponta.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 06-01)

- **Migration:** `supabase/migrations/0006_pageviews_and_metric_views.sql`
- **Tabela:** `pageviews` (colunas: `id`, `store_id`, `product_id` nullable, `created_at`)
- **Índices:** `pageviews_store_id_idx`, `pageviews_product_id_idx` (parcial, `where product_id is not null`)
- **Policies RLS:** `owner_read_pageviews` (select, owner), `public_insert_pageviews` (insert, anon, with check)
- **Views:** `product_pageview_counts` (colunas `store_id`, `product_id`, `views`), `product_order_click_counts` (colunas `store_id`, `product_id`, `clicks`) — ambas `security_invoker = true`
- **Tipos regenerados:** entradas `pageviews`, `product_pageview_counts`, `product_order_click_counts` em `src/lib/database.types.ts`
- **Teste:** `tests/rls/pageviews-rls.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/06-m-tricas-e-dashboard/06-01-SUMMARY.md` when done
</output>
