---
phase: 06-m-tricas-e-dashboard
plan: 04
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - src/components/admin-sidebar.tsx
  - src/app/(admin)/(painel)/layout.tsx
  - src/app/(admin)/(painel)/dashboard/page.tsx
  - src/app/(admin)/(painel)/produtos/page.tsx
  - src/app/(admin)/(painel)/produtos/novo/page.tsx
  - src/app/(admin)/(painel)/produtos/[id]/editar/page.tsx
  - src/app/(admin)/(painel)/produtos/photo-uploader.tsx
  - src/app/(admin)/(painel)/produtos/product-form.tsx
  - src/app/(admin)/(painel)/produtos/product-list.tsx
  - src/app/(admin)/(painel)/produtos/product-toolbar.tsx
  - src/app/(admin)/(painel)/produtos/size-grid.tsx
  - src/app/(admin)/(painel)/configuracoes/page.tsx
  - src/app/(admin)/(painel)/configuracoes/qr-code-panel.tsx
  - src/app/(admin)/(painel)/configuracoes/settings-form.tsx
  - src/app/(admin)/(painel)/configuracoes/slug-editor.tsx
  - tests/ui/dark-mode-contrast.test.ts
autonomous: false
requirements: [MTR-01]
must_haves:
  truths:
    - "Todo o painel autenticado (Dashboard/Produtos/Configurações) mostra uma sidebar fixa no desktop e um menu hambúrguer que abre um drawer `<dialog>` no mobile, com o link da página atual destacado (D-05/D-06)"
    - "'Sair da conta' aparece no rodapé da sidebar/drawer, separado dos links, e faz logout via o `signOutAction` já existente (D-07)"
    - "A sidebar NÃO aparece em `/login`, `/cadastro`, `/onboarding`, `/esqueci-senha`, `/redefinir-senha`"
    - "As URLs `/dashboard`, `/produtos`, `/produtos/novo`, `/produtos/[id]/editar`, `/configuracoes` continuam idênticas após a reorganização de rotas"
  artifacts:
    - src/components/admin-sidebar.tsx
    - src/app/(admin)/(painel)/layout.tsx
    - tests/ui/dark-mode-contrast.test.ts
  key_links:
    - "o grupo de rotas aninhado `(painel)` isola a sidebar às páginas autenticadas — NUNCA editar `(admin)/layout.tsx` (é compartilhado com as páginas públicas de auth)"
    - "um único `<main bg-white>` vive no `(painel)/layout.tsx`; cada página movida troca sua raiz `<main>`→`<div>` para evitar landmark `<main>` duplicado"
    - "a entrada `dashboard/page.tsx` do `tests/ui/dark-mode-contrast.test.ts` é repontada para `(painel)/layout.tsx` (onde `bg-white` no `<main>` passa a viver)"
---

<objective>
Criar o shell de navegação compartilhado do painel (D-05/D-06/D-07): a `AdminSidebar` (sidebar fixa no desktop via `<aside hidden md:flex>`, hambúrguer + drawer `<dialog>` nativo no mobile via `<button md:hidden>` + `.showModal()`), e um grupo de rotas aninhado `(admin)/(painel)/` com seu próprio `layout.tsx` que envolve só as páginas autenticadas. Mover `dashboard/`, `produtos/` e `configuracoes/` para dentro de `(painel)/`, trocar a raiz `<main>`→`<div>` de cada página movida (o `<main>` único passa a viver no layout), e repontar o teste de dark mode.

Purpose: substitui a navegação ad-hoc (botões soltos no dashboard) por navegação estruturada, sem vazar controles autenticados ("Sair da conta") para as telas públicas de login/cadastro. É a fatia de escopo ampliado explicitamente pedida pelo usuário (D-05). A correção idiomática do Next.js App Router é estrutural (grupo de rotas aninhado, invisível na URL), não lógica de runtime — nenhuma URL muda.

Output: painel autenticado com sidebar/drawer responsivo; páginas públicas de auth intocadas; suíte verde.

Roda por último (Wave 3) porque MOVE a `dashboard/page.tsx` reescrita em 06-03 — depende de 06-03 estar concluído.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-m-tricas-e-dashboard/06-CONTEXT.md
@.planning/phases/06-m-tricas-e-dashboard/06-RESEARCH.md
@.planning/phases/06-m-tricas-e-dashboard/06-PATTERNS.md
@.planning/phases/06-m-tricas-e-dashboard/06-UI-SPEC.md

# Analogs / arquivos a mover
@src/app/(admin)/layout.tsx
@src/lib/auth/actions.ts
@src/app/loja/[slug]/page.tsx
@src/app/(admin)/login/page.tsx
@tests/ui/dark-mode-contrast.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminSidebar (desktop aside + mobile dialog) + (painel)/layout.tsx</name>
  <read_first>
    - .planning/phases/06-m-tricas-e-dashboard/06-RESEARCH.md (Pattern 4 e 5 — código de referência completo do grupo de rotas, do layout e da AdminSidebar com `<dialog>`)
    - .planning/phases/06-m-tricas-e-dashboard/06-UI-SPEC.md (§Sidebar, §Drawer — cores, larguras w-56/w-64, active link, footer "Sair da conta", touch targets min-h-11)
    - src/lib/auth/actions.ts (`signOutAction` — reaproveitado no rodapé, nunca recriado)
    - src/app/loja/[slug]/page.tsx (técnica `hidden md:flex` / `flex md:hidden` já usada no projeto)
    - src/app/(admin)/layout.tsx (formato de layout Server Component compondo um componente + children)
  </read_first>
  <action>
    Criar `src/components/admin-sidebar.tsx` (`"use client"`, mesma pasta de `session-watcher.tsx`). Importar `useRef` de `react`, `Link` de `next/link`, `usePathname` de `next/navigation`, `Menu`/`X` de `lucide-react`, `signOutAction` de `@/lib/auth/actions`. Definir `NAV_ITEMS = [{ href: "/dashboard", label: "Dashboard" }, { href: "/produtos", label: "Produtos" }, { href: "/configuracoes", label: "Configurações" }]` (copy verbatim, D-07). Um sub-componente interno `NavLinks` mapeia `NAV_ITEMS` para `<Link>`s: link ativo (`usePathname().startsWith(item.href)`) recebe `font-medium text-[#0D21A1]` + indicador `border-l-2 border-[#0D21A1] pl-3 -ml-3`; inativo `text-[#6B6B6B]`; cada link `min-h-11` (touch target). Cuidado: `/dashboard` não deve acender como ativo quando em `/produtos` — usar `startsWith` sobre o href do item (o item `/dashboard` só bate em `/dashboard`).

    Renderizar (ambos sempre no DOM, CSS decide visibilidade):
    - Desktop `<aside className="hidden w-56 shrink-0 flex-col gap-6 border-r border-[#E7F2FD] bg-white p-4 md:flex">`: `<nav>` com `<NavLinks/>`, e no rodapé `<form action={signOutAction} className="mt-auto border-t border-[#E7F2FD] pt-4">` com `<button type="submit">Sair da conta</button>` estilizado neutro (`text-[#6B6B6B]`, NUNCA vermelho — logout não é destrutivo).
    - Mobile: `<button type="button" onClick={() => dialogRef.current?.showModal()} className="... min-h-11 min-w-11 md:hidden" aria-label="Abrir menu">` com ícone `Menu`; e `<dialog ref={dialogRef} aria-label="Menu de navegação" className="m-0 h-dvh max-h-none w-64 max-w-none bg-white p-4 backdrop:bg-black/50" onCancel={...}>` contendo o botão de fechar (`X`, `min-h-11 min-w-11`, `aria-label="Fechar menu"`, `onClick` → `dialogRef.current?.close()`), o mesmo `<NavLinks/>`, e o mesmo rodapé "Sair da conta". Usar `.showModal()` (não o atributo `open`) para ganhar focus-trap/Escape/`::backdrop` nativos. Fechar o drawer ao clicar num link (navegação) é aceitável via `onClick` nos links dentro do dialog.

    Criar `src/app/(admin)/(painel)/layout.tsx` (Server Component simples): `export default function PainelLayout({ children }: { children: ReactNode })` retornando `<div className="flex min-h-dvh"><AdminSidebar />` + o ÚNICO `<main className="min-h-dvh flex-1 bg-white">{children}</main>` + `</div>`. Importar `AdminSidebar` de `@/components/admin-sidebar`. Este é o único `<main>` das páginas do `(painel)`.
  </action>
  <verify>
    <automated>test -f src/components/admin-sidebar.tsx && test -f "src/app/(admin)/(painel)/layout.tsx" && grep -q "signOutAction" src/components/admin-sidebar.tsx && grep -q "showModal" src/components/admin-sidebar.tsx && grep -q "backdrop:bg-black" src/components/admin-sidebar.tsx && grep -q "<main" "src/app/(admin)/(painel)/layout.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `admin-sidebar.tsx` é `"use client"`, define `NAV_ITEMS` com Dashboard/Produtos/Configurações, usa `usePathname` para active state, `signOutAction` no rodapé, e um `<dialog>` com `.showModal()` + `backdrop:` para o mobile.
    - Desktop usa `<aside hidden ... md:flex>`, mobile usa `<button ... md:hidden>` + `<dialog>`.
    - `(painel)/layout.tsx` renderiza `<AdminSidebar/>` + um único `<main className="... bg-white">`.
    - `npx tsc --noEmit` passa.
  </acceptance_criteria>
  <done>A AdminSidebar entrega sidebar desktop + drawer mobile nativo com link ativo e footer de logout reusando signOutAction; o (painel)/layout.tsx a compõe com o único `<main bg-white>`.</done>
</task>

<task type="auto">
  <name>Task 2: Mover dashboard/produtos/configuracoes para (painel)/ + raiz `<main>`→`<div>` + repontar dark-mode test</name>
  <read_first>
    - .planning/phases/06-m-tricas-e-dashboard/06-RESEARCH.md (Pitfall 4 — não editar (admin)/layout.tsx; Pitfall 5 — `<main>` duplicado + quebra do dark-mode test)
    - tests/ui/dark-mode-contrast.test.ts (array `ADMIN_PAGES_WITH_MAIN` — a entrada do dashboard a repontar)
    - src/app/(admin)/produtos/page.tsx (confirmar raiz `<main className="bg-white ...">`)
    - src/app/(admin)/configuracoes/page.tsx (idem)
  </read_first>
  <action>
    Mover, preservando conteúdo, três subárvores inteiras de `src/app/(admin)/` para `src/app/(admin)/(painel)/` (usar `git mv` de diretório para preservar histórico):
    - `dashboard/` → `(painel)/dashboard/`
    - `produtos/` → `(painel)/produtos/` (inclui `page.tsx`, `novo/page.tsx`, `[id]/editar/page.tsx`, `photo-uploader.tsx`, `product-form.tsx`, `product-list.tsx`, `product-toolbar.tsx`, `size-grid.tsx`)
    - `configuracoes/` → `(painel)/configuracoes/` (inclui `page.tsx`, `qr-code-panel.tsx`, `settings-form.tsx`, `slug-editor.tsx`)
    Como `(painel)` é grupo de rotas (parênteses), NENHUMA URL muda; os imports relativos (`./product-list`, etc.) e absolutos (`@/...`) continuam válidos. NÃO mover `login/`, `cadastro/`, `onboarding/`, `esqueci-senha/`, `redefinir-senha/` nem `layout.tsx` — permanecem no `(admin)/` raiz. NÃO editar `(admin)/layout.tsx`.

    Em cada uma das 5 PÁGINAS movidas que hoje têm raiz `<main className="bg-white ...">` — `(painel)/dashboard/page.tsx`, `(painel)/produtos/page.tsx`, `(painel)/produtos/novo/page.tsx`, `(painel)/produtos/[id]/editar/page.tsx`, `(painel)/configuracoes/page.tsx` — trocar o elemento raiz de `<main ...>` para `<div ...>` (e o fechamento `</main>`→`</div>`), preservando todas as classes existentes. O `<main>` agora é único e vive no `(painel)/layout.tsx` (Pitfall 5, evita landmark duplicado). Rodar um grep de sanidade (`grep -rn "<main" src/app/(admin)/(painel)/`) para confirmar que nenhuma página movida ainda tem `<main>`.

    Atualizar `tests/ui/dark-mode-contrast.test.ts`: no array `ADMIN_PAGES_WITH_MAIN`, substituir a entrada `"src/app/(admin)/dashboard/page.tsx"` por `"src/app/(admin)/(painel)/layout.tsx"` (é onde o `<main ... bg-white>` passa a viver). As outras 5 entradas (onboarding-wizard, cadastro, login, esqueci-senha, redefinir-senha) NÃO mudam — continuam fora do `(painel)`, com seu próprio `<main>` intacto.
  </action>
  <verify>
    <automated>test -f "src/app/(admin)/(painel)/dashboard/page.tsx" && test -f "src/app/(admin)/(painel)/configuracoes/page.tsx" && test ! -e "src/app/(admin)/dashboard/page.tsx" && ! grep -rn "<main" "src/app/(admin)/(painel)/dashboard/page.tsx" "src/app/(admin)/(painel)/produtos/page.tsx" "src/app/(admin)/(painel)/configuracoes/page.tsx" && grep -q "(painel)/layout.tsx" tests/ui/dark-mode-contrast.test.ts && npx tsc --noEmit && npm run build 2>&1 | tail -5 && npm test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/`, `produtos/`, `configuracoes/` existem sob `src/app/(admin)/(painel)/` e NÃO existem mais sob `src/app/(admin)/` direto.
    - `login/`, `cadastro/`, `onboarding/`, `esqueci-senha/`, `redefinir-senha/`, `layout.tsx` permanecem em `(admin)/` raiz (inalterados).
    - Nenhuma das 5 páginas movidas tem `<main>` (grep vazio); o único `<main>` é o do `(painel)/layout.tsx`.
    - `tests/ui/dark-mode-contrast.test.ts` referencia `(painel)/layout.tsx` e passa.
    - `npx tsc --noEmit`, `npm run build` e `npm test` (suíte completa) verdes.
  </acceptance_criteria>
  <done>As três subárvores autenticadas vivem sob (painel)/ com raiz `<div>`, as páginas públicas de auth permanecem fora, o único `<main>` é o do layout, e o dark-mode test repontado passa junto com a suíte completa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Sidebar de navegação do painel: fixa no desktop (`<aside>` lateral com Dashboard/Produtos/Configurações + "Sair da conta" no rodapé), e hambúrguer que abre um drawer `<dialog>` nativo no mobile com os mesmos links. Reorganização de rotas para o grupo aninhado `(painel)` — nenhuma URL mudou. Páginas públicas de auth (login/cadastro/onboarding/esqueci-senha/redefinir-senha) NÃO recebem a sidebar.
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev` e abrir `/dashboard` autenticado.
    2. DESKTOP (viewport largo): confirmar sidebar fixa à esquerda com os 3 links; o link da página atual destacado (azul `#0D21A1` + barra à esquerda); clicar em Produtos e Configurações navega e move o destaque; "Sair da conta" no rodapé faz logout.
    3. MOBILE (viewport estreito / DevTools responsivo): a sidebar some; aparece o ícone de hambúrguer; tocar abre o drawer da esquerda; foco fica preso no drawer; tecla Escape fecha; clicar num link navega e fecha; "Sair da conta" no rodapé funciona; alvos de toque confortáveis (≥44px).
    4. Verificar que `/dashboard`, `/produtos`, `/produtos/novo`, `/configuracoes` respondem nas MESMAS URLs de antes.
    5. Deslogar e visitar `/login`, `/cadastro`, `/onboarding`, `/esqueci-senha`, `/redefinir-senha`: confirmar que NÃO há sidebar nem hambúrguer em nenhuma delas.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados (ex.: sidebar aparecendo no login, link ativo errado, drawer não fecha no Escape)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| páginas públicas de auth (`/login`, `/cadastro`, ...) vs. páginas autenticadas (`/dashboard`, `/produtos`, `/configuracoes`) | ambas vivem sob o grupo `(admin)`; só as autenticadas podem exibir controles como "Sair da conta" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Access Control (UI) | sidebar/"Sair da conta" vazando para telas públicas de login/cadastro | medium | mitigate | isolamento por ESTRUTURA de rotas — grupo aninhado `(painel)` só envolve páginas autenticadas; `(admin)/layout.tsx` NUNCA editado (Pitfall 4); verificado no checkpoint (passo 5) |
| T-06-11 | Denial of Service (a11y regressão) | dois `<main>` aninhados após introduzir o layout | low | mitigate | único `<main>` no `(painel)/layout.tsx`, raiz das páginas movidas trocada para `<div>` (Pitfall 5); verificado por grep na Task 2 |

Nota: nenhum pacote instalado nesta fase — sem threat de supply-chain. `signOutAction` é reaproveitado, sem nova superfície de auth.
</threat_model>

<verification>
- `npm test` (suíte completa) verde, incluindo `tests/ui/dark-mode-contrast.test.ts` repontado.
- `npx tsc --noEmit` e `npm run build` limpos.
- Nenhuma página sob `(painel)/` tem `<main>` (grep vazio); único `<main>` no layout.
- `(admin)/layout.tsx` inalterado (`git diff` vazio para esse arquivo).
- Checkpoint humano aprovado (sidebar/drawer responsivo + ausência nas páginas de auth).
</verification>

<success_criteria>
O painel autenticado tem navegação estruturada (sidebar desktop + drawer mobile) com link ativo e logout no rodapé, sem vazar para as telas públicas de auth, com todas as URLs preservadas e a suíte completa verde — fechando o escopo ampliado D-05/D-06/D-07.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 06-04)

- **Componente:** `AdminSidebar` em `src/components/admin-sidebar.tsx` (constante `NAV_ITEMS`; sub-componente `NavLinks`)
- **Route layout (novo):** `src/app/(admin)/(painel)/layout.tsx` (`PainelLayout`) — único `<main bg-white>` do painel
- **Grupo de rotas aninhado:** `src/app/(admin)/(painel)/` contendo `dashboard/`, `produtos/**`, `configuracoes/**` (movidos; raiz `<main>`→`<div>`)
- **Teste atualizado:** `tests/ui/dark-mode-contrast.test.ts` (entrada do dashboard repontada para `(painel)/layout.tsx`)
</artifacts_produced>

<output>
Create `.planning/phases/06-m-tricas-e-dashboard/06-04-SUMMARY.md` when done
</output>
