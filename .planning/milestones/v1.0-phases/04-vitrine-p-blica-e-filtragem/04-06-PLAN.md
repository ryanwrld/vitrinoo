---
phase: 04-vitrine-p-blica-e-filtragem
plan: 06
type: execute
wave: 5
depends_on: ["04-04", "04-05"]
files_modified:
  - src/lib/products/public-list.ts
  - src/app/loja/[slug]/page.tsx
  - src/lib/products/public-actions.ts
  - tests/storefront/sold-out-visibility.test.ts
autonomous: true
requirements: [VITR-03]
must_haves:
  truths:
    - "Produto disponível (algum tamanho em estoque) sempre aparece na vitrine, independentemente de qualquer configuração de esgotado (D-09/D-10/D-11)"
    - "Produto esgotado com exceção própria 'ocultar' (hide_when_sold_out=true) nunca aparece na vitrine pública, mesmo publicado"
    - "Produto esgotado sem exceção própria (hide_when_sold_out=null) segue o padrão global da loja (hide_sold_out_default)"
    - "Mudar o padrão global da loja (Plan 04-05) reflete imediatamente na vitrine pública para os produtos sem exceção própria (D-11, sem cache)"
  artifacts:
    - "tests/storefront/sold-out-visibility.test.ts"
  key_links:
    - "page.tsx/fetchNextPage resolvem store.hide_sold_out_default junto com o resto do hero -> passam para queryPublicProducts como parâmetro explícito (nunca uma segunda leitura duplicada dentro de public-list.ts)"
    - "queryPublicProducts aplica effectiveHide = product.hide_when_sold_out ?? storeHideSoldOutDefault -> visible = disponivel || !effectiveHide, resolvido uma única vez, nunca replicado na UI"
---

<objective>
Fechar o loop de D-09/D-10/D-11: `queryPublicProducts` passa a receber o `hide_sold_out_default` da loja (já carregado por `page.tsx`/`fetchNextPage` ao resolver a loja) e aplica a regra de visibilidade — `effectiveHide = product.hide_when_sold_out ?? storeHideSoldOutDefault`; produto aparece se `disponivel === true` OU `effectiveHide === false`. A regra é resolvida em UM único lugar (a função de query), nunca replicada em `product-card.tsx`/`product-grid.tsx`.

Purpose: o Plan 04-05 deu ao revendedor o controle de escrita; sem este plano, a vitrine pública ignora completamente essas colunas — VITR-03 (estado de estoque refletido corretamente) ficaria incompleto. Este é o último elo antes do fechamento da fase.
Output: a vitrine pública respeita a visibilidade configurável de esgotado, ponta a ponta (configurar no admin -> refletir na vitrine, sem cache, sem delay).
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-CONTEXT.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md
@src/lib/products/public-list.ts
@src/lib/products/public-actions.ts
@src/app/loja/[slug]/page.tsx
</context>

<phase_artifacts>
Ver `04-01-PLAN.md` §phase_artifacts para a lista cumulativa — este é o último plano da fase, fechando o consumo das colunas `hide_when_sold_out`/`hide_sold_out_default` criadas no Plan 04-01 e escritas no Plan 04-05. Nenhum arquivo novo é criado; apenas `queryPublicProducts`, `page.tsx` e `fetchNextPage` são estendidos.
</phase_artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aplicar a regra hide_when_sold_out/hide_sold_out_default em queryPublicProducts</name>
  <files>src/lib/products/public-list.ts, tests/storefront/sold-out-visibility.test.ts</files>
  <read_first>
    - src/lib/products/public-list.ts (arquivo dos Plans 04-02/04-03 — estender a assinatura e a lógica pós-join)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Pattern 4 — pseudocódigo exato da regra; nota sobre trade-off de contagem por página quando itens são filtrados pós-fetch)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md (§Regra "hide_when_sold_out" resolvida na query — nunca replicar no componente)
  </read_first>
  <behavior>
    - Produto com `disponivel=true` sempre aparece, mesmo com `hide_when_sold_out=true` (disponibilidade sempre vence a regra de ocultar)
    - Produto com `disponivel=false`, `hide_when_sold_out=null`, `storeHideSoldOutDefault=false` -> aparece (esmaecido)
    - Produto com `disponivel=false`, `hide_when_sold_out=null`, `storeHideSoldOutDefault=true` -> não aparece
    - Produto com `disponivel=false`, `hide_when_sold_out=true` (override), `storeHideSoldOutDefault=false` -> não aparece (override vence o padrão da loja)
    - Produto com `disponivel=false`, `hide_when_sold_out=false` (override), `storeHideSoldOutDefault=true` -> aparece (override vence o padrão da loja)
  </behavior>
  <action>
    Estender a assinatura de `queryPublicProducts` em `src/lib/products/public-list.ts` para `queryPublicProducts(supabase, storeId, params, storeHideSoldOutDefault: boolean)` — o quarto parâmetro é explícito (nunca uma segunda query interna a `stores`, já que o chamador — `page.tsx`/`fetchNextPage` — já carrega essa coluna ao resolver a loja pelo hero/slug). Adicionar `hide_when_sold_out` ao `.select(...)` de `products` já existente.

    Depois do join em memória (disponibilidade + coverPath, já implementado), aplicar o filtro de visibilidade sobre o array final: `const effectiveHide = product.hide_when_sold_out ?? storeHideSoldOutDefault; const visible = product.disponivel || !effectiveHide;` — manter só os produtos com `visible === true` antes de retornar. Documentar em comentário o trade-off assumido conscientemente (não uma redução de escopo): como o filtro roda DEPOIS da paginação `.range()`, uma página pode retornar menos que 20 itens visíveis quando parte do lote buscado é ocultada — `hasMore` continua correto em relação ao conjunto subjacente de publicados, e chamadas subsequentes de "carregar mais"/página seguinte eventualmente mostram todo o catálogo visível, nunca duplicando nem travando.

    Estender `tests/storefront/sold-out-visibility.test.ts` (novo arquivo) cobrindo exatamente os 5 casos do `<behavior>`, seedando produtos com as combinações de `disponivel`/`hide_when_sold_out`/`storeHideSoldOutDefault` diretamente (client autenticado do dono para o seed, `queryPublicProducts` chamado com o client anônimo ou autenticado — mesma disciplina dos demais testes desta fase).
  </action>
  <verify>
    <automated>npx vitest run tests/storefront/sold-out-visibility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run tests/storefront/sold-out-visibility.test.ts` passa: os 5 casos de visibilidade corretos; `queryPublicProducts` seleciona `hide_when_sold_out`; a lógica de `effectiveHide`/`visible` existe em UM único lugar neste arquivo (grep confirma que `hide_when_sold_out` não aparece em nenhum arquivo de `src/app/loja/[slug]/`).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire page.tsx + fetchNextPage — passar hide_sold_out_default da loja para a query</name>
  <files>src/app/loja/[slug]/page.tsx, src/lib/products/public-actions.ts</files>
  <read_first>
    - src/app/loja/[slug]/page.tsx (select de stores já existente — adicionar a coluna)
    - src/lib/products/public-actions.ts (fetchNextPage do Plan 04-04 — resolve a loja só por id hoje, precisa também da coluna)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Pattern 5 — D-11 já resolvido no Plan 04-05; aqui é só consumo de leitura)
  </read_first>
  <action>
    Em `page.tsx`, estender o `.select("id, name, logo_url, accent_color, tagline")` de `stores` para incluir `hide_sold_out_default`. Passar `store.hide_sold_out_default` como o novo quarto argumento em toda chamada a `queryPublicProducts` nesta rota.

    Em `public-actions.ts`, estender o `.select("id")` de `stores` dentro de `fetchNextPage` para `.select("id, hide_sold_out_default")`, e repassar `store.hide_sold_out_default` como quarto argumento na chamada a `queryPublicProducts` — mantendo a mesma disciplina de "nunca duas implementações divergentes de paginação/regra" já estabelecida nos Plans 04-02/04-04.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `page.tsx` seleciona `hide_sold_out_default` de `stores` e repassa para `queryPublicProducts`; `public-actions.ts` faz o mesmo dentro de `fetchNextPage`; `npx tsc --noEmit` passa.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queryPublicProducts (regra de visibilidade) | Única fronteira onde disponibilidade + configuração de ocultar são combinadas — nunca duplicada em componentes de apresentação |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | Information Disclosure | Regra de visibilidade incorreta expondo produto que deveria estar oculto (ou ocultando um disponível) | medium | mitigate | Resolvida em um único ponto (`queryPublicProducts`), testada com a matriz completa de 5 combinações em `sold-out-visibility.test.ts` |
| T-04-13 | Information Disclosure | `hide_sold_out_default` lido de forma obsoleta/cacheada, atrasando o reflexo de uma mudança global (quebraria VITR-03) | medium | mitigate | Lido a cada request via `createClient()` sem sessão, mesma disciplina "nunca `use cache`" de toda a rota; nenhuma camada de cache introduzida |
</threat_model>

<verification>
- `npx vitest run tests/storefront/sold-out-visibility.test.ts` verde
- `npx tsc --noEmit` passa
- `npm test` (suíte completa, ciente do bloqueador herdado de rate-limit de signUp já documentado desde a Fase 3)
</verification>

<success_criteria>
- Produto esgotado com exceção "ocultar" (por produto ou herdada do padrão global) nunca aparece na vitrine pública
- Mudar o padrão global da loja reflete imediatamente nos produtos sem exceção própria, sem delay perceptível
- Regra de visibilidade centralizada em queryPublicProducts, nunca duplicada na UI
</success_criteria>

<output>
Create `.planning/phases/04-vitrine-p-blica-e-filtragem/04-06-SUMMARY.md` when done
</output>
