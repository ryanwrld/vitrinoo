---
phase: 04-vitrine-p-blica-e-filtragem
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/validation/product.ts
  - src/lib/products/actions.ts
  - src/app/(admin)/produtos/product-form.tsx
  - src/app/(admin)/produtos/[id]/editar/page.tsx
  - src/lib/validation/onboarding.ts
  - src/lib/settings/actions.ts
  - src/app/(admin)/configuracoes/settings-form.tsx
  - src/app/(admin)/configuracoes/page.tsx
  - tests/products/hide-when-sold-out.test.ts
  - tests/settings/hide-sold-out-default.test.ts
autonomous: true
requirements: [VITR-03]
must_haves:
  truths:
    - "Revendedor configura, por produto, se ele deve continuar aparecendo esmaecido ou ser ocultado quando esgotado — 'Usar padrão da loja' é a opção default (D-09, sem exceção configurada = null)"
    - "Revendedor configura uma preferência global da loja (mostrar esmaecido vs. ocultar) em /configuracoes (D-09)"
    - "Mudar a preferência global apaga (reseta para null) todas as exceções por produto já configuradas daquela loja (D-11)"
    - "Reenviar o formulário de configurações sem mudar a preferência global NÃO reseta as exceções por produto (D-11 só dispara em mudança real)"
  artifacts:
    - "src/lib/validation/product.ts (campo hideWhenSoldOut)"
    - "src/lib/validation/onboarding.ts (campo hideSoldOutDefault)"
    - "tests/products/hide-when-sold-out.test.ts"
    - "tests/settings/hide-sold-out-default.test.ts"
  key_links:
    - "product-form.tsx select hideWhenSoldOut -> parseProductFormData -> products.hide_when_sold_out (null | true | false)"
    - "settings-form.tsx select hideSoldOutDefault -> saveStoreSettings -> stores.hide_sold_out_default + reset condicional de products.hide_when_sold_out=null quando o valor mudou (D-11)"
---

<objective>
Adicionar o campo `hide_when_sold_out` (por produto, três estados: herdar/mostrar/ocultar) em `product-form.tsx` e `[id]/editar/page.tsx`, e o campo `hide_sold_out_default` (preferência global da loja) em `settings-form.tsx` — incluindo a regra D-11: mudar a preferência global reseta (`null`) todas as exceções por produto já configuradas, mas só quando o valor realmente muda (nunca num resubmit sem alteração). Este plano entrega o lado de ESCRITA de D-09/D-10/D-11; o lado de LEITURA (a vitrine pública respeitando essa regra) é o Plan 04-06.

Purpose: sem este plano, as colunas criadas no Plan 04-01 ficam inacessíveis ao revendedor — a extensão de escopo de VITR-03 (visibilidade de esgotado configurável, D-09) exige tocar duas fases já fechadas (Fase 2 `settings-form.tsx`, Fase 3 `product-form.tsx`), o que é esperado e documentado no 04-CONTEXT.md, não uma reabertura de verificação daquelas fases.
Output: revendedor consegue configurar a exceção por produto e a preferência global da loja, com o reset condicional de D-11 funcionando corretamente.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-CONTEXT.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md
@src/lib/validation/product.ts
@src/lib/products/actions.ts
@src/app/(admin)/produtos/product-form.tsx
@src/app/(admin)/produtos/[id]/editar/page.tsx
@src/lib/validation/onboarding.ts
@src/lib/settings/actions.ts
@src/app/(admin)/configuracoes/settings-form.tsx
@tests/products/create-product.test.ts
@tests/settings/store-settings-update.test.ts
</context>

<phase_artifacts>
Ver `04-01-PLAN.md` §phase_artifacts para a lista cumulativa. Este plano NÃO cria arquivos novos de rota — estende arquivos já existentes de Fases 2/3 (`product-form.tsx`, `[id]/editar/page.tsx`, `settings-form.tsx`, `configuracoes/page.tsx`) e as Server Actions/schemas correspondentes, adicionando dois novos campos de formulário (`hideWhenSoldOut`, `hideSoldOutDefault`) que não existiam antes.
</phase_artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Campo hide_when_sold_out por produto (schema + Server Action)</name>
  <files>src/lib/validation/product.ts, src/lib/products/actions.ts, tests/products/hide-when-sold-out.test.ts</files>
  <read_first>
    - src/lib/validation/product.ts (productSchema — estender, mesmo padrão de `.optional()` já usado em `sole`/`category`)
    - src/lib/products/actions.ts (parseProductFormData, saveProduct, updateProduct — todas exigem o mesmo campo novo)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Pitfall 5 — hide_when_sold_out nullable, "" -> null, nunca "" -> false)
    - tests/products/create-product.test.ts (padrão de mock next/headers + signUp/saveOnboarding para testar Server Actions de produto contra o Supabase remoto real)
  </read_first>
  <behavior>
    - `saveProduct` com `hideWhenSoldOut` ausente ou `""` persiste `products.hide_when_sold_out = null` (herda o padrão da loja)
    - `saveProduct` com `hideWhenSoldOut = "true"` persiste `true` (ocultar quando esgotado)
    - `saveProduct` com `hideWhenSoldOut = "false"` persiste `false` (sempre mostrar esmaecido, mesmo esgotado)
    - `updateProduct` aplica a mesma regra de conversão e sobrescreve o valor anterior
  </behavior>
  <action>
    Em `src/lib/validation/product.ts`, adicionar ao `productSchema`: `hideWhenSoldOut: z.enum(["", "true", "false"]).optional()`.

    Em `src/lib/products/actions.ts`, estender `ParsedProductFields` com `hideWhenSoldOut: boolean | null`, e em `parseProductFormData` ler `formData.get("hideWhenSoldOut")`, validar via `productSchema.shape.hideWhenSoldOut.safeParse(...)`, e converter: `""` ou ausente -> `null`; `"true"` -> `true`; `"false"` -> `false`. Incluir `hide_when_sold_out: fields.hideWhenSoldOut` no payload de `.insert()` (`saveProduct`) e `.update()` (`updateProduct`) sobre `products` — mesma disciplina de "nunca duas implementações divergentes" já documentada para `parseProductFormData`.

    Criar `tests/products/hide-when-sold-out.test.ts` seguindo exatamente o padrão de mock (`vi.mock("next/headers")`/`vi.mock("next/navigation")`) e o helper `signUpAndCompleteOnboarding` de `tests/products/create-product.test.ts`. Casos: `saveProduct` sem o campo -> `hide_when_sold_out` null no banco; com `"true"` -> `true`; com `"false"` -> `false`; `updateProduct` mudando de `"true"` para `""` -> volta a `null`.
  </action>
  <verify>
    <automated>npx vitest run tests/products/hide-when-sold-out.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run tests/products/hide-when-sold-out.test.ts` passa: os 3 estados (`null`/`true`/`false`) persistidos corretamente via `saveProduct` e `updateProduct`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: UI do campo hide_when_sold_out — product-form.tsx + hidratação em editar/page.tsx</name>
  <files>src/app/(admin)/produtos/product-form.tsx, src/app/(admin)/produtos/[id]/editar/page.tsx</files>
  <read_first>
    - src/app/(admin)/produtos/product-form.tsx (padrão de campo select com label+erro, ex. campo `brand`/`sole`, linhas 156-173/210-226)
    - src/app/(admin)/produtos/[id]/editar/page.tsx (defaultValues montado a partir de `product.*` — hidratar o novo campo aqui)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md (§Extensão de product-form.tsx — snippet exato do select de 3 opções)
  </read_first>
  <action>
    Em `product-form.tsx`, adicionar um novo bloco `<div className="flex flex-col gap-1">` (mesma seção "Solado &amp; Categoria" ou uma nova seção "Visibilidade") com `<label htmlFor="hideWhenSoldOut">Exibir quando esgotado</label>` e `<select id="hideWhenSoldOut" {...register("hideWhenSoldOut")}>` com três `<option>`: `value=""` "Usar padrão da loja", `value="false"` "Sempre mostrar (esmaecido)", `value="true"` "Ocultar da vitrine". Adicionar `hideWhenSoldOut: defaultValues?.hideWhenSoldOut ?? ""` aos `defaultValues` do `useForm` (mesmo padrão dos demais campos opcionais). Incluir `formData.set("hideWhenSoldOut", values.hideWhenSoldOut ?? "")` em `onSubmit`, ao lado dos demais `formData.set(...)`.

    Em `[id]/editar/page.tsx`, adicionar ao objeto `defaultValues: Partial<ProductInput>` já existente: `hideWhenSoldOut: product.hide_when_sold_out === null ? "" : product.hide_when_sold_out ? "true" : "false"` — mapeia o valor `boolean | null` do banco de volta para a string do select, fechando o ciclo (mesma disciplina de `formatBRLPriceInput` fechando o ciclo string<->numeric<->string do campo `price`).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `product-form.tsx` contém um `<select id="hideWhenSoldOut">` com as 3 `<option>` descritas; `[id]/editar/page.tsx` mapeia `product.hide_when_sold_out` (boolean | null) para a string correta no `defaultValues`; `npx tsc --noEmit` passa.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preferência global da loja (hide_sold_out_default) + reset condicional de exceções (D-11)</name>
  <files>src/lib/validation/onboarding.ts, src/lib/settings/actions.ts, src/app/(admin)/configuracoes/settings-form.tsx, src/app/(admin)/configuracoes/page.tsx, tests/settings/hide-sold-out-default.test.ts</files>
  <read_first>
    - src/lib/validation/onboarding.ts (onboardingSchema — schema compartilhado entre o wizard de onboarding e settings-form.tsx, D-07 da Fase 2; adicionar campo `.optional()` para não quebrar o wizard)
    - src/lib/settings/actions.ts (saveStoreSettings, getOwnedStore — estender o update de stores)
    - src/app/(admin)/configuracoes/settings-form.tsx (padrão de campo + defaultValues; este componente é reescrito do zero na Fase 2, D-07 — não importa o wizard)
    - src/app/(admin)/configuracoes/page.tsx (linha ~29: `.select("id, name, slug, logo_url, accent_color, tagline")` sobre `stores` — NÃO inclui `hide_sold_out_default` hoje; e o objeto `store={{ name, accentColor, tagline }}` passado para `<SettingsForm>` — ambos precisam ganhar o novo campo)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Pattern 5 — reset condicional D-11, comparar valor anterior vs. novo ANTES de decidir resetar)
    - tests/settings/store-settings-update.test.ts (padrão de mock next/headers + signUp/saveOnboarding para testar saveStoreSettings)
  </read_first>
  <behavior>
    - `saveStoreSettings` com `hideSoldOutDefault="true"` (valor anterior era `false`) persiste `stores.hide_sold_out_default = true` E reseta TODOS os `products.hide_when_sold_out` daquela loja para `null`
    - `saveStoreSettings` chamado de novo com o MESMO valor (`"true"` novamente, sem mudança) NÃO reseta as exceções por produto já configuradas após a primeira chamada
    - `saveStoreSettings` sem o campo `hideSoldOutDefault` no FormData mantém o valor atual (não força `false`)
  </behavior>
  <action>
    Em `src/lib/validation/onboarding.ts`, adicionar ao `onboardingSchema`: `hideSoldOutDefault: z.enum(["true", "false"]).optional()` (optional para não exigir o campo no wizard de onboarding da Fase 1, que não o define).

    Em `src/lib/settings/actions.ts`, dentro de `saveStoreSettings`, ANTES do update de `stores`: buscar o valor atual via `owned.supabase.from("stores").select("hide_sold_out_default").eq("id", owned.storeId).single()`. Calcular `nextDefault = parsed.data.hideSoldOutDefault === "true"` (default para o valor atual se o campo não vier no FormData — nunca assumir `false` silenciosamente quando ausente). Incluir `hide_sold_out_default: nextDefault` no payload do `.update()` de `stores` já existente. Depois do update bem-sucedido, SE `nextDefault !== currentStore.hide_sold_out_default`, rodar um segundo `owned.supabase.from("products").update({ hide_when_sold_out: null }).eq("store_id", owned.storeId)` (D-11 — reset condicional, só em mudança real).

    Em `settings-form.tsx`, adicionar `<select id="hideSoldOutDefault" {...register("hideSoldOutDefault")}>` com duas `<option>`: `value="false"` "Não — mostrar esmaecido (padrão)", `value="true"` "Sim — ocultar da vitrine"; `defaultValues.hideSoldOutDefault: store.hideSoldOutDefault ? "true" : "false"` no `useForm`; `formData.set("hideSoldOutDefault", values.hideSoldOutDefault)` no `onSubmit`. A prop `SettingsFormProps.store` precisa ganhar `hideSoldOutDefault: boolean`.

    Em `src/app/(admin)/configuracoes/page.tsx` (dois ajustes obrigatórios — sem eles os `defaultValues` do form quebram silenciosamente, porque o dado nunca chega ao componente): (1) na query de `stores` (hoje, linha ~29, `.select("id, name, slug, logo_url, accent_color, tagline")`), adicionar `hide_sold_out_default` ao `select` → `.select("id, name, slug, logo_url, accent_color, tagline, hide_sold_out_default")`; (2) no objeto `store={{ ... }}` passado para `<SettingsForm>` (hoje só `name`/`accentColor`/`tagline`), adicionar `hideSoldOutDefault: store.hide_sold_out_default`. Confirmar explicitamente durante a execução que a coluna aparece no `select` ANTES de considerar a Task pronta.

    Criar `tests/settings/hide-sold-out-default.test.ts` (mesmo padrão de mock de `tests/settings/store-settings-update.test.ts`). Seed: signUp + onboarding; criar 2 produtos com `hide_when_sold_out` já setado (`true` e `false`) via o client autenticado do teste. Chamar `saveStoreSettings` mudando `hideSoldOutDefault` de `false` (padrão) para `true` — assert que ambos os produtos voltam a `hide_when_sold_out = null`. Chamar `saveStoreSettings` de novo com `hideSoldOutDefault="true"` (sem mudança) depois de configurar uma nova exceção manual num produto — assert que essa exceção NÃO é resetada.
  </action>
  <verify>
    <automated>npx vitest run tests/settings/hide-sold-out-default.test.ts && grep -q "hide_sold_out_default" "src/app/(admin)/configuracoes/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run tests/settings/hide-sold-out-default.test.ts` passa: mudança real de `hideSoldOutDefault` reseta todas as exceções por produto da loja; resubmit sem mudança preserva exceções configuradas depois da última mudança real. Além disso, `configuracoes/page.tsx` inclui `hide_sold_out_default` no `.select(...)` de `stores` E repassa `hideSoldOutDefault` no objeto `store` para `<SettingsForm>` (sem isso os `defaultValues` do form ficariam sempre no padrão, ignorando a preferência salva do revendedor).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| revendedor autenticado -> products/stores (novos campos) | Mesma fronteira RLS `owner_full_access_*` já existente — nenhuma fronteira nova, só novos campos dentro do escopo já protegido |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Tampering | parseProductFormData (hideWhenSoldOut aceito sem validação de enum) | medium | mitigate | Validado via `productSchema.shape.hideWhenSoldOut.safeParse` (enum `["", "true", "false"]`) antes de qualquer insert/update, mesma disciplina do restante do schema |
| T-04-11 | Broken Access Control | saveStoreSettings reset em lote de products.hide_when_sold_out (D-11) afetando produtos de outra loja | high | mitigate | `.eq("store_id", owned.storeId)` — `owned.storeId` vem de `getOwnedStore()` (RLS-scoped por `auth.uid()`), mesma fronteira já usada em todo update de `stores`/`products` no projeto |
</threat_model>

<verification>
- `npx vitest run tests/products/hide-when-sold-out.test.ts` verde
- `npx vitest run tests/settings/hide-sold-out-default.test.ts` verde
- `npx tsc --noEmit` passa
</verification>

<success_criteria>
- Revendedor configura exceção por produto (herdar/mostrar/ocultar) no formulário de produto
- Revendedor configura a preferência global da loja em /configuracoes
- Mudança real do padrão global reseta exceções por produto; resubmit sem mudança não reseta nada
</success_criteria>

<output>
Create `.planning/phases/04-vitrine-p-blica-e-filtragem/04-05-SUMMARY.md` when done
</output>
