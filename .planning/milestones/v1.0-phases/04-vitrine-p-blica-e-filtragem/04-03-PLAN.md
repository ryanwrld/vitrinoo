---
phase: 04-vitrine-p-blica-e-filtragem
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/lib/products/public-list.ts
  - src/app/loja/[slug]/product-filters.tsx
  - src/app/loja/[slug]/page.tsx
  - tests/storefront/list-filter-paginate.test.ts
autonomous: true
requirements: [VITR-02]
must_haves:
  truths:
    - "Cliente filtra produtos por marca/solado/modalidade com múltipla seleção na mesma categoria (ex.: Nike E Adidas ao mesmo tempo, D-02)"
    - "Cliente busca produtos por nome (busca parcial, case-insensitive, D-03)"
    - "Abrir a URL com os mesmos parâmetros de filtro reproduz exatamente a mesma visualização (searchParams como única fonte de verdade, nunca estado React paralelo)"
    - "Os chips de filtro e a busca permanecem fixos/visíveis no topo ao rolar a página (D-04)"
  artifacts:
    - "src/lib/products/public-list.ts (estendido: brand/sole/fulfillment multi-select + busca q)"
    - "src/app/loja/[slug]/product-filters.tsx"
  key_links:
    - "product-filters.tsx -> router.push reconstrói a URL a partir de currentParams (prop derivada de searchParams real) -> page.tsx -> queryPublicProducts"
    - "searchParams.getAll(\"brand\") (multi-valor) -> .in(\"brand\", brands) no Supabase -> nunca .eq() single-select (diferença deliberada do admin)"
---

<objective>
Estender `queryPublicProducts` para aceitar filtros multi-select de marca/solado/modalidade e busca por nome, e construir `product-filters.tsx`: chips sempre visíveis (nunca dropdown/drawer, D-01), múltipla seleção dentro da mesma categoria (D-02), busca por texto (D-03), fixos no topo ao rolar (D-04). A URL continua a única fonte de verdade dos filtros, mesmo padrão já estabelecido em `ProductToolbar` (Fase 3), adaptado para multi-select via `searchParams.getAll`.

Purpose: entrega VITR-02 como fatia vertical completa — cliente filtra e vê o resultado atualizado, com a URL refletindo e reproduzindo exatamente o estado filtrado (compartilhável). Sem essa fatia, o card grid do Plan 04-02 é só uma lista estática sem nenhuma forma de refinamento.
Output: cliente filtra/busca produtos na vitrine pública, com resultado e URL sincronizados.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-CONTEXT.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md
@src/app/(admin)/produtos/product-toolbar.tsx
@src/lib/hooks/use-debounce.ts
@src/lib/products/constants.ts
@src/lib/products/public-list.ts
@src/app/loja/[slug]/page.tsx
</context>

<phase_artifacts>
Ver `04-01-PLAN.md` §phase_artifacts para a lista cumulativa. Este plano cria `src/app/loja/[slug]/product-filters.tsx` e estende `queryPublicProducts` (novos campos em `QueryPublicProductsParams`: `brand?: string[]`, `sole?: string[]`, `fulfillment?: string[]`, `q?: string`) e `page.tsx` (parsing de searchParams multi-valor).
</phase_artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender queryPublicProducts com filtros multi-select + busca</name>
  <files>src/lib/products/public-list.ts, tests/storefront/list-filter-paginate.test.ts</files>
  <read_first>
    - src/lib/products/public-list.ts (arquivo do Plan 04-02 — estender, nunca recriar)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Pattern 3 — `.in("brand", brands)` em vez de `.eq()`; §Security Domain V5 — validar contra listas fixas antes de usar)
    - src/lib/products/constants.ts (BRANDS, SOLES, FULFILLMENTS — listas fixas para validação server-side)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md (§public-list.ts — divergência multi-select)
  </read_first>
  <behavior>
    - `queryPublicProducts(supabase, storeId, { brand: ["Nike", "Adidas"] })` retorna produtos cuja marca é Nike OU Adidas (multi-select, D-02) — nunca restrito a uma única marca
    - `queryPublicProducts(supabase, storeId, { sole: ["FG"] })` filtra por solado
    - `queryPublicProducts(supabase, storeId, { fulfillment: ["pronta_entrega"] })` filtra por modalidade
    - `queryPublicProducts(supabase, storeId, { q: "merc" })` retorna produtos cujo nome contém "merc" (case-insensitive, parcial — mesmo `ilike` do admin)
    - Filtros combinados (brand + sole + q) aplicam AND entre categorias, OR dentro da mesma categoria
    - Um valor de filtro fora das listas fixas (`BRANDS`/`SOLES`/`FULFILLMENTS`) é silenciosamente ignorado, nunca lançado como erro nem repassado cru ao SQL (Security Domain V5)
  </behavior>
  <action>
    Estender `QueryPublicProductsParams` em `src/lib/products/public-list.ts` adicionando `brand?: string[]`, `sole?: string[]`, `fulfillment?: string[]`, `q?: string` (mantendo `page?: number` já existente). Antes de montar a query, filtrar cada array recebido para manter só valores presentes em `BRANDS`/`SOLES`/`FULFILLMENTS` (import de `@/lib/products/constants`) — nunca confiar em `searchParams` não validado indo direto para `.in()`. Se `params.brand?.length` (após filtrar), aplicar `.in("brand", validBrands)`; mesma lógica para `sole`/`fulfillment`. Se `params.q`, aplicar `.ilike("name", \`%${params.q}%\`)`. Manter `status = 'published'` fixo e a técnica de paginação "buscar 21 mostrar 20" já implementada no Plan 04-02 — a ordem de aplicação dos filtros não deve alterar a lógica de paginação existente.

    Estender `tests/storefront/list-filter-paginate.test.ts` (mesmo arquivo do Plan 04-02) com novos casos: seed de produtos com marcas/solados/modalidades variadas, asserts de multi-select (`brand: ["Nike","Adidas"]` retorna ambas), busca por nome parcial, filtro combinado, e um valor de marca inválido/inexistente sendo ignorado sem erro.
  </action>
  <verify>
    <automated>npx vitest run tests/storefront/list-filter-paginate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run tests/storefront/list-filter-paginate.test.ts` passa incluindo os novos casos de filtro multi-select, busca e validação de valores inválidos. `queryPublicProducts` nunca usa `.eq("brand", ...)` para o campo `brand` (sempre `.in()`).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: product-filters.tsx — chips multi-select + busca, sticky, URL como fonte de verdade</name>
  <files>src/app/loja/[slug]/product-filters.tsx</files>
  <read_first>
    - src/app/(admin)/produtos/product-toolbar.tsx (analog — debounce da busca, disciplina "nunca estado próprio de filtro", padrão `navigate()`)
    - src/lib/hooks/use-debounce.ts (useDebouncedValue, reusar sem modificar)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Code Examples — BrandChip multi-select; §Pitfall 4 — resetar page ao mudar filtro)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md (§product-filters.tsx — divergência multi-select em relação ao product-toolbar.tsx)
  </read_first>
  <action>
    Criar `product-filters.tsx` (`"use client"`). Props: `{ slug: string; currentParams: { q?: string; brand: string[]; sole: string[]; fulfillment: string[] } }` (arrays já normalizados pelo `page.tsx`, nunca `string | string[]` bruto dentro deste componente). Container com `className="sticky top-0 z-10 flex flex-col gap-2 bg-white py-2"` (D-04 — fixo no topo).

    Busca por nome: `useState` local do texto digitado + `useDebouncedValue(searchInput, 400)` (mesmo hook, mesmo delay de `product-toolbar.tsx`); `useEffect` compara o valor debounced com `currentParams.q` e só dispara `navigate` quando diverge (mesma disciplina anti-loop de `product-toolbar.tsx` linhas 48-53).

    Chips de marca/solado/modalidade: renderizar um `<button type="button" aria-pressed={active}>` por valor de `BRANDS`/`SOLES`/`FULFILLMENTS` (import de `@/lib/products/constants`), classes condicionais via `clsx` (`border-[#00C46A] bg-[#00C46A] text-white` quando ativo, `border-[#F5F5F3] bg-white text-[#111111]` quando inativo). Toggle multi-select (D-02): `next = active ? current.filter(v => v !== value) : [...current, value]` — nunca substituir o array inteiro por um único valor.

    Função `navigate(overrides)` central: reconstrói `URLSearchParams` a partir de `currentParams` + `overrides`, usando `search.delete(chave)` seguido de `next.forEach(v => search.append(chave, v))` para cada array (produz múltiplos `?brand=Nike&brand=Adidas` na URL, nunca uma string CSV). SEMPRE `search.delete("page")` antes de `router.push` (D-06 — todo filtro reseta a paginação, Pitfall 4) — sem exceção, mesmo quando só a busca muda. `router.push(\`/loja/${slug}?${search.toString()}\`)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `product-filters.tsx` usa `searchParams.getAll`-style arrays (nunca `.get()` single-value para brand/sole/fulfillment); toda chamada de `navigate`/toggle inclui `search.delete("page")`; container tem classe `sticky`; `npx tsc --noEmit` passa.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire page.tsx — parsing multi-valor de searchParams + dois empty states</name>
  <files>src/app/loja/[slug]/page.tsx</files>
  <read_first>
    - src/app/loja/[slug]/page.tsx (arquivo do Plan 04-02 — estender)
    - src/app/(admin)/produtos/page.tsx (padrão de dois empty states: "sem produto nenhum" vs. "sem resultado para o filtro")
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Code Examples — toArray helper para normalizar string | string[] do searchParams)
  </read_first>
  <action>
    Estender o tipo de `searchParams` em `page.tsx` para `Promise<{ q?: string; brand?: string | string[]; sole?: string | string[]; fulfillment?: string | string[]; page?: string }>`. Adicionar a função `toArray(value: string | string[] | undefined): string[]` (retorna `[]` se `undefined`, `[value]` se string, o próprio array se já array) — usar para normalizar `brand`/`sole`/`fulfillment` antes de repassar a `queryPublicProducts` e a `<ProductFilters currentParams={...}>`.

    Passar os filtros normalizados para `queryPublicProducts(supabase, store.id, { q: sp.q, brand: toArray(sp.brand), sole: toArray(sp.sole), fulfillment: toArray(sp.fulfillment), page: 1 })`. Renderizar `<ProductFilters slug={slug} currentParams={{ q: sp.q, brand: toArray(sp.brand), sole: toArray(sp.sole), fulfillment: toArray(sp.fulfillment) }} />` logo abaixo do `<StoreHero>`.

    Diferenciar dois empty states (mesma disciplina do admin, `src/app/(admin)/produtos/page.tsx`): buscar a contagem total de produtos publicados da loja SEM nenhum filtro (`.select("id", { count: "exact", head: true }).eq("store_id", store.id).eq("status", "published")`) para saber se a loja tem QUALQUER produto publicado. Se `totalPublished === 0`, mostrar "Esta loja ainda não tem produtos disponíveis."; se `totalPublished > 0` mas o resultado filtrado veio vazio, mostrar "Nenhum produto encontrado com esse filtro." — nunca a mesma mensagem para os dois casos.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `page.tsx` contém a função `toArray`; `queryPublicProducts` recebe `brand`/`sole`/`fulfillment` sempre como array (nunca `string | string[]` bruto); as duas mensagens de empty state são visivelmente distintas no código-fonte; `npx tsc --noEmit` passa.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (searchParams não confiável) -> queryPublicProducts | Todo valor de filtro cruza essa fronteira antes de qualquer uso em `.in()`/`.ilike()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering / Information Disclosure | queryPublicProducts (valores de brand/sole/fulfillment não validados injetados via URL) | medium | mitigate | Valores filtrados contra `BRANDS`/`SOLES`/`FULFILLMENTS` (listas fixas) antes de qualquer `.in()`; valor inválido é ignorado silenciosamente, nunca propagado como erro nem interpolado cru |
| T-04-07 | Tampering | queryPublicProducts (busca `q` usada em `ilike`) | low | mitigate | Sempre via `.ilike()` parametrizado do Supabase-js, nunca concatenação de string SQL |
</threat_model>

<verification>
- `npx vitest run tests/storefront/list-filter-paginate.test.ts` verde (casos de filtro/busca)
- `npx tsc --noEmit` passa
- Manual: abrir `/loja/[slug]?brand=Nike&brand=Adidas`, confirmar que ambos os chips aparecem ativos e o resultado reflete OR entre as duas marcas
</verification>

<success_criteria>
- Cliente filtra por múltiplas marcas/solados/modalidades simultaneamente e busca por nome
- URL reflete o estado do filtro; reabrir a mesma URL reproduz a mesma visualização
- Filtros ficam fixos no topo ao rolar
</success_criteria>

<output>
Create `.planning/phases/04-vitrine-p-blica-e-filtragem/04-03-SUMMARY.md` when done
</output>
