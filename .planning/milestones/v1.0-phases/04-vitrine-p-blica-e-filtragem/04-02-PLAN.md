---
phase: 04-vitrine-p-blica-e-filtragem
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/products/public-list.ts
  - src/app/loja/[slug]/page.tsx
  - src/app/loja/[slug]/store-hero.tsx
  - src/app/loja/[slug]/product-grid.tsx
  - src/app/loja/[slug]/product-card.tsx
  - src/app/loja/[slug]/image-with-fallback.tsx
  - tests/storefront/list-filter-paginate.test.ts
autonomous: true
requirements: [VITR-01, VITR-03, VITR-05]
must_haves:
  truths:
    - "Um visitante sem sessão abre /loja/[slug] e vê os produtos publicados da loja (foto ou placeholder, nome, marca, preço, disponível/esgotado)"
    - "Produtos com status='draft' nunca aparecem, mesmo que a loja tenha produtos rascunho"
    - "O hero exibe nome, logo e cor da loja; a frase de apresentação só aparece quando preenchida (D-13)"
    - "Uma foto com URL quebrada mostra o placeholder ImageOff sem quebrar o layout do card (VITR-05)"
    - "A rota nunca usa \"use cache\" — o estoque exibido é sempre lido direto do Postgres a cada request (VITR-03)"
  artifacts:
    - "src/lib/products/public-list.ts"
    - "src/app/loja/[slug]/page.tsx"
    - "src/app/loja/[slug]/store-hero.tsx"
    - "src/app/loja/[slug]/product-grid.tsx"
    - "src/app/loja/[slug]/product-card.tsx"
    - "src/app/loja/[slug]/image-with-fallback.tsx"
    - "tests/storefront/list-filter-paginate.test.ts"
  key_links:
    - "page.tsx resolve store por slug via createClient() sem sessão (role anon) -> depende da policy public_read_published_stores (Plan 04-01)"
    - "queryPublicProducts fixa status='published' -> depende da policy public_read_published_products (Plan 04-01)"
    - "product-card.tsx -> image-with-fallback.tsx (Client Component, onError) -> next/image images.remotePatterns já configurado"
---

<objective>
Substituir o placeholder público de `src/app/loja/[slug]/page.tsx` pela primeira fatia real da vitrine: hero da loja (logo/cor/tagline) + grid de produtos publicados (foto com fallback, nome, marca, preço, disponibilidade), lendo os primeiros 20 produtos via uma nova função `queryPublicProducts`. Sem filtros nem paginação incremental ainda (chegam nos Plans 04-03/04-04) — esta fatia prova o caminho end-to-end mínimo: slug -> loja -> produtos publicados -> UI renderizada, sem login.

Purpose: é a primeira capacidade observável no navegador desta fase (VITR-01) — depende inteiramente da RLS pública aplicada no Plan 04-01. Estabelece `queryPublicProducts` como a única fonte de verdade de leitura pública (04-RESEARCH.md Pitfall 3 — nunca duas implementações de paginação/leitura divergentes), que os Plans 04-03/04-04/04-06 vão estender, nunca recriar.
Output: `/loja/[slug]` renderiza hero + até 20 produtos publicados reais, com fallback de imagem funcionando.

Nota de escopo (contagem de arquivos): este plano cria/modifica 7 arquivos (query + reescrita de `page.tsx` + 4 componentes de UI + teste) em 3 tasks sequencialmente dependentes (fundação de query -> rota/página -> componentes de UI). Está no limite superior aceitável para um único plano, mas mantido junto de propósito por ser uma fatia vertical coesa cujo split fragmentaria a feature. Ponto de atenção na execução: se a Task 3 (criação dos componentes) começar a consumir contexto demais, é o candidato natural a virar uma wave separada — não agir agora, só monitorar.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-CONTEXT.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md
@src/lib/products/list.ts
@src/app/loja/[slug]/page.tsx
@src/app/(admin)/produtos/product-list.tsx
@tests/products/list-filter-sort.test.ts
@tests/setup/supabase-test.ts
</context>

<phase_artifacts>
Ver `04-01-PLAN.md` §phase_artifacts para a lista cumulativa de artefatos desta fase. Este plano cria: `src/lib/products/public-list.ts` (`queryPublicProducts`, tipos `QueryPublicProductsParams`/`PublicProduct`/`QueryPublicProductsResult`), a reescrita de `src/app/loja/[slug]/page.tsx`, e os componentes novos `store-hero.tsx`/`product-grid.tsx`/`product-card.tsx`/`image-with-fallback.tsx`.
</phase_artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: queryPublicProducts — leitura pública paginada de produtos publicados</name>
  <files>src/lib/products/public-list.ts, tests/storefront/list-filter-paginate.test.ts</files>
  <read_first>
    - src/lib/products/list.ts (analog exato — duas queries separadas + join em memória; NÃO modificar este arquivo, é o padrão admin, intocado)
    - tests/products/list-filter-sort.test.ts (padrão de teste: seed direto via client autenticado, sem passar por Server Action)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Pattern 3 — esqueleto de queryPublicProducts; §Open Question 3 — técnica "buscar 21, mostrar 20" em vez de count exact; §Pitfall 7 — page 1-based em toda a stack)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md (§public-list.ts — divergências obrigatórias em relação ao analog)
  </read_first>
  <behavior>
    - `queryPublicProducts(supabase, storeId, { page: 1 })` retorna só produtos com `status='published'` daquela `storeId`, ordenados por `created_at desc`
    - Produtos com `status='draft'` da mesma loja nunca aparecem no resultado
    - Produtos de outra `storeId` nunca aparecem (defesa em profundidade via `.eq("store_id", storeId)`, mesma disciplina de T-03-13)
    - Com 21 produtos publicados seedados: `page: 1` retorna 20 produtos e `hasMore: true`; `page: 2` retorna 1 produto e `hasMore: false`
    - `disponivel` (EXISTS sobre `product_sizes.available=true`) e `coverPath` (foto de `position` mais baixa) computados igual ao `queryProducts` do admin
  </behavior>
  <action>
    Criar `src/lib/products/public-list.ts` espelhando a estrutura de duas queries + join em memória de `src/lib/products/list.ts` (NUNCA importar/modificar `list.ts` — é uma função nova e paralela). Exportar `type QueryPublicProductsParams = { page?: number }` (campos de filtro `brand`/`sole`/`fulfillment`/`q` chegam no Plan 04-03 — não implementar aqui), `type PublicProduct = { id, name, brand, brand_other, line, price, disponivel, coverPath }`, `type QueryPublicProductsResult = { products: PublicProduct[]; hasMore: boolean }`, e a constante `const PUBLIC_PAGE_SIZE = 20`.

    `queryPublicProducts(supabase: SupabaseClient<Database>, storeId: string, params: QueryPublicProductsParams): Promise<QueryPublicProductsResult>`: `status` é SEMPRE fixo `.eq("status", "published")` — nunca vindo de `params` (nunca expor esse controle ao cliente final). `page` é 1-based (default 1); calcular `from = (page - 1) * PUBLIC_PAGE_SIZE` e buscar `.range(from, from + PUBLIC_PAGE_SIZE)` (PAGE_SIZE + 1 linhas, técnica "buscar 21 mostrar 20" em vez de `count: "exact"` — 04-RESEARCH.md Open Question 3). Ordenar por `created_at` desc. Depois de receber os produtos, se `products.length > PUBLIC_PAGE_SIZE`, `hasMore = true` e cortar o array para os primeiros `PUBLIC_PAGE_SIZE`; senão `hasMore = false`. Buscar `product_sizes`/`product_photos` via `.in("product_id", productIds)` só dos produtos já cortados (mesma técnica de join em memória de `list.ts`: `Set` de ids disponíveis, `Map` de cover path pela menor `position`).

    Criar `tests/storefront/list-filter-paginate.test.ts` cobrindo exatamente o `<behavior>` acima: seed de uma loja com produtos publicados/rascunho (via `seedAuthenticatedAccount`, inserts diretos, mesma disciplina de `tests/products/list-filter-sort.test.ts`), 21 produtos publicados para testar `hasMore`, e um produto de outra loja para o teste de isolamento cross-tenant.
  </action>
  <verify>
    <automated>npx vitest run tests/storefront/list-filter-paginate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run tests/storefront/list-filter-paginate.test.ts` passa: produtos draft excluídos, produtos de outra loja excluídos, `page=1` retorna 20 + `hasMore=true`, `page=2` retorna 1 + `hasMore=false`, `disponivel`/`coverPath` corretos.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rota /loja/[slug] — resolver loja por slug e renderizar a primeira página</name>
  <files>src/app/loja/[slug]/page.tsx</files>
  <read_first>
    - src/app/loja/[slug]/page.tsx (placeholder atual — preservar o comentário de "sem NENHUMA checagem de auth", adaptar o texto)
    - src/middleware.ts (matcher `/admin/:path*` — confirma que esta rota é estruturalmente inalcançável pelo middleware)
    - src/app/(admin)/produtos/page.tsx (padrão de composição searchParams -> query -> render, adaptado aqui sem auth/onboarding-guard)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md (§page.tsx — aviso obrigatório no topo do arquivo)
  </read_first>
  <action>
    Reescrever `src/app/loja/[slug]/page.tsx` como Server Component assíncrono `LojaPublicaPage({ params }: { params: Promise<{ slug: string }> })`. Comentário obrigatório no topo do arquivo preservando a garantia estrutural: "Vitrine pública — SEM NENHUMA checagem de auth (matcher do middleware é /admin/:path*, esta rota é inalcançável por construção). NUNCA adicionar \"use cache\" nesta rota nem em public-list.ts — o estoque precisa refletir o painel do revendedor com delay de segundos (VITR-03, CLAUDE.md), e Cache Components do Next 16 é opt-in."

    `await params` para obter `slug`. Usar `createClient()` de `@/lib/supabase/server` (mesmo helper do resto do projeto — sem cookie de sessão presente nesta rota, o Postgres resolve o papel como `anon` automaticamente). Resolver a loja: `.from("stores").select("id, name, logo_url, accent_color, tagline").eq("slug", slug).single()`. Se `error` ou loja ausente, chamar `notFound()` de `next/navigation` (nenhum precedente no codebase ainda — é a forma canônica do Next.js para uma rota dinâmica sem correspondência).

    Chamar `queryPublicProducts(supabase, store.id, { page: 1 })` (de `@/lib/products/public-list`). Mapear `coverPath` para `coverUrl` via `supabase.storage.from("product-images").getPublicUrl(coverPath).data.publicUrl` (mesmo padrão de `src/app/(admin)/produtos/page.tsx`), só quando `coverPath` não for `null`. Renderizar `<main>` com `<StoreHero store={...} />`, depois `<ProductGrid products={...} />` quando `products.length > 0`, senão um estado vazio simples: "Nenhum produto disponível ainda." (texto direto em `<p>`, sem componente dedicado — os dois empty states diferenciados por filtro chegam no Plan 04-03).

    D-08 (explícito): NUNCA renderizar um contador de produtos ("23 produtos") nesta página — esse contador é um recurso exclusivo do painel admin (`src/app/(admin)/produtos/page.tsx`, Fase 3) e não deve vazar para a experiência do cliente final. Nenhum texto tipo "{products.length} produtos" deve aparecer em `page.tsx` nem em `product-grid.tsx`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `page.tsx` não importa nenhum helper de auth/onboarding-guard; usa `notFound()` quando o slug não resolve; chama `queryPublicProducts` com `page: 1`; nunca contém a string `"use cache"` (checável via `grep -c '"use cache"' src/app/loja/[slug]/page.tsx` retornando 0); não renderiza nenhum contador de produtos tipo "N produtos" (D-08); `npx tsc --noEmit` passa.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Componentes de UI — StoreHero, ProductGrid, ProductCard, ImageWithFallback</name>
  <files>src/app/loja/[slug]/store-hero.tsx, src/app/loja/[slug]/product-grid.tsx, src/app/loja/[slug]/product-card.tsx, src/app/loja/[slug]/image-with-fallback.tsx</files>
  <read_first>
    - src/app/(admin)/produtos/product-list.tsx (padrão de card: fallback ImageOff, dot de disponibilidade, formatBRLPrice — linhas 94-131; NÃO copiar os botões editar/excluir, linhas 133-149)
    - src/lib/currency/brl.ts (formatBRLPrice)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Code Examples — ImageWithFallback, cópia quase literal)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md (§image-with-fallback.tsx, §product-card.tsx/product-grid.tsx, §Sem Analog Direto — store-hero.tsx)
  </read_first>
  <action>
    Criar `image-with-fallback.tsx` (`"use client"`): props `{ src: string | null; alt: string }`; `useState(!src)` para `errored`; se `errored || !src`, renderizar `<div className="flex aspect-square w-full items-center justify-center rounded-xl bg-[#F5F5F3]">` com `<ImageOff className="h-8 w-8 text-[#6B6B6B]" aria-hidden="true" />` (import de `lucide-react`); senão `<Image src={src} alt={alt} fill className="rounded-xl object-cover" onError={() => setErrored(true)} />` (import de `next/image`).

    Criar `product-card.tsx` (Server Component): props `{ product: { id, name, brand, brand_other, line, price, disponivel, coverUrl } }`. Renderizar `<ImageWithFallback src={product.coverUrl} alt={product.name} />` num container `relative aspect-square`, nome (`<span className="truncate font-medium text-[#111111]">`), linha secundária marca+linha (`brand_other` substitui `brand` quando `brand === "Outra"`, mesma lógica de `product-list.tsx`), preço via `formatBRLPrice(product.price)`, e o dot de disponibilidade (`text-[#00C46A]`/`bg-[#00C46A]` quando `disponivel`, senão `text-[#6B6B6B]`/`bg-[#6B6B6B]`) com o texto "Disponível"/"Esgotado". NUNCA incluir botões de editar/excluir (exclusivos do admin).

    Criar `product-grid.tsx` (Server Component): props `{ products: ProductCardProps["product"][] }`; grid responsivo `grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4 md:gap-4`, mapeando `<ProductCard key={product.id} product={product} />`.

    Criar `store-hero.tsx` (Server Component, sem analog direto no codebase — D-12/D-13): props `{ store: { name: string; logoUrl: string | null; accentColor: string | null; tagline: string | null } }`. Container com `style={{ backgroundColor: store.accentColor ?? "#0D3D2B" }}` e texto branco por cima (D-12 — cor de destaque como fundo/acento); `<ImageWithFallback src={store.logoUrl} alt={store.name} />` num círculo (`h-16 w-16 rounded-full overflow-hidden`); `<h1>{store.name}</h1>`; a frase de apresentação (`store.tagline`) só renderiza se truthy — sem elemento vazio quando ausente (D-13, `{store.tagline && <p>...</p>}`).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    Os 4 arquivos existem; `product-card.tsx` não contém nenhum `<Link href="/produtos` nem botão de editar/excluir; `store-hero.tsx` só renderiza a tagline dentro de uma condicional (`store.tagline &&`); `image-with-fallback.tsx` é `"use client"` e usa `onError`; `npx tsc --noEmit` passa.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon -> Postgres (leitura de produtos publicados) | queryPublicProducts nunca aceita `status` de fora — sempre fixo `'published'` no código, não em `params` |
| anon -> next/image (fotos do bucket público) | URLs geradas via `getPublicUrl`, mesmo `images.remotePatterns` já configurado; nenhuma nova superfície |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Information Disclosure | queryPublicProducts (vazamento de produtos draft se `status` viesse de input externo) | high | mitigate | `status` hardcoded `'published'` no código-fonte, nunca parametrizável via `QueryPublicProductsParams`; testado em `list-filter-paginate.test.ts` |
| T-04-05 | Tampering | page.tsx (slug não validado usado em query) | low | mitigate | `slug` passado via `.eq("slug", slug)` parametrizado do Supabase-js, nunca interpolação de string SQL crua |
</threat_model>

<verification>
- `npx vitest run tests/storefront/list-filter-paginate.test.ts` verde
- `npx tsc --noEmit` passa
- `/loja/[slug]` renderiza hero + grid de produtos publicados reais (verificação manual/checkpoint no fechamento da fase)
</verification>

<success_criteria>
- Visitante sem sessão abre `/loja/[slug]` e vê hero + produtos publicados (nome/marca/preço/disponibilidade/foto ou fallback)
- Produtos draft nunca aparecem
- Paginação de 20 por carga já embutida em `queryPublicProducts` (UI de "carregar mais"/numerada chega no Plan 04-04)
</success_criteria>

<output>
Create `.planning/phases/04-vitrine-p-blica-e-filtragem/04-02-SUMMARY.md` when done
</output>
