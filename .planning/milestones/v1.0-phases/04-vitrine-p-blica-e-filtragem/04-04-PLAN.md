---
phase: 04-vitrine-p-blica-e-filtragem
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/lib/products/public-actions.ts
  - src/app/loja/[slug]/load-more-button.tsx
  - src/app/loja/[slug]/pagination-numbered.tsx
  - src/app/loja/[slug]/page.tsx
  - tests/storefront/load-more-pagination.test.ts
autonomous: true
requirements: [VITR-04]
must_haves:
  truths:
    - "No mobile, o cliente clica 'Carregar mais' e vê mais produtos aparecerem sem reload completo da página (D-05, D-07)"
    - "No desktop, o cliente navega por paginação numerada (anterior/próxima), variante secundária (D-05)"
    - "Mudar de página (numerada) ou clicar 'carregar mais' nunca duplica nem pula produtos (mesma função de query em ambos os caminhos)"
  artifacts:
    - "src/lib/products/public-actions.ts"
    - "src/app/loja/[slug]/load-more-button.tsx"
    - "src/app/loja/[slug]/pagination-numbered.tsx"
  key_links:
    - "load-more-button.tsx -> fetchNextPage (Server Action) -> queryPublicProducts (mesma função usada por page.tsx, nunca uma segunda implementação de paginação)"
    - "fetchNextPage resolve coverPath -> coverUrl no servidor (supabase.storage.getPublicUrl) ANTES de retornar -> LoadMoreButton (Client Component) só consome coverUrl pronto, nunca coverPath cru"
    - "CSS hidden md:flex / flex md:hidden decide qual controle aparece -> nunca JS de detecção de device"
---

<objective>
Adicionar os dois controles de paginação adaptativa (D-05): `LoadMoreButton` (Client Component, mobile, primário) chamando a nova Server Action `fetchNextPage`; `PaginationNumbered` (Server Component, desktop, `<Link href="?page=N">`). Ambos consomem a MESMA `queryPublicProducts` já existente (Plan 04-02/04-03) — nunca uma segunda implementação de paginação (04-RESEARCH.md Pitfall 3). A escolha entre os dois controles é puramente CSS (`hidden md:flex` / `flex md:hidden`), nunca detecção de device via JS/user-agent (Pitfall/Anti-padrão do 04-RESEARCH.md).

Purpose: fecha VITR-04 — sem isso, a vitrine só mostra os primeiros 20 produtos sem nenhuma forma de ver o resto do catálogo. `fetchNextPage` vive num arquivo NOVO (`public-actions.ts`), deliberadamente separado de `src/lib/products/actions.ts` (owner-scoped): mistura de uma Server Action pública (sem `getOwnedStore()`) no mesmo módulo que ações autenticadas do dono cria risco de copy-paste acidental do padrão `getOwnedStore()` num caminho de código público — divergência intencional do mapeamento sugerido em 04-PATTERNS.md, por separação de responsabilidades de segurança.
Output: cliente navega por todo o catálogo publicado, com o controle certo por dispositivo, sem reload de página inteira no mobile.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-CONTEXT.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md
@src/lib/products/public-list.ts
@src/app/(admin)/produtos/product-list.tsx
@src/app/loja/[slug]/page.tsx
</context>

<phase_artifacts>
Ver `04-01-PLAN.md` §phase_artifacts para a lista cumulativa. Este plano cria `src/lib/products/public-actions.ts` (`fetchNextPage`), `load-more-button.tsx` e `pagination-numbered.tsx`.
</phase_artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fetchNextPage — Server Action pública para a próxima página</name>
  <files>src/lib/products/public-actions.ts, tests/storefront/load-more-pagination.test.ts</files>
  <read_first>
    - src/lib/products/public-list.ts (queryPublicProducts — a função que fetchNextPage chama, nunca duplicar sua lógica)
    - src/app/loja/[slug]/page.tsx (padrão de resolver store por slug via createClient() sem sessão)
    - tests/products/create-product.test.ts (padrão de mock de next/headers para testar Server Actions contra o Supabase remoto real)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Pattern 7 — fetchNextPage nunca usa "use cache"; §Pitfall 3 — nunca duas implementações de paginação divergentes; §Pitfall 7 — page 1-based consistente)
  </read_first>
  <behavior>
    - `fetchNextPage(slug, { page: 2, brand: [], sole: [], fulfillment: [], q: undefined })` retorna `{ products, hasMore }` correspondendo às MESMAS linhas (mesmos `id` na mesma ordem, mesmo `hasMore`) que `queryPublicProducts(supabase, storeId, { page: 2 })` retornaria — porém com cada `coverPath` já resolvido para `coverUrl` (a URL pública final), pois o Client Component `LoadMoreButton` não tem acesso ao `supabase.storage` para fazer essa conversão
    - Cada produto retornado por `fetchNextPage` tem o campo `coverUrl` populado (string da URL pública quando havia `coverPath`, `null` quando não havia) — nunca `coverPath` cru, que o cliente não conseguiria renderizar
    - `fetchNextPage` com um `slug` inexistente retorna `{ error: string }`, nunca lança exceção
    - `fetchNextPage` nunca aceita `storeId` diretamente do chamador — sempre resolve a loja pelo `slug` público (mesma superfície de entrada que `page.tsx`, nenhum atalho que pule a resolução por slug)
  </behavior>
  <action>
    Criar `src/lib/products/public-actions.ts` com `"use server"` no topo. Comentário de cabeçalho explicando a separação deliberada de `src/lib/products/actions.ts` (owner-scoped): este arquivo é exclusivamente para Server Actions PÚBLICAS/anônimas — nunca importar/chamar `getOwnedStore()` aqui.

    Exportar `type PublicProductWithUrl = Omit<PublicProduct, "coverPath"> & { coverUrl: string | null }` (o produto pronto para o cliente, com a URL pública já resolvida em vez do `coverPath` cru), `type FetchNextPageResult = { error: string } | { products: PublicProductWithUrl[]; hasMore: boolean }` (import de `PublicProduct` de `@/lib/products/public-list`) e `async function fetchNextPage(slug: string, params: Omit<QueryPublicProductsParams, "page">, page: number): Promise<FetchNextPageResult>`. Corpo: `createClient()` de `@/lib/supabase/server`; resolver `.from("stores").select("id").eq("slug", slug).single()`; se erro/ausente, `{ error: "Loja não encontrada." }`; senão chamar `queryPublicProducts(supabase, store.id, { ...params, page })` (nunca reimplementar `.range()`/`.order()` aqui).

    RESOLUÇÃO DE URL (obrigatória, não opcional): antes de retornar, mapear cada produto do resultado convertendo `coverPath` para `coverUrl` via `supabase.storage.from("product-images").getPublicUrl(coverPath).data.publicUrl` (só quando `coverPath` não for `null`; senão `coverUrl: null`) — exatamente o mesmo padrão já usado em `page.tsx` (Plan 04-02 Task 2). Esta conversão TEM que acontecer aqui no servidor: o `LoadMoreButton` (Task 2) é um Client Component e não tem acesso ao `supabase.storage`, então se `fetchNextPage` devolvesse `coverPath` cru as imagens da próxima página falhariam silenciosamente sem fallback. Retornar `{ products: <produtos já com coverUrl>, hasMore }`.

    Criar `tests/storefront/load-more-pagination.test.ts` (mock de `next/headers` com jar vazio, igual ao padrão de `tests/products/create-product.test.ts` — `fetchNextPage` não precisa de sessão, mas `createClient()` ainda chama `cookies()` internamente, então o mock evita o erro "cookies was called outside a request scope"). Seed de 21 produtos publicados numa loja; comparar `fetchNextPage(slug, {}, 2)` com uma chamada direta a `queryPublicProducts(supabase, storeId, { page: 2 })` usando o client autenticado do seed — os dois devem retornar as mesmas linhas na mesma ordem (mesmos `id`) e o mesmo `hasMore` (Pitfall 3); assertar adicionalmente que todo produto retornado por `fetchNextPage` expõe `coverUrl` (string ou `null`) e NÃO expõe `coverPath` cru. Testar também `fetchNextPage("slug-inexistente-xyz", {}, 1)` retornando `{ error: expect.any(String) }`.
  </action>
  <verify>
    <automated>npx vitest run tests/storefront/load-more-pagination.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run tests/storefront/load-more-pagination.test.ts` passa; `public-actions.ts` não importa `getOwnedStore` nem qualquer símbolo de `src/lib/products/actions.ts`; `fetchNextPage` chama `queryPublicProducts` (nunca reimplementa `.range()`); todo produto retornado por `fetchNextPage` tem `coverUrl` resolvido via `supabase.storage...getPublicUrl` (nunca devolve `coverPath` cru ao cliente).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: LoadMoreButton — controle mobile primário (Client Component + Server Action)</name>
  <files>src/app/loja/[slug]/load-more-button.tsx</files>
  <read_first>
    - src/app/(admin)/produtos/product-list.tsx (padrão useTransition + Server Action + toast, linhas 56-80)
    - src/lib/products/public-actions.ts (fetchNextPage, criado na Task 1)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Code Examples — LoadMoreButton; §Pattern 6 — wrapper CSS de visibilidade por device)
  </read_first>
  <action>
    Criar `load-more-button.tsx` (`"use client"`). Props: `{ slug: string; initialPage: number; initialHasMore: boolean; filters: Omit<QueryPublicProductsParams, "page"> }`. Estado: `items` (`PublicProduct[]`, inicia vazio — os primeiros 20 já vieram do Server Component em `page.tsx`, este componente só acumula o que vem DEPOIS), `currentPage` (inicia em `initialPage`), `hasMore` (inicia em `initialHasMore`), `isPending`/`startTransition` de `useTransition`.

    `handleLoadMore`: dentro de `startTransition`, chamar `fetchNextPage(slug, filters, currentPage + 1)`; se `"error" in result`, `toast.error(result.error)` (import de `sonner`, mesmo padrão de `product-list.tsx`) e retornar; senão `setItems(prev => [...prev, ...result.products])`, `setCurrentPage(p => p + 1)`, `setHasMore(result.hasMore)`.

    Renderizar `items.map(product => <ProductCard key={product.id} product={product} />)` dentro do MESMO grid CSS de `product-grid.tsx` (reusar as mesmas classes `grid grid-cols-2 gap-3 sm:grid-cols-3 md:grid-cols-4`, nunca duplicar via um segundo componente de grid) seguido do botão "Carregar mais" (`disabled={isPending}`, texto "Carregando…" durante pending) só quando `hasMore`. Nota: `fetchNextPage` (Task 1) JÁ devolve cada produto com `coverUrl` resolvido (`PublicProductWithUrl`), portanto este Client Component apenas consome `product.coverUrl` diretamente — NUNCA tentar chamar `supabase.storage.getPublicUrl` aqui (indisponível no cliente) e NUNCA receber `coverPath` cru.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `load-more-button.tsx` importa `fetchNextPage` de `@/lib/products/public-actions`; usa `useTransition`; nunca substitui `items` (sempre `setItems(prev => [...prev, ...novos])` — acumulativo, nunca reset); botão só renderiza quando `hasMore`; `npx tsc --noEmit` passa.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: PaginationNumbered (desktop) + wiring final em page.tsx (CSS adaptativo, page 1-based)</name>
  <files>src/app/loja/[slug]/pagination-numbered.tsx, src/app/loja/[slug]/page.tsx</files>
  <read_first>
    - src/app/loja/[slug]/page.tsx (arquivo dos Plans 04-02/04-03 — estender)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Pattern 6 — hidden md:flex / flex md:hidden, ambos renderizados no servidor)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md (§pagination-numbered.tsx — sem analog direto, usar <Link> genérico do Next.js)
  </read_first>
  <action>
    Criar `pagination-numbered.tsx` (Server Component, sem estado). Props: `{ slug: string; currentPage: number; hasMore: boolean; searchParamsString: string }` (a query string atual já serializada, preservando `q`/`brand`/`sole`/`fulfillment`, exceto `page`). Renderizar um `<nav className="hidden md:flex items-center gap-3">` com `<Link href={\`/loja/${slug}?${searchParamsString}&page=${currentPage - 1}\`}>` habilitado só quando `currentPage > 1`, e `<Link href={\`/loja/${slug}?${searchParamsString}&page=${currentPage + 1}\`}>` habilitado só quando `hasMore`. Sem estado nenhum — cada clique é uma navegação de página inteira que o Server Component recalcula do zero (sem cache, VITR-03).

    Estender `page.tsx`: parsear `page` de `searchParams` (`const page = Number(sp.page ?? "1") || 1`, 1-based em toda a stack — Pitfall 7); passar `page` para `queryPublicProducts` no lugar do `page: 1` fixo do Plan 04-02. Serializar a query string atual SEM `page` (reusar a mesma construção de `URLSearchParams` já usada para os filtros, removendo a chave `page`) para passar a `pagination-numbered.tsx`. Renderizar, logo após o grid inicial:
    - `<div className="hidden md:flex"><PaginationNumbered slug={slug} currentPage={page} hasMore={hasMore} searchParamsString={...} /></div>`
    - `<div className="flex md:hidden"><LoadMoreButton slug={slug} initialPage={page} initialHasMore={hasMore} filters={{ q: sp.q, brand: brands, sole: soles, fulfillment: fulfillments }} /></div>`

    Confirmar que NENHUM dos dois blocos usa `navigator.userAgent`, `window.innerWidth` nem qualquer detecção de device em JS — a decisão de qual aparece é 100% CSS (Anti-padrão do 04-RESEARCH.md). D-08 (explícito): nem `PaginationNumbered` nem `LoadMoreButton` devem exibir um texto tipo "Página X de Y" com contagem total ou "mostrando N produtos" — isso seria o mesmo contador de produtos que D-08 proíbe explicitamente na vitrine pública (exclusivo do painel admin); `PaginationNumbered` só mostra os controles anterior/próxima, sem número total de páginas.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "navigator.userAgent" src/app/loja/[slug]/page.tsx src/app/loja/[slug]/pagination-numbered.tsx src/app/loja/[slug]/load-more-button.tsx | grep -qv "[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    `page.tsx` parseia `page` de `searchParams` como 1-based; renderiza os dois blocos de paginação com `hidden md:flex`/`flex md:hidden`; nenhum arquivo desta pasta contém `navigator.userAgent`; `npx tsc --noEmit` passa.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon -> fetchNextPage (Server Action pública) | Única Server Action deste projeto sem `getOwnedStore()` — a única defesa é resolver a loja pelo slug público, nunca aceitar um `storeId` direto do cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Denial of Service (parcial) | fetchNextPage chamável repetidamente sem rate-limit | low | accept | Fora de escopo do MVP (tráfego baixo, "dezenas" de lojas — 04-RESEARCH.md §Known Threat Patterns); revisitar se o produto crescer |
| T-04-09 | Information Disclosure / Elevation of Privilege | fetchNextPage aceitando um storeId arbitrário em vez de resolver por slug | high | mitigate | Assinatura da função só aceita `slug` (dado público) — nunca `storeId` direto; resolve a loja internamente, mesma superfície de entrada de `page.tsx` |
</threat_model>

<verification>
- `npx vitest run tests/storefront/load-more-pagination.test.ts` verde
- `npx tsc --noEmit` passa
- Manual: em viewport mobile, "Carregar mais" aparece e funciona sem reload; em viewport desktop, paginação numerada aparece e navega corretamente
</verification>

<success_criteria>
- Cliente vê todo o catálogo publicado através de "carregar mais" (mobile) ou paginação numerada (desktop)
- Nenhuma duplicação/pulo de produtos entre páginas
- Nenhuma detecção de device via JS
</success_criteria>

<output>
Create `.planning/phases/04-vitrine-p-blica-e-filtragem/04-04-SUMMARY.md` when done
</output>
