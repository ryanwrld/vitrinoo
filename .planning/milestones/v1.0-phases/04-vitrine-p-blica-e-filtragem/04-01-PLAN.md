---
phase: 04-vitrine-p-blica-e-filtragem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0004_public_storefront_rls_and_visibility.sql
  - src/lib/database.types.ts
  - tests/storefront/public-access-rls.test.ts
autonomous: false
requirements: [VITR-01]
must_haves:
  truths:
    - "Um client Supabase anônimo (sem sessão, sem auth.uid()) consegue ler stores e products com status='published' — hoje isso retorna [] para qualquer request anônimo (achado crítico do 04-RESEARCH.md)"
    - "Um client anônimo NUNCA lê products com status='draft', mesmo sabendo o id/store_id exato"
    - "Um client anônimo NUNCA lê store_settings (WhatsApp), mesmo depois da migration (Pitfall 2 — escopo mínimo)"
    - "products ganha a coluna hide_when_sold_out (nullable, sem default) e stores ganha hide_sold_out_default (not null default false) — pré-requisito de schema para D-09/D-10/D-11 (Plans 04-05/04-06)"
  artifacts:
    - "supabase/migrations/0004_public_storefront_rls_and_visibility.sql"
    - "src/lib/database.types.ts (regenerado)"
    - "tests/storefront/public-access-rls.test.ts"
  key_links:
    - "policy public_read_published_stores (to anon, using true) -> resolve loja por slug sem sessão"
    - "policy public_read_published_products/_sizes/_photos (to anon, using status='published' ou subquery) -> única leitura pública permitida"
    - "policies aditivas (OR) -> owner_full_access_* existentes continuam intactas, nunca substituídas"
---

<objective>
Criar a migration `0004_public_storefront_rls_and_visibility.sql`: quatro policies RLS aditivas `to anon` (restritas a `status = 'published'`) em `stores`/`products`/`product_sizes`/`product_photos`, e as duas colunas novas de visibilidade de esgotado (`products.hide_when_sold_out` nullable, `stores.hide_sold_out_default not null default false`). Aplicar o [BLOCKING] push ao Postgres remoto, regenerar `src/lib/database.types.ts`, e provar com um teste de integração real (client anônimo, sem `signIn`) que a leitura pública funciona exatamente no escopo esperado — nem mais, nem menos.

Purpose: 04-RESEARCH.md confirmou por grep direto nas migrations existentes que NENHUMA policy `to anon`/`using (true)` existe hoje — toda policy é `owner_full_access_*` escopada a `auth.uid()`. Sem esta migration, a vitrine pública (todo o resto da fase) renderiza "vazia" silenciosamente: RLS bloqueando não é distinguível de "tabela vazia" (retorna `[]`, nunca um erro visível) — Pitfall 1 do 04-RESEARCH.md. Este é o gate mais crítico da fase: todo plano seguinte depende de leituras anônimas funcionando de verdade.
Output: migration aplicada ao banco remoto, tipos regenerados, teste de acesso público verde provando o escopo exato (published sim, draft não, store_settings não).
</objective>

<phase_user_story>
**As a** cliente final que recebeu o link da vitrine, **I want to** acessar o catálogo de produtos publicados de um revendedor sem precisar criar conta ou logar, **so that** eu possa escolher um modelo e mandar uma mensagem de pedido pronta pelo WhatsApp sem fricção nenhuma.

(Nota do planejamento: o `**Goal:**` da Fase 4 no ROADMAP já é outcome-shaped mas não está na forma estrita "As a/I want to/so that" — a user story acima é uma reafirmação fiel do goal + Success Criteria 1, sem inventar escopo novo, seguindo o mesmo precedente aplicado em 03-01-PLAN.md. Esta primeira fatia entrega a fundação de acesso público (dados); a primeira capacidade visível no navegador chega no Plan 04-02.)
</phase_user_story>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-CONTEXT.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md
@.planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md
@supabase/migrations/0001_init_stores_rls.sql
@supabase/migrations/0003_products_schema_rls.sql
@tests/setup/supabase-test.ts
@tests/rls/product-isolation.test.ts
</context>

<phase_artifacts>
## Artifacts this phase produces (cumulative reference for all 6 plans in Phase 4)

- `supabase/migrations/0004_public_storefront_rls_and_visibility.sql` — policies: `public_read_published_stores`, `public_read_published_products`, `public_read_published_product_sizes`, `public_read_published_product_photos`; columns: `products.hide_when_sold_out`, `stores.hide_sold_out_default` (this plan)
- `src/lib/products/public-list.ts` — `queryPublicProducts()`, types `QueryPublicProductsParams`/`PublicProduct`/`QueryPublicProductsResult` (Plan 04-02, extended by 04-03/04-06)
- `src/app/loja/[slug]/page.tsx` — rewritten route (Plan 04-02, extended by 04-03/04-04/04-06)
- `src/app/loja/[slug]/store-hero.tsx`, `product-grid.tsx`, `product-card.tsx`, `image-with-fallback.tsx` (Plan 04-02)
- `src/app/loja/[slug]/product-filters.tsx` (Plan 04-03)
- `src/lib/products/public-actions.ts` — `fetchNextPage()` Server Action (Plan 04-04, extended by 04-06)
- `src/app/loja/[slug]/load-more-button.tsx`, `pagination-numbered.tsx` (Plan 04-04)
- `products.hide_when_sold_out` field wired into `src/lib/validation/product.ts`, `product-form.tsx`, `products/actions.ts`, `[id]/editar/page.tsx`; `stores.hide_sold_out_default` wired into `src/lib/validation/onboarding.ts`, `settings-form.tsx`, `settings/actions.ts` (Plan 04-05)
</phase_artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration 0004 (RLS pública to anon + colunas de visibilidade)</name>
  <files>supabase/migrations/0004_public_storefront_rls_and_visibility.sql</files>
  <read_first>
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Pattern 1 — DDL exata das 4 policies; §Pitfall 2 — store_settings NUNCA ganha policy nesta migration; §Pitfall 5 — hide_when_sold_out DEVE ser nullable sem default)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-PATTERNS.md (§migration 0004 — padrão de policy aditiva + coluna nullable)
    - supabase/migrations/0003_products_schema_rls.sql (análogo de disciplina: comentário de cabeçalho explicando a não-negociável desta migration)
    - supabase/migrations/0001_init_stores_rls.sql (mostra a policy owner_full_access_stores/settings já existente — esta migration NUNCA remove/substitui essas, só adiciona novas ao lado)
  </read_first>
  <action>
    Criar `supabase/migrations/0004_public_storefront_rls_and_visibility.sql` com um comentário de cabeçalho explicando: esta migration NÃO cria tabelas novas — adiciona (a) quatro policies RLS aditivas restritas ao papel `anon` e (b) duas colunas nullable/default. Policies do mesmo `for select` são combinadas com OR pelo Postgres — as policies `owner_full_access_*` existentes continuam intactas, nunca remover nem alterá-las.

    Quatro `create policy ... for select to anon using (...)` (NUNCA `for all` — Antipadrão do 04-RESEARCH.md, T-04-03 abaixo):
    - `public_read_published_stores` em `stores`: `using (true)` — necessário para resolver slug -> id; `stores` não tem coluna sensível (WhatsApp fica em `store_settings`, que este arquivo NÃO toca).
    - `public_read_published_products` em `products`: `using (status = 'published')`.
    - `public_read_published_product_sizes` em `product_sizes`: `using (product_id in (select id from products where status = 'published'))`.
    - `public_read_published_product_photos` em `product_photos`: mesma subquery de `product_sizes`.

    Duas colunas novas (D-09/D-10/D-11, resolvendo 04-RESEARCH.md Open Question 2 e Pitfall 5):
    - `alter table products add column hide_when_sold_out boolean;` — nullable, SEM `not null`/`default` (D-10: produto novo nasce sem exceção própria = `null`, herdando o padrão global até o revendedor configurar algo diferente; D-11 exige poder resetar para "sem exceção", e `false` já seria um valor "configurado" se a coluna tivesse default `false`).
    - `alter table stores add column hide_sold_out_default boolean not null default false;` — a LOJA precisa de um valor inicial não-nulo (D-10: nunca ocultar por padrão, "false" = mostrar esmaecido).

    Comentário explícito no final do arquivo confirmando que `store_settings` NÃO é tocada nesta migration (Pitfall 2 — WhatsApp só ganha exposição pública quando a Fase 5 decidir como/quando, princípio de menor privilégio).
  </action>
  <verify>
    <automated>test -f supabase/migrations/0004_public_storefront_rls_and_visibility.sql && grep -c "to anon" supabase/migrations/0004_public_storefront_rls_and_visibility.sql | grep -q "^4$" && ! grep -q "create policy.*store_settings" supabase/migrations/0004_public_storefront_rls_and_visibility.sql</automated>
  </verify>
  <acceptance_criteria>
    Arquivo existe; contém exatamente 4 ocorrências de `to anon`; contém `for select` em cada uma delas (nunca `for all`); contém `using (status = 'published')` para `products`; contém `alter table products add column hide_when_sold_out boolean` SEM `not null`/`default` na mesma linha; contém `alter table stores add column hide_sold_out_default boolean not null default false`; NÃO contém nenhuma `create policy` referenciando `store_settings`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar a migration ao Supabase remoto + regenerar tipos</name>
  <files>src/lib/database.types.ts</files>
  <read_first>
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-01-PLAN.md (§Task 2 — padrão exato de db push + SUPABASE_ACCESS_TOKEN + fallback manual + typegen, já usado com sucesso 3 vezes neste projeto)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Environment Availability — Supabase CLI 2.109.1, projeto remoto já linkado)
  </read_first>
  <action>
    [BLOCKING — rodar DEPOIS da Task 1 e ANTES da Task 3.] Sem este push, build/typecheck passam (tipos vêm de `database.types.ts`, não do banco vivo) mas os testes de integração desta fase rodam contra o Supabase remoto real (`tests/setup/supabase-test.ts`) — sem o push, todo teste desta fase falharia silenciosamente contra um schema desatualizado, um falso positivo de verificação.

    Aplicar a migration ao projeto remoto linkado: rodar `supabase db push`. Se exigir auth num ambiente não-interativo, exportar `SUPABASE_ACCESS_TOKEN` primeiro; se ainda assim exigir um prompt interativo que não pode ser suprimido, PARAR e sinalizar esta task para intervenção manual (este plano já é `autonomous: false` por esta razão). Fallback se a CLI falhar: aplicar o SQL via SQL editor do Supabase Dashboard, depois regenerar os tipos de qualquer forma.

    Após o push bem-sucedido, regenerar `src/lib/database.types.ts` via `supabase gen types typescript --linked` e escrever a saída sobre o arquivo. Os tipos DEVEM vir do schema vivo — nunca editar `database.types.ts` à mão. Confirmar que os tipos regenerados contêm `hide_when_sold_out` em `products` e `hide_sold_out_default` em `stores`.
  </action>
  <verify>
    <automated>grep -q "hide_when_sold_out" src/lib/database.types.ts && grep -q "hide_sold_out_default" src/lib/database.types.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `supabase db push` aplicou a migration ao banco remoto (ou fallback via Dashboard); `src/lib/database.types.ts` regenerado contém `hide_when_sold_out` e `hide_sold_out_default`; `npx tsc --noEmit` passa sem erros. Se o push exigiu prompt interativo impossível de suprimir, a task foi sinalizada para intervenção manual em vez de marcada como concluída silenciosamente.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Teste de acesso público anônimo (published sim, draft não, store_settings não)</name>
  <files>tests/storefront/public-access-rls.test.ts</files>
  <read_first>
    - tests/rls/product-isolation.test.ts (padrão de seed via seedAuthenticatedAccount + inserts diretos pelo client autenticado do dono)
    - tests/setup/supabase-test.ts (createAnonClient — client SEM signIn, chave anon pura; seedAuthenticatedAccount — client do dono para popular os dados)
    - .planning/phases/04-vitrine-p-blica-e-filtragem/04-RESEARCH.md (§Pitfall 1 — RLS bloqueando retorna [] sem erro, nunca lançar exceção; §Validation Architecture — Wave 0 Gap 1)
  </read_first>
  <behavior>
    - Seed: uma conta autenticada (dono) cria 1 store, 1 product status='published' (com 1 product_sizes + 1 product_photos), 1 product status='draft' (com 1 product_sizes + 1 product_photos), e 1 store_settings row.
    - `createAnonClient()` (sem signIn) faz SELECT em `stores` filtrando pelo slug seedado -> retorna a linha (não `[]`, não erro).
    - O mesmo client anônimo faz SELECT em `products` filtrando `id` do produto `published` -> retorna a linha.
    - O mesmo client anônimo faz SELECT em `products` filtrando `id` do produto `draft` -> retorna `[]` (nunca a linha, nunca erro).
    - O mesmo client anônimo faz SELECT em `product_sizes`/`product_photos` do produto `published` -> retorna as linhas; do produto `draft` -> retorna `[]`.
    - O mesmo client anônimo faz SELECT em `store_settings` da store seedada -> retorna `[]` (Pitfall 2 — nenhuma policy pública nesta tabela).
  </behavior>
  <action>
    Criar `tests/storefront/public-access-rls.test.ts` seguindo a mesma disciplina de `tests/rls/product-isolation.test.ts`: `seedAuthenticatedAccount` para o dono, inserts diretos de `stores`/`products` (published + draft)/`product_sizes`/`product_photos`/`store_settings` via o client autenticado do dono (nunca service_role). Depois, um `createAnonClient()` separado (sem nenhum `signIn`) faz as 6 leituras descritas em `<behavior>`, cada uma com `expect(error).toBeNull()` seguido do assert de conteúdo (linha esperada ou array vazio — RLS bloqueando nunca é um erro, é sempre `[]`, conforme Pitfall 1). Importar tipos de `src/lib/database.types.ts`. Limpar (delete da store seedada, cascade) no `afterAll`.
  </action>
  <verify>
    <automated>npx vitest run tests/storefront/public-access-rls.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run tests/storefront/public-access-rls.test.ts` passa: as 6 asserts (stores sim, products published sim, products draft não, sizes/photos published sim, sizes/photos draft não, store_settings não) todas verdes. Este teste falharia (vermelho) se a Task 1/2 não tivessem aplicado a migration corretamente — prova que o push realmente aconteceu.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (visitante sem sessão) -> Postgres (RLS) | Primeira vez que o projeto concede leitura a um papel sem `auth.uid()` — a fronteira inteira depende da precisão exata de `using (status = 'published')` |
| anon -> stores (sem coluna sensível) | `using (true)` é aceitável aqui porque `stores` não guarda WhatsApp/segredos — a fronteira real de sensibilidade fica em `store_settings`, fora do escopo desta policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | products / product_sizes / product_photos (policy `to anon` mal escrita, ex. `using (true)` em vez de `using (status = 'published')`) | critical | mitigate | Policy explícita restrita a `status = 'published'`; testada em `public-access-rls.test.ts` (produto draft nunca retorna para anon) |
| T-04-02 | Information Disclosure | store_settings (WhatsApp exposto antes da hora da Fase 5) | high | mitigate | Migration 0004 explicitamente NÃO cria nenhuma policy em `store_settings`; testado (`store_settings` retorna `[]` para anon) |
| T-04-03 | Elevation of Privilege | policy `to anon` usando `for all` em vez de `for select` (permitiria INSERT/UPDATE/DELETE anônimo) | critical | mitigate | Todas as 4 policies desta migration são `for select` explicitamente; grep de acceptance criteria confirma ausência de `for all` |
</threat_model>

<verification>
- `npx vitest run tests/storefront/public-access-rls.test.ts` verde (acesso público exato: published sim, draft não, store_settings não)
- `grep -q "hide_when_sold_out" src/lib/database.types.ts` e `hide_sold_out_default` (typegen rodou contra o schema vivo)
- `npx tsc --noEmit` passa
- Migration contém exatamente 4 policies `to anon`, todas `for select`, e as 2 colunas novas com a nulabilidade correta
</verification>

<success_criteria>
- As 4 policies RLS `to anon` existem no banco remoto, restritas a `status = 'published'`
- `store_settings` permanece inacessível a `anon` (nenhuma policy nova ali)
- `products.hide_when_sold_out` (nullable) e `stores.hide_sold_out_default` (not null default false) existem no banco remoto
- Teste de acesso público prova o escopo exato com dados reais (não mockado)
- Tipos regenerados refletindo o schema vivo
</success_criteria>

<output>
Create `.planning/phases/04-vitrine-p-blica-e-filtragem/04-01-SUMMARY.md` when done
</output>
