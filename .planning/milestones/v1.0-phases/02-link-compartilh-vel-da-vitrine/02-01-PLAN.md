---
phase: 02-link-compartilh-vel-da-vitrine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [LOJA-02, LOJA-03, LOJA-04]
user_setup: []

must_haves:
  truths:
    - "qrcode and lucide-react are installed and importable in the Next 16 bundle"
    - "lucide-react legitimacy is confirmed by a human before install (SUS flag from research)"
  artifacts:
    - "package.json lists qrcode, lucide-react and @types/qrcode"
  key_links:
    - "qrcode resolves for both the browser (toCanvas) and node (toDataURL) bundles used later this phase"
---

<objective>
Install the two phase dependencies the UI slices need — `qrcode` (1.5.4, QR generation for LOJA-03) and `lucide-react` (1.24.0, status/copy/download icons for LOJA-02/03/04 per 02-UI-SPEC) — plus `@types/qrcode`. `qrcode` was audited OK; `lucide-react` was flagged `[SUS]` by the Package Legitimacy Audit in 02-RESEARCH.md (its latest version published 3 days before research — almost certainly a false positive given 74M weekly downloads and an established org, but protocol requires a blocking human legitimacy gate before install).

Purpose: front-load the supply-chain gate to Wave 1 so later UI plans (02-04/05/06) can import both libraries freely without re-touching package.json.
Output: `qrcode`, `lucide-react`, `@types/qrcode` present in package.json and resolvable.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy gate for lucide-react (T-02-SC)</name>
  <read_first>.planning/phases/02-link-compartilh-vel-da-vitrine/02-RESEARCH.md (§Package Legitimacy Audit)</read_first>
  <files>none (human verification gate — no files changed)</files>
  <action>Do NOT install anything yet. This is a mandatory, non-auto-approvable supply-chain gate before Task 2. `qrcode` was audited OK; `lucide-react` carries a `[SUS]` "too-new latest version" flag and must be human-confirmed per the package legitimacy protocol. Pause and request human confirmation of the checks in the verify block below before proceeding to Task 2.</action>
  <what-built>Nothing built yet — this is a mandatory supply-chain verification before any install runs. `qrcode` was audited OK; `lucide-react` carries a `[SUS]` "too-new latest version" flag and must be human-confirmed per the package legitimacy protocol (this gate is never auto-approvable).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/lucide-react and confirm the package page loads and shows an established package (millions of weekly downloads, not a brand-new or typosquat entry).
    2. Run `npm view lucide-react repository.url` and confirm it resolves to `github.com/lucide-icons/lucide`.
    3. Run `npm view lucide-react version` and confirm it matches what npmjs.com shows (research recorded 1.24.0).
    4. Run `npm view lucide-react scripts.postinstall` and confirm it returns empty (no postinstall script).
    5. Repeat step 4 for qrcode: `npm view qrcode scripts.postinstall` returns empty.
  </how-to-verify>
  <verify>
    <human-check>Human confirms lucide-react's repository.url resolves to github.com/lucide-icons/lucide, the version matches npmjs.com, and neither lucide-react nor qrcode declares a postinstall script.</human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms lucide-react repository resolves to github.com/lucide-icons/lucide and there is no postinstall script.
    - Human types approval before Task 2 runs.
  </acceptance_criteria>
  <done>Human has approved lucide-react's legitimacy; Task 2 may install.</done>
  <resume-signal>Type "approved" to proceed with install, or describe the concern to abort.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install qrcode, lucide-react and @types/qrcode</name>
  <read_first>package.json</read_first>
  <files>package.json, package-lock.json</files>
  <action>After the Task 1 gate is approved, run `npm install qrcode@1.5.4 lucide-react@1.24.0` and `npm install -D @types/qrcode`. Pin the exact versions verified in 02-RESEARCH.md (qrcode 1.5.4, lucide-react 1.24.0). Do NOT install any other package. Do NOT add a caret/tilde downgrade of existing pinned deps. These libraries power the QR preview/download (LOJA-03, D-09/D-11), the copy-link icon (LOJA-04, D-13) and the slug-status icons (LOJA-02, D-03).</action>
  <verify>
    <automated>node -e "require.resolve('qrcode'); require.resolve('lucide-react')"</automated>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.qrcode||!p.dependencies['lucide-react']||!p.devDependencies['@types/qrcode']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `qrcode` and `lucide-react`; devDependencies include `@types/qrcode`.
    - Both `require.resolve('qrcode')` and `require.resolve('lucide-react')` succeed (exit 0).
  </acceptance_criteria>
  <done>qrcode, lucide-react and @types/qrcode are installed at the pinned versions and importable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev machine | Third-party package code is downloaded and executed at install/build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (`lucide-react` [SUS], `qrcode`) | high | mitigate | Blocking human legitimacy checkpoint (Task 1) before install; verify repo URL + absence of postinstall script; pin exact versions verified against the npm registry in 02-RESEARCH.md |
</threat_model>

<verification>
- `node -e "require.resolve('qrcode'); require.resolve('lucide-react')"` exits 0.
- `package.json` records the two runtime deps and the one dev dep at pinned versions.
- Human legitimacy gate for lucide-react was explicitly approved.
</verification>

<success_criteria>
qrcode, lucide-react and @types/qrcode are installed at pinned versions, importable in the project bundle, and the lucide-react supply-chain gate was human-approved.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `package.json` / `package-lock.json` entries: `qrcode@1.5.4`, `lucide-react@1.24.0`, `@types/qrcode` (dev)
- No new source symbols (dependency-enabling plan)
</artifacts>

<output>
Create `.planning/phases/02-link-compartilh-vel-da-vitrine/02-01-SUMMARY.md` when done.
</output>
