---
phase: 02-link-compartilh-vel-da-vitrine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/slug/slugify.ts
  - src/lib/slug/validation.ts
  - src/lib/slug/store-url.ts
  - src/lib/hooks/use-debounce.ts
  - src/lib/auth/actions.ts
  - tests/slug/slugify.test.ts
  - tests/slug/validation.test.ts
  - tests/slug/store-url.test.ts
autonomous: true
requirements: [LOJA-02]
user_setup:
  - service: none-required-optional-env
    why: "Public vitrine base URL used by buildStoreUrl; defaults to https://vitrinoo.app when unset (no deploy yet — see MEMORY: project has no GitHub/Vercel remote)"
    env_vars:
      - name: NEXT_PUBLIC_SITE_URL
        source: "Optional. Set to the deployed origin (e.g. https://vitrinoo.app) once the app is hosted. Falls back to https://vitrinoo.app if unset."

must_haves:
  truths:
    - "slugify folds diacritics correctly: 'Sapatênis São Paulo' becomes 'sapatenis-sao-paulo' (D-01, sem acento)"
    - "the slug Zod schema rejects strings under 3 or over 30 chars and any char outside [a-z0-9-] (D-02)"
    - "signup still produces a unique store slug, now via the shared slugify utility (no divergent algorithm)"
    - "buildStoreUrl('minha-loja') returns the base URL + '/loja/minha-loja'"
  artifacts:
    - "src/lib/slug/slugify.ts"
    - "src/lib/slug/validation.ts"
    - "src/lib/slug/store-url.ts"
    - "src/lib/hooks/use-debounce.ts"
  key_links:
    - "generateStoreSlug in src/lib/auth/actions.ts calls the shared slugify() — one slugify algorithm project-wide"
---

<objective>
Build the shared, test-first slug foundation every later slice consumes: a diacritic-folding `slugify()` (fixes 02-RESEARCH.md Pitfall 2 — the existing `generateStoreSlug` regex turns "café" into "caf-" and cannot satisfy D-01 "sem acento"), a slug-format Zod schema (D-02, 3–30 chars, `[a-z0-9-]`), a public-URL builder, and the first `useDebouncedValue` hook (D-03, ~400ms). Then update `generateStoreSlug` to call the shared `slugify()` so the codebase never diverges into two slugify algorithms (explicit warning in 02-CONTEXT.md Integration Points).

Purpose: single source of truth for slug normalization/validation/URL-building, unit-tested before any UI or DB work depends on it.
Output: four new `src/lib` modules + a refactored `generateStoreSlug`, all covered by fast unit tests.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-RESEARCH.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-PATTERNS.md
@src/lib/auth/actions.ts
@src/lib/validation/onboarding.ts
@tests/auth/signup.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared slugify() + fold-diacritics fix, and refactor generateStoreSlug to use it</name>
  <read_first>src/lib/auth/actions.ts, .planning/phases/02-link-compartilh-vel-da-vitrine/02-PATTERNS.md, tests/auth/signup.test.ts</read_first>
  <files>src/lib/slug/slugify.ts, src/lib/auth/actions.ts, tests/slug/slugify.test.ts</files>
  <behavior>
    - slugify("Sapatênis São Paulo") returns "sapatenis-sao-paulo" (diacritics folded, spaces to hyphens, lowercase)
    - slugify("café") returns "cafe" (D-01 "sem acento") — NOT "caf-"
    - slugify("  --Nike__Air!!  ") returns "nike-air" (leading/trailing hyphens trimmed, runs collapsed)
    - slugify("Ção Ótimo") returns "cao-otimo"
    - slugify("") returns "" (empty in, empty out — callers decide the fallback)
  </behavior>
  <action>Create `slugify(input: string): string` in `src/lib/slug/slugify.ts`. First normalize with Unicode NFD then strip combining diacritical marks (the U+0300–U+036F combining-marks block), then lowercase, then replace every run of characters outside `[a-z0-9]` with a single hyphen, then trim leading/trailing hyphens — this order is mandatory (fold BEFORE the non-alnum→hyphen replace, per 02-RESEARCH.md Pitfall 2 and Don't Hand-Roll table). Then refactor `generateStoreSlug` in `src/lib/auth/actions.ts` to derive its base by calling the shared `slugify(email.split("@")[0])` (falling back to "loja" when the result is empty) and keep the existing random suffix behavior — do NOT leave the old inline `.replace(/[^a-z0-9]+/g, "-")` regex in place (that is the divergent second algorithm 02-CONTEXT.md forbids). Do NOT change the signup redirect or store-insert behavior. Write `tests/slug/slugify.test.ts` covering the behavior cases above before implementing (RED → GREEN).</action>
  <verify>
    <automated>npx vitest run tests/slug/slugify.test.ts</automated>
    <automated>npx vitest run tests/auth/signup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/slug/slugify.test.ts` passes, including the accented cases that produce fully-folded output (no dropped letters, no doubled hyphens).
    - `tests/auth/signup.test.ts` still passes (generateStoreSlug regression guard — signup still produces a unique slug).
    - `src/lib/auth/actions.ts` imports and calls `slugify` from `@/lib/slug/slugify`; the inline non-alnum regex no longer lives in generateStoreSlug.
  </acceptance_criteria>
  <done>slugify() folds diacritics per D-01, generateStoreSlug reuses it, and both the new and existing signup tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Slug-format Zod schema (D-02)</name>
  <read_first>src/lib/validation/onboarding.ts, .planning/phases/02-link-compartilh-vel-da-vitrine/02-PATTERNS.md, .planning/phases/02-link-compartilh-vel-da-vitrine/02-UI-SPEC.md</read_first>
  <files>src/lib/slug/validation.ts, tests/slug/validation.test.ts</files>
  <behavior>
    - a 2-char slug is rejected; a 31-char slug is rejected; a 3-char and a 30-char slug are accepted (D-02)
    - a slug containing uppercase, spaces, accents, or symbols other than hyphen is rejected with the invalid-format message
    - a slug starting or ending with a hyphen is rejected
    - the exported inferred type SlugInput is a string
  </behavior>
  <action>Create `slugSchema` in `src/lib/slug/validation.ts` following the named-regex-constant + trim + Portuguese-message + exported-inferred-type convention from `src/lib/validation/onboarding.ts`. Enforce: min 3 and max 30 chars (D-02), charset `[a-z0-9-]` only, and no leading/trailing hyphen. Use the exact copywriting-contract strings from 02-UI-SPEC.md for the format error ("Use apenas letras, números e hífens (3 a 30 caracteres)."). Export `type SlugInput`. Write `tests/slug/validation.test.ts` (boundary cases 2/3/30/31, invalid charset, leading/trailing hyphen) before implementing.</action>
  <verify>
    <automated>npx vitest run tests/slug/validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/slug/validation.test.ts` passes all boundary and charset cases.
    - Invalid-format rejection message matches the 02-UI-SPEC.md Copywriting Contract string exactly.
  </acceptance_criteria>
  <done>slugSchema rejects out-of-range/invalid slugs with the UI-SPEC copy and accepts valid ones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Public-URL builder + debounce hook</name>
  <read_first>.planning/phases/02-link-compartilh-vel-da-vitrine/02-RESEARCH.md (§Pattern 2), src/app/loja/[slug]/page.tsx</read_first>
  <files>src/lib/slug/store-url.ts, src/lib/hooks/use-debounce.ts, tests/slug/store-url.test.ts</files>
  <behavior>
    - buildStoreUrl("minha-loja") returns "https://vitrinoo.app/loja/minha-loja" when NEXT_PUBLIC_SITE_URL is unset
    - buildStoreUrl trims a trailing slash from the configured base so the result never contains "//loja"
  </behavior>
  <action>Create `buildStoreUrl(slug: string): string` in `src/lib/slug/store-url.ts` that reads `process.env.NEXT_PUBLIC_SITE_URL`, falls back to the literal default origin `https://vitrinoo.app` when unset/empty, strips any trailing slash from the base, and returns base + `/loja/` + slug (the public vitrine route is `src/app/loja/[slug]/page.tsx`). Create `useDebouncedValue<T>(value: T, delayMs: number): T` in `src/lib/hooks/use-debounce.ts` as a `"use client"` module using the useState+useEffect+setTimeout pattern from 02-RESEARCH.md Pattern 2 (this is the first debounce hook in the codebase). Write `tests/slug/store-url.test.ts` for the buildStoreUrl behavior cases (the hook is exercised by the slug editor in plan 02-05 and by React runtime, not unit-tested here).</action>
  <verify>
    <automated>npx vitest run tests/slug/store-url.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tests/slug/store-url.test.ts` passes (default-origin and no-double-slash cases).
    - `npx tsc --noEmit` passes (the "use client" hook and the URL builder both typecheck).
  </acceptance_criteria>
  <done>buildStoreUrl composes the public vitrine URL deterministically and useDebouncedValue is available for the slug editor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| revendedor input → slug value | Free-typed store name/slug is normalized before it becomes a public URL segment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | slugify() output used as a URL path segment | medium | mitigate | slugify collapses everything outside [a-z0-9-] and trims hyphens, so the output can never contain path-traversal or protocol characters; slugSchema re-validates charset/length before persistence (plan 02-03) |
| T-02-02 | Information Disclosure | divergent slugify algorithms causing inconsistent slugs | low | mitigate | single shared slugify() used by both signup and the slug editor (generateStoreSlug refactor) — removes the drift risk 02-CONTEXT.md flags |
</threat_model>

<verification>
- `npx vitest run tests/slug/` is green (slugify, validation, store-url).
- `npx vitest run tests/auth/signup.test.ts` still green (no signup regression).
- `npx tsc --noEmit` passes.
</verification>

<success_criteria>
slugify folds diacritics per D-01, slugSchema enforces D-02, buildStoreUrl builds the public URL, useDebouncedValue exists, and generateStoreSlug reuses the shared slugify — all unit-tested, no signup regression.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `slugify(input: string): string` — `src/lib/slug/slugify.ts`
- `slugSchema` + `type SlugInput` — `src/lib/slug/validation.ts`
- `buildStoreUrl(slug: string): string` — `src/lib/slug/store-url.ts`
- `useDebouncedValue<T>(value: T, delayMs: number): T` — `src/lib/hooks/use-debounce.ts`
- Modified: `generateStoreSlug` in `src/lib/auth/actions.ts` now delegates to `slugify()`
</artifacts>

<output>
Create `.planning/phases/02-link-compartilh-vel-da-vitrine/02-02-SUMMARY.md` when done.
</output>
