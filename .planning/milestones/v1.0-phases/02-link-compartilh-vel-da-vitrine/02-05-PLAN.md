---
phase: 02-link-compartilh-vel-da-vitrine
plan: 05
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - src/app/(admin)/configuracoes/slug-editor.tsx
autonomous: true
requirements: [LOJA-02]
user_setup: []

must_haves:
  truths:
    - "typing in the slug field auto-slugifies every keystroke (lowercase, sem acento, spaces to hyphens) per D-01"
    - "after ~400ms of no typing the field shows Verificando / Disponível / 'Este link já está em uso.' based on the live uniqueness check (D-03)"
    - "changing the slug requires confirming a native dialog that warns in plain language the old link will break (D-04, D-08); cancelling changes nothing"
    - "the slug is only written on the explicit confirm button, never on dialog cancel/escape (Pitfall 4)"
  artifacts:
    - "src/app/(admin)/configuracoes/slug-editor.tsx (real implementation)"
  key_links:
    - "slug-editor calls checkSlugAvailability (debounced via useDebouncedValue) and updateStoreSlug only on the explicit 'Sim, trocar o link' click"
---

<objective>
Replace the slug-editor placeholder with the real live-uniqueness slug editor (LOJA-02, D-01/D-02/D-03/D-04/D-08). As the revendedor types, the value is auto-slugified every keystroke via the shared `slugify()` (D-01); the slugified value is debounced ~400ms via `useDebouncedValue` (D-03) then checked through `checkSlugAvailability`, driving an inline status pill (checking / available / taken) using the exact 02-UI-SPEC copy and lucide-react icons. Saving the slug is a separate "Salvar novo link" action (D-08, isolated from the Loja/WhatsApp form) that opens a native `<dialog>` confirmation warning in plain, non-technical language that the old link stops working (D-04, and the 02-CONTEXT.md `<specifics>` note — no "404" jargon); `updateStoreSlug` fires ONLY from the explicit confirm button, never on cancel/escape (Pitfall 4).

Purpose: after this plan a revendedor can safely change their vitrine slug with real-time feedback and an explicit, understandable confirmation.
Output: the interactive slug editor component.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-RESEARCH.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-PATTERNS.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-UI-SPEC.md
@src/app/(admin)/onboarding/onboarding-wizard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slug input with auto-slugify + debounced live availability (D-01, D-02, D-03)</name>
  <read_first>src/app/(admin)/configuracoes/slug-editor.tsx, .planning/phases/02-link-compartilh-vel-da-vitrine/02-RESEARCH.md (§Pattern 2, §Open Questions), .planning/phases/02-link-compartilh-vel-da-vitrine/02-UI-SPEC.md</read_first>
  <files>src/app/(admin)/configuracoes/slug-editor.tsx</files>
  <action>Rewrite the placeholder `slug-editor.tsx` as the real `"use client"` component taking `currentSlug: string`. Track raw input state; derive the displayed slug by calling `slugify(raw)` from `@/lib/slug/slugify` on every keystroke (D-01). Validate format synchronously with `slugSchema` from `@/lib/slug/validation` for instant format feedback (do NOT debounce format checks — 02-RESEARCH.md Open Question 2), showing the exact invalid-format copy from 02-UI-SPEC when format fails. Debounce the slugified value with `useDebouncedValue(slug, 400)` (D-03); in an effect, when the debounced value is ≥3 chars, format-valid, and differs from currentSlug, set status "checking" and call `checkSlugAvailability` inside `useTransition`, mapping the result to status "available" / "taken". Render an inline status pill directly under the input using the exact 02-UI-SPEC copy — "Verificando disponibilidade…" (neutral #6B6B6B), "Disponível" (#00C46A), "Este link já está em uso." (#FF4D4D) — each with the appropriate lucide-react icon (a spinner/loader, a check, and an x). Reuse the label/input classes and focus color from onboarding-wizard.tsx. Leave the save button + dialog to Task 2 (keep a disabled-state hook so save is only enabled when status is "available").</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -q "useDebouncedValue" "src/app/(admin)/configuracoes/slug-editor.tsx" && grep -q "checkSlugAvailability" "src/app/(admin)/configuracoes/slug-editor.tsx" && grep -q "slugify" "src/app/(admin)/configuracoes/slug-editor.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The component slugifies on keystroke, validates format synchronously, and debounces only the network uniqueness check (~400ms).
    - The status pill uses the exact 02-UI-SPEC strings and colors with lucide-react icons.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The slug input auto-slugifies, validates format instantly, and shows debounced live availability feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Separate "Salvar novo link" action + native dialog confirmation (D-04, D-08, Pitfall 4)</name>
  <read_first>.planning/phases/02-link-compartilh-vel-da-vitrine/02-RESEARCH.md (§Pattern 3, §Pitfall 4), .planning/phases/02-link-compartilh-vel-da-vitrine/02-UI-SPEC.md, .planning/phases/02-link-compartilh-vel-da-vitrine/02-CONTEXT.md</read_first>
  <files>src/app/(admin)/configuracoes/slug-editor.tsx</files>
  <action>Add the dedicated "Salvar novo link" button (02-UI-SPEC copy, accent #00C46A, enabled only when status is "available") which opens a native `<dialog>` via `showModal()` on a ref (D-08 — its own save/confirm, separate from the Loja/WhatsApp form). The dialog uses the exact 02-UI-SPEC Destructive-confirmation copy: title "Trocar o link da sua vitrine?", the plain-language body warning that already-shared links will stop working (no "404" jargon per the 02-CONTEXT.md specifics note), a "Cancelar" button (`<form method="dialog">` submit) and a destructive "Sim, trocar o link" button (#FF4D4D). Wire `updateStoreSlug(debouncedSlug)` inside useTransition to fire ONLY from the "Sim, trocar o link" button's onClick, then close the dialog — never from the dialog's cancel/close/escape events (Pitfall 4). On success, `toast.success` and reflect the new slug (call `router.refresh()` so the page's publicUrl/QR/readonly-link update); on the 23505 friendly error, `toast.error(result.error)`. Cancelling or pressing Escape must leave the slug unchanged.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -q "showModal" "src/app/(admin)/configuracoes/slug-editor.tsx" && grep -q "updateStoreSlug" "src/app/(admin)/configuracoes/slug-editor.tsx"</automated>
    <human-check>Run `npm run dev`, log in, go to /configuracoes → Link e QR Code. Type a name with accents (e.g. "Café São Paulo") and confirm it slugifies to "cafe-sao-paulo" with no dropped letters. Type an existing slug (seed a second account first) and confirm "Este link já está em uso." appears; type a fresh one and confirm "Disponível". Click "Salvar novo link", confirm the dialog text is understandable and warns the old link breaks; press Escape / Cancelar and confirm the slug did NOT change; reopen and click "Sim, trocar o link" and confirm the slug saves, the toast shows, and the QR/link update to the new slug.</human-check>
  </verify>
  <acceptance_criteria>
    - `showModal()` opens the confirmation; `updateStoreSlug` is invoked only from the explicit confirm button, not from cancel/escape (Pitfall 4).
    - The dialog copy matches the 02-UI-SPEC Destructive-confirmation contract exactly and contains no technical jargon.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Slug changes require an explicit, plain-language confirmation; the write fires only on confirm and the page reflects the new slug.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slug editor → updateStoreSlug | A slug change is destructive (breaks shared links, D-04) and must be intentional |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | accidental slug change breaking already-shared links | medium | mitigate | Separate save button + native `<dialog>` confirmation (D-08); write fires only from the explicit confirm onClick, never on cancel/escape (Pitfall 4) |
| T-02-10 | Tampering | client-only slug validation bypass | medium | mitigate | slugSchema is re-validated server-side inside checkSlugAvailability/updateStoreSlug (plan 02-03); the client checks are UX only, the DB UNIQUE constraint is the final guard |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- slug-editor uses useDebouncedValue + slugify + checkSlugAvailability + updateStoreSlug + showModal.
- Human check confirms accent-folding, live status, and that cancel/escape never changes the slug while confirm does.
</verification>

<success_criteria>
A revendedor can retype their slug with instant slugify + debounced availability feedback and change it only through an explicit, plain-language confirmation dialog that writes on confirm alone.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `SlugEditor` (real) — `src/app/(admin)/configuracoes/slug-editor.tsx`
</artifacts>

<output>
Create `.planning/phases/02-link-compartilh-vel-da-vitrine/02-05-SUMMARY.md` when done.
</output>
