---
phase: 02-link-compartilh-vel-da-vitrine
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - supabase/migrations/0002_slug_availability_rpc.sql
  - src/lib/database.types.ts
  - src/lib/settings/actions.ts
  - tests/settings/slug-availability.test.ts
  - tests/settings/update-slug.test.ts
  - tests/settings/store-settings-update.test.ts
autonomous: false
requirements: [LOJA-02]
user_setup:
  - service: supabase
    why: "Schema push of the new is_slug_available RPC to the remote project the tests run against"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (only needed if `supabase db push` cannot use an existing interactive login in this environment)"

must_haves:
  truths:
    - "is_slug_available returns false for a slug already owned by a DIFFERENT tenant (cross-tenant RLS regression guard — the single most important behavior this phase)"
    - "is_slug_available returns true for an unused slug"
    - "updateStoreSlug surfaces the friendly 'Este link já está em uso. Escolha outro.' message on a Postgres unique_violation (23505), never a raw DB error"
    - "saveStoreSettings persists edited name/color/tagline/whatsapp/template scoped by owner_id, reusing normalizeWhatsAppBR"
    - "the migration is pushed to the remote DB and src/lib/database.types.ts is regenerated to include the RPC"
  artifacts:
    - "supabase/migrations/0002_slug_availability_rpc.sql"
    - "src/lib/settings/actions.ts"
  key_links:
    - "checkSlugAvailability calls supabase.rpc('is_slug_available', { candidate_slug })"
    - "stores.slug UNIQUE constraint (from 0001) is the true race-condition safety net behind updateStoreSlug"
---

<objective>
Build the backend that makes real-time slug uniqueness possible without breaking tenant isolation. The `stores` RLS policy (`owner_id = auth.uid()`, from 0001) structurally blocks any RLS-bound client from seeing OTHER tenants' rows, so a naive `.from('stores').select().eq('slug', x)` always reports "available" even when a slug is taken (02-RESEARCH.md Pitfall 1 — verified against the codebase). The fix is a `SECURITY DEFINER` Postgres RPC `is_slug_available` returning ONLY a boolean, granted only to `authenticated`, with a pinned `search_path`. Then create the three settings Server Actions (`checkSlugAvailability`, `updateStoreSlug`, `saveStoreSettings`) — all in one file since they share `src/lib/settings/actions.ts` — with the mandatory `[BLOCKING]` schema push + type regen ordered between the migration file and the integration tests.

Purpose: satisfy D-03 (real-time uniqueness) correctly and provide the save actions the UI slices call.
Output: the migration, the pushed RPC, regenerated DB types, three Server Actions, and three integration tests against the real Supabase project.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-RESEARCH.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-PATTERNS.md
@supabase/migrations/0001_init_stores_rls.sql
@src/lib/onboarding/actions.ts
@src/lib/validation/onboarding.ts
@src/lib/phone/normalize-br.ts
@tests/rls/isolation.test.ts
@tests/onboarding/store-settings.test.ts
@tests/setup/supabase-test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — is_slug_available SECURITY DEFINER RPC (D-03, Pitfall 1)</name>
  <read_first>supabase/migrations/0001_init_stores_rls.sql, .planning/phases/02-link-compartilh-vel-da-vitrine/02-RESEARCH.md (§Pattern 1, §Security Domain), .planning/phases/02-link-compartilh-vel-da-vitrine/02-PATTERNS.md</read_first>
  <files>supabase/migrations/0002_slug_availability_rpc.sql</files>
  <action>Create `supabase/migrations/0002_slug_availability_rpc.sql` following the comment-header convention of 0001 (a Portuguese header explaining WHY, citing Pitfall 1 of 02-RESEARCH.md by name). Define `public.is_slug_available(candidate_slug text) returns boolean`, `language sql`, `security definer`, with `set search_path = public, pg_temp` pinned on the function (mitigates the search_path-hijack EoP class per 02-RESEARCH.md Security Domain), whose body returns `not exists (select 1 from stores where slug = candidate_slug)` — a boolean only, never row data. Then `grant execute on function public.is_slug_available(text) to authenticated;` (only `authenticated`, not `anon` — 02-RESEARCH.md Open Question 1). Do NOT loosen any existing RLS policy on `stores`; the RPC is the narrow, explicit exception, not a broadened policy.</action>
  <verify>
    <automated>grep -q "is_slug_available" supabase/migrations/0002_slug_availability_rpc.sql</automated>
    <automated>grep -qi "security definer" supabase/migrations/0002_slug_availability_rpc.sql && grep -qi "search_path" supabase/migrations/0002_slug_availability_rpc.sql && grep -qi "to authenticated" supabase/migrations/0002_slug_availability_rpc.sql</automated>
  </verify>
  <acceptance_criteria>
    - The migration file defines `is_slug_available` as `security definer` with a pinned `search_path` and grants execute to `authenticated`.
    - The function body returns a boolean derived from `not exists`, exposing no row columns.
    - No existing RLS policy in the migration set is weakened.
  </acceptance_criteria>
  <done>The RPC migration exists, is boolean-only, SECURITY DEFINER with pinned search_path, and granted only to authenticated.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push schema to remote + regenerate DB types</name>
  <read_first>supabase/config.toml, src/lib/database.types.ts, tests/setup/supabase-test.ts</read_first>
  <files>src/lib/database.types.ts</files>
  <action>[BLOCKING — must run after Task 1 and before Task 3.] Apply the new migration to the remote Supabase project the tests run against (the test harness in tests/setup/supabase-test.ts connects to the live project via NEXT_PUBLIC_SUPABASE_URL / ANON_KEY): run `supabase db push`. If the command needs auth in this non-interactive environment, set `SUPABASE_ACCESS_TOKEN` first; if it still requires an interactive prompt that cannot be suppressed, STOP and surface this task for manual intervention (this plan is already `autonomous: false`). After the push succeeds, regenerate the typed schema so `supabase.rpc("is_slug_available", …)` typechecks — the current `src/lib/database.types.ts` has `Functions: { [_ in never]: never }` and would fail typechecking otherwise. Run `supabase gen types typescript --linked` (or `--project-id vitrinoo`) and write the output over `src/lib/database.types.ts`. Fallback if the CLI is unavailable: apply the SQL via the Supabase Dashboard SQL editor, then still regenerate types. Do NOT hand-edit database.types.ts to fake the function entry — the type must come from the live schema so it proves the push actually landed.</action>
  <verify>
    <automated>grep -q "is_slug_available" src/lib/database.types.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/database.types.ts` contains `is_slug_available` under the public schema Functions (proving the migration is live and types were regenerated from it).
    - `npx tsc --noEmit` passes with the regenerated types.
  </acceptance_criteria>
  <done>The migration is applied to the remote DB and the regenerated types include the RPC.</done>
</task>

<task type="auto">
  <name>Task 3: Settings Server Actions + integration tests</name>
  <read_first>src/lib/onboarding/actions.ts, src/lib/validation/onboarding.ts, src/lib/phone/normalize-br.ts, src/lib/slug/validation.ts, tests/rls/isolation.test.ts, tests/onboarding/store-settings.test.ts, tests/setup/supabase-test.ts</read_first>
  <files>src/lib/settings/actions.ts, tests/settings/slug-availability.test.ts, tests/settings/update-slug.test.ts, tests/settings/store-settings-update.test.ts</files>
  <action>Create `src/lib/settings/actions.ts` with the top-of-file `"use server"` directive and three exported async Server Actions, all following the established "getUser() → look up store by owner_id → operate scoped by store.id" three-step sequence from `saveOnboarding` (src/lib/onboarding/actions.ts lines 85-100):
    (1) `checkSlugAvailability(candidateSlug: string)` — safeParse with `slugSchema` from `@/lib/slug/validation` (return `{ available: false, error }` on invalid format, D-03 server-side re-validation), then `supabase.rpc("is_slug_available", { candidate_slug })`, returning `{ available: boolean }` or `{ available: false, error: "Não foi possível verificar o link agora." }` on RPC error.
    (2) `updateStoreSlug(newSlug: string)` — re-validate with slugSchema, then `.from("stores").update({ slug: newSlug }).eq("id", store.id)`; on error, if `error.code === "23505"` return `{ error: "Este link já está em uso. Escolha outro." }` (the TOCTOU race safety net, 02-RESEARCH.md Pitfall 3), else a generic friendly error; on success return `{ success: true }`.
    (3) `saveStoreSettings(formData: FormData)` — mirror `saveOnboarding` exactly (reuse `onboardingSchema` per D-07 and `normalizeWhatsAppBR`), updating `stores` (name/accent_color/tagline) and `store_settings` (whatsapp_e164/message_template) scoped by owner_id, but WITHOUT setting onboarding_completed_at and WITHOUT any redirect() — this is an in-place settings save that returns `{ success: true }` or `{ error }`. (Logo upload may reuse the onboarding validateLogoFile pattern only if a logo file is present; keep it optional.)
  Write the three integration tests test-first, adapting fixtures from the existing analog tests (they seed real accounts via signUp + signInWithPassword through the RLS policies, never service_role — Padrão 4):
    - `tests/settings/slug-availability.test.ts`: seed two stores under two different owners (mirror tests/rls/isolation.test.ts); assert `is_slug_available` (or checkSlugAvailability) returns available=false for a slug owned by the OTHER store, and true for an unused slug. This is the cross-tenant RLS regression guard (Threat T-02-03).
    - `tests/settings/update-slug.test.ts`: seed two stores; have store B already hold slug X; assert store A's updateStoreSlug(X) returns the friendly "já está em uso" error (23505 path), and that a fresh unused slug succeeds.
    - `tests/settings/store-settings-update.test.ts`: adapt tests/onboarding/store-settings.test.ts; complete onboarding, then call saveStoreSettings with edited values and assert the stores/store_settings rows reflect the edits (name/accent_color/tagline/whatsapp_e164/message_template) and that onboarding_completed_at is unchanged (not reset).</action>
  <verify>
    <automated>npx vitest run tests/settings/slug-availability.test.ts tests/settings/update-slug.test.ts tests/settings/store-settings-update.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/settings/slug-availability.test.ts` proves cross-tenant detection: available=false for a slug owned by a different tenant.
    - `tests/settings/update-slug.test.ts` proves the 23505 path returns the exact "Este link já está em uso. Escolha outro." message.
    - `tests/settings/store-settings-update.test.ts` proves saveStoreSettings persists edits scoped by owner_id and leaves onboarding_completed_at intact.
    - All three actions live in `src/lib/settings/actions.ts` and follow the getUser → owner-scoped-store pattern.
  </acceptance_criteria>
  <done>The three settings actions work against the live DB and their integration tests pass, including the critical cross-tenant slug regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated revendedor → Server Action → Postgres | Slug availability crosses tenant isolation via an elevated-privilege RPC |
| Server Action → stores.slug write | Two revendedores may race for the same slug (TOCTOU) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Information Disclosure | is_slug_available RPC (SECURITY DEFINER bypasses RLS) | high | mitigate | Function returns ONLY a boolean via `not exists`, never row columns; granted only to `authenticated`; regression test asserts no cross-tenant row data is reachable |
| T-02-04 | Elevation of Privilege | SECURITY DEFINER search_path hijack | high | mitigate | Pin `set search_path = public, pg_temp` on the function definition (Task 1) |
| T-02-05 | Tampering | slug uniqueness TOCTOU race between debounce check and save | medium | mitigate | DB-level UNIQUE constraint on stores.slug (from 0001) is the true enforcement; updateStoreSlug catches error code 23505 and returns the friendly message (Task 3, tested) |
| T-02-06 | Information Disclosure | authenticated slug enumeration via the boolean RPC | low | accept | Slugs are public vitrine URLs by design (meant to be shared); existence of a slug leaks nothing sensitive; grant limited to authenticated keeps it off the anonymous surface |
</threat_model>

<verification>
- `npx vitest run tests/settings/` is green (all three integration tests).
- `src/lib/database.types.ts` includes `is_slug_available` (migration pushed + types regenerated).
- `npx tsc --noEmit` passes.
- The migration is boolean-only, SECURITY DEFINER, pinned search_path, authenticated-only grant.
</verification>

<success_criteria>
The RPC migration is live, types regenerated, and the three settings Server Actions pass integration tests — including the cross-tenant slug-availability regression and the 23505 unique-violation friendly-error path.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- SQL function `public.is_slug_available(candidate_slug text) returns boolean` — `supabase/migrations/0002_slug_availability_rpc.sql`
- `checkSlugAvailability(candidateSlug: string)` — `src/lib/settings/actions.ts`
- `updateStoreSlug(newSlug: string)` — `src/lib/settings/actions.ts`
- `saveStoreSettings(formData: FormData)` — `src/lib/settings/actions.ts`
- Regenerated `src/lib/database.types.ts` (now includes the RPC under public Functions)
</artifacts>

<output>
Create `.planning/phases/02-link-compartilh-vel-da-vitrine/02-03-SUMMARY.md` when done.
</output>
