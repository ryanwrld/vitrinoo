---
phase: 02-link-compartilh-vel-da-vitrine
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - src/app/(admin)/configuracoes/page.tsx
  - src/app/(admin)/configuracoes/settings-form.tsx
  - src/app/(admin)/configuracoes/slug-editor.tsx
  - src/app/(admin)/configuracoes/qr-code-panel.tsx
autonomous: true
requirements: [LOJA-02, LOJA-03, LOJA-04]
user_setup: []

must_haves:
  truths:
    - "/configuracoes is only reachable after a valid session AND completed onboarding (same guard as /dashboard) (D-05)"
    - "the revendedor can edit and save store identity (name/logo/color/tagline) and WhatsApp (number/template) in place and see a success toast (D-07, onboarding-revisit — SC #4)"
    - "the page shows a single scrolling layout with three sections in order: Loja, WhatsApp, Link e QR Code (D-06, no tabs)"
  artifacts:
    - "src/app/(admin)/configuracoes/page.tsx"
    - "src/app/(admin)/configuracoes/settings-form.tsx"
    - "src/app/(admin)/configuracoes/slug-editor.tsx (placeholder shell)"
    - "src/app/(admin)/configuracoes/qr-code-panel.tsx (placeholder shell)"
  key_links:
    - "settings-form calls saveStoreSettings from src/lib/settings/actions.ts"
    - "page.tsx computes the public URL via buildStoreUrl(store.slug) and passes it down"
---

<objective>
Deliver the first user-facing slice: the `/configuracoes` screen (D-05, dedicated admin route) as a single scrolling page with three sections — Loja, WhatsApp, Link e QR Code (D-06, no tabs) — where the revendedor can revisit and save the store identity and WhatsApp settings collected in Phase 1's onboarding (LOJA-01/WPP-01/WPP-02 revisit, ROADMAP SC #4). The Loja+WhatsApp form is written fresh for this screen (D-07 — reuse the `onboardingSchema` Zod schema and `normalizeWhatsAppBR`, but NOT the onboarding-wizard component) and saved via `saveStoreSettings` with a success toast (D-12 toast convention). The slug editor and QR/copy panel are rendered as thin placeholder shells here so the page structure and route are complete and buildable; plans 02-05 and 02-06 replace those shells with their real interactive implementations.

Purpose: after this plan a revendedor can open /configuracoes and actually edit + save their store and WhatsApp settings.
Output: the route page, the working Loja/WhatsApp settings form, and two placeholder section components.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-UI-SPEC.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-PATTERNS.md
@src/app/(admin)/dashboard/page.tsx
@src/app/(admin)/onboarding/onboarding-wizard.tsx
@src/lib/auth/onboarding-guard.ts
@src/lib/validation/onboarding.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Loja/WhatsApp settings form (D-07) + placeholder section shells</name>
  <read_first>src/app/(admin)/onboarding/onboarding-wizard.tsx, src/lib/validation/onboarding.ts, .planning/phases/02-link-compartilh-vel-da-vitrine/02-UI-SPEC.md, .planning/phases/02-link-compartilh-vel-da-vitrine/02-PATTERNS.md</read_first>
  <files>src/app/(admin)/configuracoes/settings-form.tsx, src/app/(admin)/configuracoes/slug-editor.tsx, src/app/(admin)/configuracoes/qr-code-panel.tsx</files>
  <action>Create `settings-form.tsx` as a fresh `"use client"` component (D-07 — do NOT import onboarding-wizard.tsx). Accept the current store + settings as props (name, accent_color, tagline, whatsapp_e164, message_template). Use `useForm` with `zodResolver(onboardingSchema)` imported from `@/lib/validation/onboarding`, defaulting each field from the props so existing values pre-fill. Reuse the exact label/input/error markup convention and hex colors (#111111 label, #F5F5F3 border, #00C46A focus, #FF4D4D error) from onboarding-wizard.tsx for the Loja section (name, logo, accent color, tagline) and the WhatsApp section (number with the `new AsYouType("BR").input(...)` live preview, message template textarea). One shared "Salvar alterações" button (02-UI-SPEC Copywriting Contract) at the bottom submits via useTransition → `saveStoreSettings` (from `@/lib/settings/actions`), calling `toast.success("Configurações salvas!")` on success and `toast.error(result.error)` on failure (D-12 / 02-UI-SPEC error copy). Also create `slug-editor.tsx` and `qr-code-panel.tsx` as minimal `"use client"` placeholder shells: each renders its section container/heading only ("Link e QR Code" area) with a short "em breve" note and accepts the props its real version will need (slug-editor: `currentSlug: string`; qr-code-panel: `publicUrl: string`) so page.tsx can wire them now. Do NOT import qrcode or lucide-react in the shells (those arrive with the real implementations in 02-05/02-06).</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - settings-form.tsx uses `onboardingSchema` and calls `saveStoreSettings`; it does NOT import onboarding-wizard.tsx (D-07).
    - The form pre-fills every field from props and shows the AsYouType WhatsApp preview.
    - slug-editor.tsx and qr-code-panel.tsx exist as typed placeholder components accepting `currentSlug`/`publicUrl` respectively.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The Loja/WhatsApp form persists edits via saveStoreSettings with a success toast; placeholder shells exist for the slug and QR sections.</done>
</task>

<task type="auto">
  <name>Task 2: /configuracoes route page (guard + data fetch + wiring)</name>
  <read_first>src/app/(admin)/dashboard/page.tsx, src/lib/auth/onboarding-guard.ts, src/lib/slug/store-url.ts, .planning/phases/02-link-compartilh-vel-da-vitrine/02-UI-SPEC.md</read_first>
  <files>src/app/(admin)/configuracoes/page.tsx</files>
  <action>Create `src/app/(admin)/configuracoes/page.tsx` as an async Server Component whose FIRST line is `await requireCompletedOnboarding()` (same combined auth+onboarding gate as dashboard/page.tsx, D-05). Then fetch the store row (id, name, slug, logo_url, accent_color, tagline) and the store_settings row (whatsapp_e164, message_template) via `createClient()` scoped by `owner_id = auth.uid()` (query shape per onboarding-guard.ts). Compute `publicUrl = buildStoreUrl(store.slug)` from `@/lib/slug/store-url`. Render the page `<main>` with the same container classes as the other admin pages (bg-white, max-w-md, px-4 py-10, gap-6), an `<h1>` "Configurações" (#0D3D2B, text-2xl font-bold — the single bold exception per 02-UI-SPEC Typography), and the three stacked sections in order (D-06): the Loja+WhatsApp `<SettingsForm store settings/>`, then a "Link e QR Code" section rendering `<SlugEditor currentSlug={store.slug}/>` and `<QrCodePanel publicUrl={publicUrl}/>`. Do NOT add `"use cache"` anywhere on this route (dynamic-by-default, 02-RESEARCH.md — slug changes must reflect immediately, D-04). Optionally include a "Voltar ao painel" outline link back to /dashboard mirroring the dashboard's Sair-da-conta outline-button style.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -q "requireCompletedOnboarding" "src/app/(admin)/configuracoes/page.tsx"</automated>
    <human-check>Run `npm run dev`, log in, visit /configuracoes: the page renders three sections (Loja, WhatsApp, Link e QR Code); editing the store name / accent color / tagline / WhatsApp and pressing "Salvar alterações" shows the "Configurações salvas!" toast and the values persist after refresh. Visiting /configuracoes while logged out redirects to /login.</human-check>
  </verify>
  <acceptance_criteria>
    - page.tsx calls `requireCompletedOnboarding()` as its first statement and never adds `"use cache"`.
    - The page passes `buildStoreUrl(store.slug)` to QrCodePanel and `store.slug` to SlugEditor.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>/configuracoes is guarded, fetches store+settings, computes the public URL, and renders the three sections; the settings-edit slice works end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /configuracoes admin route | Only an authenticated, onboarded revendedor may reach the settings screen |
| settings-form → saveStoreSettings | Edited store/WhatsApp values cross into a scoped DB write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Spoofing / Elevation of Privilege | /configuracoes route access | high | mitigate | `requireCompletedOnboarding()` runs first (auth gate then onboarding gate, as two sequential checks) — same proven guard as /dashboard |
| T-02-08 | Tampering | client-submitted settings values | medium | mitigate | saveStoreSettings re-validates with onboardingSchema server-side and scopes the write by owner_id (never trust the client form) — enforced in plan 02-03 |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- page.tsx guards with requireCompletedOnboarding and adds no `"use cache"`.
- Human check confirms the three-section layout and a working save-with-toast on /configuracoes.
</verification>

<success_criteria>
A revendedor can open /configuracoes (guarded), see the three sections in order, and edit + save store identity and WhatsApp settings in place with a success toast.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- Route `src/app/(admin)/configuracoes/page.tsx` (Server Component, guarded)
- `SettingsForm` — `src/app/(admin)/configuracoes/settings-form.tsx`
- `SlugEditor` placeholder — `src/app/(admin)/configuracoes/slug-editor.tsx`
- `QrCodePanel` placeholder — `src/app/(admin)/configuracoes/qr-code-panel.tsx`
</artifacts>

<output>
Create `.planning/phases/02-link-compartilh-vel-da-vitrine/02-04-SUMMARY.md` when done.
</output>
