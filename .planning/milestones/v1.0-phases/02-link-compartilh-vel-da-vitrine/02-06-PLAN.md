---
phase: 02-link-compartilh-vel-da-vitrine
plan: 06
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-04]
files_modified:
  - src/lib/qr.ts
  - src/lib/clipboard.ts
  - src/app/(admin)/configuracoes/qr-code-panel.tsx
  - tests/settings/qr-code.test.ts
  - tests/settings/copy-link.test.ts
autonomous: true
requirements: [LOJA-03, LOJA-04]
user_setup: []

must_haves:
  truths:
    - "the QR code preview renders on page load for the current vitrine URL (D-11)"
    - "the 'Baixar PNG' button downloads a PNG of the QR (D-09)"
    - "the readonly field shows the full vitrine URL and the 'Copiar' button copies that exact URL, showing the 'Link copiado!' toast (LOJA-04, D-12, D-13)"
  artifacts:
    - "src/lib/qr.ts"
    - "src/lib/clipboard.ts"
    - "src/app/(admin)/configuracoes/qr-code-panel.tsx (real implementation)"
    - "tests/settings/qr-code.test.ts"
    - "tests/settings/copy-link.test.ts"
  key_links:
    - "qr-code-panel receives publicUrl from page.tsx (buildStoreUrl(store.slug))"
    - "copyText writes the exact publicUrl string; generateQrDataUrl encodes the same URL"
---

<objective>
Replace the QR/copy placeholder with the real "Link e QR Code" panel (LOJA-03, LOJA-04, D-09–D-13). It renders a QR preview on page load for the current vitrine URL (D-11), a "Baixar PNG" button that downloads the QR as PNG (D-09, simple QR, no logo — D-10), a readonly field showing the full public URL (D-13), and a "Copiar" button that writes that exact URL to the clipboard and shows the "Link copiado!" toast (LOJA-04, D-12). The QR generation and clipboard writes are extracted into thin, node-testable helpers (`generateQrDataUrl`, `copyText`) so LOJA-03/LOJA-04 have real automated coverage in the node test environment; the visual QR render + real-camera scan is a human check.

Purpose: after this plan a revendedor can copy their vitrine link with one click and download a scannable QR of it.
Output: two helper modules, the real QR/copy panel, and two unit tests.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-RESEARCH.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-PATTERNS.md
@.planning/phases/02-link-compartilh-vel-da-vitrine/02-UI-SPEC.md
@src/lib/slug/store-url.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QR + clipboard helpers with node unit tests (LOJA-03, LOJA-04)</name>
  <read_first>.planning/phases/02-link-compartilh-vel-da-vitrine/02-RESEARCH.md (§Code Examples), src/lib/slug/store-url.ts, tests/onboarding/store-settings.test.ts</read_first>
  <files>src/lib/qr.ts, src/lib/clipboard.ts, tests/settings/qr-code.test.ts, tests/settings/copy-link.test.ts</files>
  <behavior>
    - generateQrDataUrl(buildStoreUrl("minha-loja")) resolves to a string starting with "data:image/png;base64," (LOJA-03)
    - copyText(url) calls navigator.clipboard.writeText with the EXACT url and resolves true on success (LOJA-04)
    - copyText resolves false (no throw) when navigator.clipboard.writeText rejects
  </behavior>
  <action>Create `src/lib/qr.ts` exporting `generateQrDataUrl(url: string): Promise<string>` that calls `QRCode.toDataURL(url, { width: 240, margin: 2 })` (default error-correction level M is sufficient — no logo compositing, D-10). Create `src/lib/clipboard.ts` exporting `copyText(text: string): Promise<boolean>` that awaits `navigator.clipboard.writeText(text)` and returns true, or returns false on rejection — it must NOT import sonner (keep it a pure, toast-free, node-testable boundary). Write `tests/settings/qr-code.test.ts` asserting generateQrDataUrl on `buildStoreUrl("minha-loja")` returns a `data:image/png;base64,` string (qrcode runs in the node env; no canvas mock needed). Write `tests/settings/copy-link.test.ts` that stubs the global `navigator.clipboard.writeText` (via `vi.stubGlobal`) and asserts copyText writes the exact URL and returns true, and returns false when the mock rejects. Author both tests before implementing (RED → GREEN).</action>
  <verify>
    <automated>npx vitest run tests/settings/qr-code.test.ts tests/settings/copy-link.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/settings/qr-code.test.ts` proves a PNG data URL is produced for a built vitrine URL (LOJA-03).
    - `tests/settings/copy-link.test.ts` proves copyText writes the exact URL and returns true, and returns false without throwing on rejection (LOJA-04).
    - `src/lib/clipboard.ts` does not import sonner.
  </acceptance_criteria>
  <done>generateQrDataUrl and copyText exist and are covered by passing node unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Real QR/copy panel UI (D-09–D-13)</name>
  <read_first>src/app/(admin)/configuracoes/qr-code-panel.tsx, .planning/phases/02-link-compartilh-vel-da-vitrine/02-RESEARCH.md (§Code Examples), .planning/phases/02-link-compartilh-vel-da-vitrine/02-UI-SPEC.md</read_first>
  <files>src/app/(admin)/configuracoes/qr-code-panel.tsx</files>
  <action>Rewrite the placeholder `qr-code-panel.tsx` as the real `"use client"` component taking `publicUrl: string`. On mount (useEffect + canvas ref) render the QR into a `<canvas>` via `QRCode.toCanvas(canvasRef, publicUrl, { width: 240, margin: 2 })` for the on-load preview (D-11). Provide a readonly `<input value={publicUrl} readOnly>` showing the full URL (D-13) beside a "Copiar" button (02-UI-SPEC copy) that calls `copyText(publicUrl)` from `@/lib/clipboard` and then `toast.success("Link copiado!")` on true / `toast.error("Não foi possível copiar o link. Selecione e copie manualmente.")` on false (D-12, 02-UI-SPEC). Provide a "Baixar PNG" button that downloads the QR — from the canvas `toDataURL("image/png")` into a temporary `<a download="vitrine-qrcode.png">` click (D-09). Use lucide-react icons for the copy and download buttons; ensure icon-only buttons keep a ≥44×44px touch target (p-3 padding) per 02-UI-SPEC mobile-first exceptions. Use the section heading "Seu QR Code" and the #F5F5F3 preview-panel background per 02-UI-SPEC.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -q "toCanvas" "src/app/(admin)/configuracoes/qr-code-panel.tsx" && grep -q "copyText" "src/app/(admin)/configuracoes/qr-code-panel.tsx"</automated>
    <human-check>Run `npm run dev`, log in, visit /configuracoes → Link e QR Code: the QR preview renders on load; the readonly field shows the full vitrine URL; "Copiar" shows the "Link copiado!" toast and the clipboard holds the exact URL (paste to confirm); "Baixar PNG" downloads vitrine-qrcode.png. Scan the downloaded PNG with a real phone camera and confirm it opens the correct vitrine URL. Confirm all controls are usable and tap targets are comfortable on a mobile viewport.</human-check>
  </verify>
  <acceptance_criteria>
    - QR preview renders on page load; "Baixar PNG" downloads a PNG; "Copiar" copies the exact publicUrl and toasts "Link copiado!".
    - Icon-only buttons have ≥44×44px touch targets (mobile-first).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The panel previews the QR on load, downloads it as PNG, and copies the exact vitrine URL with the success toast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clipboard write / QR encode | The vitrine URL is already public; the only risk is silent failure, not disclosure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Denial of Service (availability/UX) | clipboard write failing silently on unsupported/insecure contexts | low | mitigate | copyText returns false instead of throwing; the panel surfaces an explicit fallback error toast rather than a silent no-op (02-RESEARCH.md Security Domain) |
| T-02-12 | Information Disclosure | QR/URL exposure | low | accept | The vitrine slug URL is public by design (meant to be shared); encoding it in a QR discloses nothing non-public |
</threat_model>

<verification>
- `npx vitest run tests/settings/qr-code.test.ts tests/settings/copy-link.test.ts` is green.
- `npx tsc --noEmit` passes.
- Human check confirms QR preview/download, one-click copy with toast, and a real-camera scan opening the correct URL.
</verification>

<success_criteria>
A revendedor sees a QR preview on load, downloads it as a scannable PNG, and copies the exact vitrine URL with one click and a success toast.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `generateQrDataUrl(url: string): Promise<string>` — `src/lib/qr.ts`
- `copyText(text: string): Promise<boolean>` — `src/lib/clipboard.ts`
- `QrCodePanel` (real) — `src/app/(admin)/configuracoes/qr-code-panel.tsx`
- `tests/settings/qr-code.test.ts`, `tests/settings/copy-link.test.ts`
</artifacts>

<output>
Create `.planning/phases/02-link-compartilh-vel-da-vitrine/02-06-SUMMARY.md` when done.
</output>
