---
phase: 01-funda-o-conta-e-isolamento-multi-tenant
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/(admin)/onboarding/onboarding-wizard.tsx
  - src/app/(admin)/cadastro/page.tsx
  - src/app/(admin)/login/page.tsx
  - src/app/(admin)/dashboard/page.tsx
  - src/app/(admin)/esqueci-senha/page.tsx
  - src/app/(admin)/redefinir-senha/page.tsx
  - tests/ui/dark-mode-contrast.test.ts
autonomous: true
gap_closure: true
requirements: [AUTH-01, AUTH-02, AUTH-05, LOJA-01, WPP-01, WPP-02]

must_haves:
  truths:
    - "Labels, headings e placeholders de todas as páginas do grupo (admin) permanecem legíveis mesmo quando o SO/navegador do usuário está em dark mode"
    - "globals.css força esquema de cor claro e não reintroduz o flip de dark mode do boilerplate create-next-app"
    - "Todo <main> do grupo (admin) declara um fundo claro explícito (bg-white) como defesa em profundidade"
  artifacts:
    - src/app/globals.css
    - tests/ui/dark-mode-contrast.test.ts
  key_links:
    - "Com o bloco @media dark removido, --foreground permanece #171717 (escuro) sobre fundo claro — resolve simultaneamente o texto quase-invisível dos labels (escuro sobre escuro) e o texto quase-invisível dos inputs (claro sobre branco)"
---

<objective>
Corrigir o gap major de contraste (UAT M-4 + achado bônus) reportado no onboarding e presente em TODAS as páginas admin: quando o SO do usuário está em dark mode, o bloco de media query de esquema escuro herdado do boilerplate do create-next-app inverte o fundo para quase-preto, enquanto os labels usam cores hex fixas escuras (`text-[#111111]`, `text-[#0D3D2B]`) — resultando em texto quase invisível. O projeto NÃO tem dark mode (CLAUDE.md confirma fora de escopo).

Purpose: Restaurar legibilidade em todo o painel admin (cadastro, login, dashboard, esqueci-senha, redefinir-senha, onboarding) para qualquer revendedor cujo dispositivo tem preferência de dark mode ativa — um default extremamente comum em macOS/iOS/Windows.

Output: `globals.css` forçando esquema claro (sem flip de dark mode) + `bg-white` explícito em todo `<main>` do admin + teste de regressão estático.

Deferido (nota de backlog, NÃO fazer neste plano): a migração de longo prazo dos valores hex hardcoded (`text-[#111111]` etc.) para tokens `@theme` conectados às variáveis CSS. A diagnose (`.planning/debug/onboarding-invisible-labels.md`) marcou isso como opcional/menor prioridade; com o flip de dark mode neutralizado o bug reportado fica 100% resolvido, e o refactor cosmético amplo em 6 páginas adiciona risco de regressão visual sem benefício funcional. Fica registrado como item de backlog para uma futura fase de polimento de UI.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Diagnose completa do gap (root cause project-wide ja confirmado por leitura de codigo)
@.planning/debug/onboarding-invisible-labels.md

# Fonte a corrigir e pagina de exemplo afetada
@src/app/globals.css
@src/app/(admin)/onboarding/onboarding-wizard.tsx

# Padrao de teste estatico ja usado no projeto (le a fonte e faz assercoes)
@tests/middleware/matcher.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever teste de regressao de contraste (RED)</name>
  <files>tests/ui/dark-mode-contrast.test.ts</files>
  <action>
    Criar o arquivo de teste estatico (mesmo padrao de `tests/middleware/matcher.test.ts`, que le a fonte e faz assercoes — o ambiente do vitest e `node`, nao renderiza React). O teste importa `fs`/`path` do node e le os arquivos de fonte como string via `path.resolve(process.cwd(), rel)` (o vitest roda a partir da raiz do projeto).

    Assercoes obrigatorias:
    1. `src/app/globals.css` contem um esquema de cor claro forcado — casar com a regex `/color-scheme:\s*light/`.
    2. `src/app/globals.css` NAO reintroduz o flip de esquema escuro do boilerplate — a regex `/@media[^{]*prefers-color-scheme:\s*dark/` NAO deve casar no conteudo do arquivo. (Regex estruturada de proposito: exige a combinacao de `@media` com a preferencia escura, entao um mero comentario residual nao gera falso positivo.)
    3. Para CADA uma das 6 paginas admin abaixo, o elemento `<main>` declara fundo claro explicito: extrair o primeiro `<main ... className="...">` via a regex `/<main[^>]*className="([^"]*)"/` e afirmar que a className capturada inclui `bg-white`. Enumerar explicitamente os arquivos (usar `it.each`):
       - `src/app/(admin)/onboarding/onboarding-wizard.tsx`
       - `src/app/(admin)/cadastro/page.tsx`
       - `src/app/(admin)/login/page.tsx`
       - `src/app/(admin)/dashboard/page.tsx`
       - `src/app/(admin)/esqueci-senha/page.tsx`
       - `src/app/(admin)/redefinir-senha/page.tsx`

    Este teste DEVE falhar agora (RED) porque globals.css ainda tem o flip e nenhum `<main>` tem `bg-white`.

    Referencia de root cause: `.planning/debug/onboarding-invisible-labels.md` (defeito project-wide, confirmado por leitura de codigo nas Plans 01-03/01-04/01-05).
  </action>
  <verify>
    <automated>npx vitest run tests/ui/dark-mode-contrast.test.ts 2>&1 | grep -qiE "fail|✗|×" && echo "RED OK (teste falha antes do fix)"</automated>
  </verify>
  <done>O arquivo de teste existe, roda no vitest e falha (RED) porque o fix ainda nao foi aplicado.</done>
</task>

<task type="auto">
  <name>Task 2: Neutralizar o dark mode no globals.css</name>
  <files>src/app/globals.css</files>
  <action>
    Editar `src/app/globals.css` para eliminar o flip de esquema escuro herdado do create-next-app e forcar esquema de cor claro (o projeto nao tem dark mode — CLAUDE.md/PROJECT.md, fora de escopo):

    1. Remover por completo o bloco de media query que sobrescreve `--background`/`--foreground` para a preferencia de esquema escuro (atualmente linhas 15-20). Apos a remocao, `--background` fica `#ffffff` e `--foreground` fica `#171717` em qualquer condicao de SO.
    2. Adicionar `color-scheme: light;` dentro do seletor `:root` (junto as variaveis `--background`/`--foreground`). Isso instrui o navegador a renderizar controles de formulario, scrollbars e defaults de UA sempre em modo claro, mesmo com o SO em dark mode.

    Manter intactos o `@import "tailwindcss";`, o bloco `@theme inline { ... }` e o seletor `body { ... }`. Nao introduzir `tailwind.config.js` (Tailwind v4 e CSS-first). Nao adicionar variantes `dark:` em lugar nenhum.
  </action>
  <verify>
    <automated>node -e "const c=require('node:fs').readFileSync('src/app/globals.css','utf8'); if(!/color-scheme:\s*light/.test(c)) throw new Error('falta color-scheme light'); if(/@media[^{]*prefers-color-scheme:\s*dark/.test(c)) throw new Error('flip de esquema escuro ainda presente'); console.log('globals.css OK');"</automated>
  </verify>
  <done>globals.css forca `color-scheme: light` em `:root` e nao contem mais o bloco de media query de esquema escuro; `--background`/`--foreground` permanecem claros em qualquer SO.</done>
</task>

<task type="auto">
  <name>Task 3: Adicionar bg-white em todo main do admin + fechar o teste (GREEN)</name>
  <files>src/app/(admin)/onboarding/onboarding-wizard.tsx, src/app/(admin)/cadastro/page.tsx, src/app/(admin)/login/page.tsx, src/app/(admin)/dashboard/page.tsx, src/app/(admin)/esqueci-senha/page.tsx, src/app/(admin)/redefinir-senha/page.tsx</files>
  <action>
    Defesa em profundidade: adicionar a classe `bg-white` a lista de classes do elemento `<main>` em cada uma das 6 paginas admin. Editar SOMENTE o atributo `className` do `<main>` (nao mexer no restante do markup). As classes atuais de cada `<main>` (todas comecam com `mx-auto flex min-h-dvh w-full ...`):
      - `src/app/(admin)/onboarding/onboarding-wizard.tsx` (linha ~69, `max-w-md ... justify-center`)
      - `src/app/(admin)/cadastro/page.tsx` (linha ~39, `max-w-sm ... justify-center`)
      - `src/app/(admin)/login/page.tsx` (linha ~37, `max-w-sm ... justify-center`)
      - `src/app/(admin)/dashboard/page.tsx` (linha ~16, `max-w-md ...`)
      - `src/app/(admin)/esqueci-senha/page.tsx` (linha ~44, `max-w-sm ... justify-center`)
      - `src/app/(admin)/redefinir-senha/page.tsx` (linha ~52, `max-w-sm ... justify-center`)

    Inserir `bg-white` no inicio da lista de classes de cada `<main>` (ex.: `className="bg-white mx-auto flex ...`). Nao remover nenhuma classe existente. Nao tocar em labels/inputs (o fix do globals.css ja resolve o texto; `bg-white` no `<main>` e cinto-e-suspensorio para garantir fundo claro mesmo que qualquer variavel CSS mude no futuro).

    Ao final, rodar a suite completa para confirmar GREEN e ausencia de regressao nos testes existentes.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/dark-mode-contrast.test.ts 2>&1 | grep -qiE "fail|✗|×" && (echo "AINDA RED — fix incompleto" && exit 1) || echo "GREEN OK"</automated>
  </verify>
  <done>Todos os 6 `<main>` do admin incluem `bg-white`; `tests/ui/dark-mode-contrast.test.ts` passa (GREEN) e nenhum teste existente quebra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do usuario → CSS/markup renderizado | Preferencia de esquema de cor do SO (`prefers-color-scheme`) e uma entrada controlada pelo ambiente do usuario que altera a renderizacao |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06-01 | Denial of Service (usabilidade) | globals.css / paginas admin | low | mitigate | Forcar `color-scheme: light` + remover o flip de esquema escuro + `bg-white` explicito garante que a preferencia de dark mode do SO nunca torne a UI ilegivel (falha de acesso funcional para o revendedor) |
| T-01-06-02 | Tampering | mudanca CSS/markup apenas, sem novas deps | low | accept | Nenhuma dependencia nova instalada (usa Tailwind v4 ja presente); sem superficie de supply-chain nova. Mudanca puramente de apresentacao, sem novo trust boundary de dados |
</threat_model>

<verification>
- `npx vitest run tests/ui/dark-mode-contrast.test.ts` passa (GREEN): color-scheme claro forcado, sem flip de esquema escuro, 6 mains com `bg-white`.
- `npm test` (suite completa) permanece verde — nenhuma regressao nos testes de auth/RLS/middleware/onboarding existentes.
- Verificacao manual opcional (end-of-phase UAT): abrir `/cadastro`, `/login`, `/onboarding` com o SO em dark mode e confirmar que labels/placeholders ficam legiveis.
</verification>

<success_criteria>
- Gap M-4 fechado: labels e placeholders do wizard de onboarding (e demais paginas admin) legiveis com o SO em dark mode.
- `src/app/globals.css` sem media query de esquema escuro e com `color-scheme: light`.
- Os 6 `<main>` do grupo (admin) com `bg-white` explicito.
- Teste de regressao estatico commitado e verde.
- Migracao hex→@theme deferida como nota de backlog (nao implementada aqui).
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-06-SUMMARY.md` when done
</output>
