---
phase: 01-funda-o-conta-e-isolamento-multi-tenant
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/server.ts
  - tests/supabase/server-cookies.test.ts
autonomous: true
gap_closure: true
requirements: [AUTH-02, AUTH-04]

must_haves:
  truths:
    - "Recarregar qualquer pagina admin (ex: /onboarding) nao lanca a excecao de cookie mesmo quando o token de sessao precisa ser renovado durante a renderizacao de um Server Component"
    - "Em contexto de Server Action / Route Handler (onde escrita de cookie E permitida), setAll continua escrevendo cada cookie normalmente — sem regressao de silent-swallow"
  artifacts:
    - src/lib/supabase/server.ts
    - tests/supabase/server-cookies.test.ts
  key_links:
    - "O try/catch envolve o forEach de cookieStore.set dentro de setAll — a excecao lancada pelo Next.js durante o render de Server Component e engolida com seguranca porque src/middleware.ts (updateSession) ja renova a sessao a cada request; o catch NAO e atingido em contexto de escrita permitida, provado pelo teste do caminho de sucesso"
---

<objective>
Corrigir o gap blocker (UAT teste 6): `createClient()` em `src/lib/supabase/server.ts` — a factory usada em Server Components, Server Actions e Route Handlers — implementa `setAll` sem o try/catch documentado pela orientacao oficial do `@supabase/ssr` para o App Router do Next.js. Quando `getUser()` e chamado a partir de um Server Component (ex: via `requireCompletedOnboarding()` em `onboarding-guard.ts`, invocado por `page.tsx`) e o token de sessao precisa de refresh, o cliente Supabase tenta escrever os cookies renovados via `setAll` -> `cookieStore.set()`. O App Router so permite escrita de cookies dentro de Server Actions e Route Handlers — nunca durante o render de um Server Component — entao a chamada lanca `Cookies can only be modified in a Server Action or Route Handler` (visto no Console ao recarregar /onboarding, apontando para server.ts:23).

Purpose: Recarregar qualquer pagina admin deve ser silencioso mesmo quando o token vence durante o render. A supressao e segura por design: `src/middleware.ts` (via `updateSession`) ja renova a sessao a cada request, entao o refresh redundante disparado por um Server Component pode ser ignorado sem nenhuma regressao de frescor de sessao.

Output: `setAll` envolve o `cookiesToSet.forEach(...)` em um try/catch (catch com comentario explicativo, sem re-throw), seguindo o padrao oficial documentado do `@supabase/ssr`. Teste unitario deterministico provando os dois caminhos: (a) contexto de Server Component (`.set()` lanca) NAO propaga a excecao; (b) contexto de Server Action/Route Handler (`.set()` sucede) ainda escreve todos os cookies corretamente.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Diagnose completo do gap (root cause confirmado no 01-UAT.md, Gaps > teste 6)
@.planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-UAT.md

# Contexto de criacao original da factory server (convencao server vs browser)
@.planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-01-SUMMARY.md

# Fonte a corrigir
@src/lib/supabase/server.ts

# Referencia do padrao correto ja aplicado no helper de middleware (setAll pareado com getUser por request — o refresh real que torna o catch do Server Component redundante)
@src/lib/supabase/middleware.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escrever teste dos dois contextos de setAll (RED)</name>
  <files>tests/supabase/server-cookies.test.ts</files>
  <behavior>
    - Contexto Server Component (escrita proibida): quando o `cookieStore` retornado por `cookies()` tem um `.set()` que lanca (simulando a restricao do Next.js), invocar `setAll([...])` NAO propaga a excecao — `expect(() => setAll(...)).not.toThrow()`.
    - Contexto Server Action / Route Handler (escrita permitida): quando `.set()` sucede, `setAll(cookiesToSet)` chama `.set(name, value, options)` uma vez por cookie, com os argumentos corretos — nenhum cookie e silenciosamente descartado.
  </behavior>
  <action>
    Criar `tests/supabase/server-cookies.test.ts` como teste UNITARIO totalmente mockado (deterministico, sem rede nem Supabase real).

    Estrutura de mocks (usar `vi.hoisted` porque `vi.mock` e hoisted — o holder de `vi.hoisted` e a forma suportada de compartilhar os spies com os factories):
    - Declarar `const { mockCreateServerClient, mockCookies } = vi.hoisted(() => ({ mockCreateServerClient: vi.fn(), mockCookies: vi.fn() }));`
    - `vi.mock("@supabase/ssr", () => ({ createServerClient: mockCreateServerClient }));`
    - `vi.mock("next/headers", () => ({ cookies: mockCookies }));`
    - Importar `createClient` de `@/lib/supabase/server` DEPOIS das chamadas `vi.mock`.

    Helper para capturar a config de cookies: `createClient()` chama `createServerClient(url, key, options)`; o objeto `options.cookies` (com `getAll`/`setAll`) e o terceiro argumento. Escrever `function captureCookies() { return mockCreateServerClient.mock.calls[0][2].cookies; }`.

    `beforeEach`: limpar o registro de chamadas entre os testes com `mockCreateServerClient.mockClear()` e `mockCookies.mockReset()` (cada teste redefine seu proprio `mockResolvedValue` do cookieStore, entao calls[0] sempre se refere a chamada daquele teste).

    Caso 1 — Server Component (`.set()` lanca, NAO deve propagar):
    - `const set = vi.fn(() => { throw new Error("Cookies can only be modified in a Server Action or Route Handler"); });`
    - `mockCookies.mockResolvedValue({ getAll: () => [], set });`
    - `await createClient();`
    - `const { setAll } = captureCookies();`
    - `expect(() => setAll([{ name: "sb-access-token", value: "x", options: { path: "/" } }])).not.toThrow();`

    Caso 2 — Server Action / Route Handler (`.set()` sucede, deve escrever tudo):
    - `const set = vi.fn();`
    - `mockCookies.mockResolvedValue({ getAll: () => [], set });`
    - `await createClient();`
    - `const { setAll } = captureCookies();`
    - montar `const cookiesToSet = [{ name: "sb-access-token", value: "abc", options: { path: "/" } }, { name: "sb-refresh-token", value: "def", options: { path: "/" } }];`
    - `setAll(cookiesToSet);`
    - `expect(set).toHaveBeenCalledTimes(2);` e `expect(set).toHaveBeenCalledWith("sb-access-token", "abc", { path: "/" });` e `expect(set).toHaveBeenCalledWith("sb-refresh-token", "def", { path: "/" });`

    Este teste DEVE falhar agora (RED): o Caso 1 hoje propaga a excecao (server.ts nao tem try/catch), entao `not.toThrow()` falha. O Caso 2 ja passa. A suite do arquivo falha no total.

    Referencia de root cause: `01-UAT.md`, secao Gaps, item do teste 6.
  </action>
  <verify>
    <automated>npx vitest run tests/supabase/server-cookies.test.ts 2>&1 | grep -qiE "fail|✗|×" && echo "RED OK (Caso 1 propaga a excecao antes do fix)"</automated>
  </verify>
  <done>O teste existe, roda no vitest e falha (RED) no Caso 1 porque o fix ainda nao foi aplicado; o Caso 2 (escrita permitida) ja passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Envolver setAll em try/catch em server.ts (GREEN)</name>
  <files>src/lib/supabase/server.ts</files>
  <behavior>
    - `.set()` lanca (Server Component) → `setAll` engole a excecao e retorna normalmente, sem re-throw.
    - `.set()` sucede (Server Action / Route Handler) → `setAll` escreve todos os cookies como antes; o catch nunca e atingido.
  </behavior>
  <action>
    Editar `src/lib/supabase/server.ts`: dentro de `setAll`, envolver a chamada existente `cookiesToSet.forEach(({ name, value, options }) => cookieStore.set(name, value, options))` em um bloco `try { ... } catch { ... }`.

    O bloco `catch` NAO deve re-lancar. Adicionar um comentario em portugues explicando por que ignorar e seguro: a escrita de cookie so e permitida em Server Actions e Route Handlers; quando `setAll` e chamado durante o render de um Server Component (via um refresh de token disparado por `getUser()`), o `cookieStore.set()` lanca — e ignorar e seguro porque `src/middleware.ts` (via `updateSession`) ja renova a sessao a cada request, tornando esse refresh redundante. Este e o padrao oficial documentado do `@supabase/ssr` para o App Router.

    Nao alterar `getAll`, a assinatura de `createClient`, nem o doc-comment de topo do arquivo (a convencao server-vs-browser e a regra `getUser()` permanecem intactas). Nao tocar em `src/lib/supabase/middleware.ts` (o helper de middleware ja lida com cookies via request/response e nao sofre desta restricao).
  </action>
  <verify>
    <automated>npx vitest run tests/supabase/server-cookies.test.ts 2>&1 | grep -qiE "fail|✗|×" && (echo "AINDA RED — fix incompleto" && exit 1) || echo "GREEN OK"</automated>
  </verify>
  <done>`setAll` engole a excecao no contexto de Server Component e ainda escreve todos os cookies no contexto de escrita permitida; `tests/supabase/server-cookies.test.ts` passa (GREEN) e a suite completa (`npm test`) permanece verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render de Server Component → escrita de cookie de sessao | O App Router proibe escrita de cookie durante o render; um refresh de token disparado por `getUser()` tenta cruzar essa fronteira e lanca |
| Server Action / Route Handler → escrita de cookie de sessao | Escrita permitida; o cookie de sessao renovado DEVE ser persistido aqui — nao pode ser silenciosamente descartado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08-01 | Denial of Service | setAll (catch pode mascarar falha de escrita legitima) | low | mitigate | O catch so e atingido quando `cookieStore.set()` lanca — o que, em Server Actions/Route Handlers (contexto de escrita permitida), nao ocorre. O Caso 2 do teste prova que a escrita de cookie continua acontecendo integralmente no contexto permitido; a supressao fica restrita ao render de Server Component, onde `src/middleware.ts` ja garante o refresh real da sessao por request |
| T-01-08-02 | Tampering | superficie de dependencia | low | accept | Nenhuma dependencia nova e instalada — apenas try/catch em codigo existente usando `@supabase/ssr` ja presente. Nenhuma superficie de supply-chain adicionada |
</threat_model>

<verification>
- `npx vitest run tests/supabase/server-cookies.test.ts` passa (GREEN): Caso 1 (Server Component) nao propaga a excecao; Caso 2 (Server Action/Route Handler) escreve os dois cookies com os argumentos corretos.
- `npm test` (suite completa) permanece verde — nenhum teste existente regride.
- Verificacao manual opcional (end-of-phase UAT / reconfirmacao do gap): recarregar /onboarding no navegador com uma sessao ativa e confirmar que o Console nao exibe mais `Cookies can only be modified in a Server Action or Route Handler`.
</verification>

<success_criteria>
- Gap blocker do teste 6 fechado: recarregar paginas admin nao lanca a excecao de cookie durante refresh de token em Server Component.
- Sem regressao de silent-swallow: o contexto de escrita permitida (Server Action/Route Handler) continua persistindo o cookie de sessao renovado, provado por teste.
- Nenhuma dependencia nova; padrao oficial documentado do `@supabase/ssr` aplicado.
- Teste unitario dos dois contextos commitado e verde (ciclo RED -> GREEN).
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-08-SUMMARY.md` when done
</output>
