---
phase: 01-funda-o-conta-e-isolamento-multi-tenant
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth/actions.ts
  - tests/auth/signin-network-error.test.ts
autonomous: true
gap_closure: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "Login sem conexao de rede mostra uma mensagem distinta de erro de rede, nao a mensagem de credenciais invalidas"
    - "Login com credenciais reais invalidas continua mostrando a mensagem generica anti-enumeracao inalterada"
  artifacts:
    - src/lib/auth/actions.ts
    - tests/auth/signin-network-error.test.ts
  key_links:
    - "isAuthRetryableFetchError(error) e checado ANTES do fallback generico dentro de signInAction — preserva 100% o padrao anti-enumeracao para AuthApiError e so diferencia o caso de rede (que nao carrega informacao sobre existencia de conta)"
---

<objective>
Corrigir o gap major (UAT teste 5): `signInAction` em `src/lib/auth/actions.ts` colapsa falhas de rede (`AuthRetryableFetchError`, status 0) e falhas reais de credencial (`AuthApiError`, status 400) na mesma mensagem generica "Email ou senha inválidos". Com o wifi desligado, o usuario ve "Email ou senha inválidos" — enganoso, sugere senha errada quando na verdade a requisicao nunca chegou ao Supabase.

Purpose: Dar feedback honesto de conectividade ao revendedor sem enfraquecer a mitigacao anti-enumeracao (Information Disclosure, T-01-08 do plano original). A distincao e segura: uma falha de rede nao carrega nenhuma informacao sobre a existencia da conta.

Output: `signInAction` checa `isAuthRetryableFetchError(error)` antes do fallback generico e retorna uma mensagem de rede distinta apenas nesse caso; a mensagem generica permanece intacta para toda falha real de credencial. Teste unitario cobrindo os dois caminhos.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Diagnose completa do gap (root cause confirmado por leitura do codigo-fonte do @supabase/auth-js)
@.planning/debug/login-network-error-message.md

# Fonte a corrigir
@src/lib/auth/actions.ts

# Padrao de teste de auth ja usado no projeto (mock de next/headers + next/navigation)
@tests/auth/signup.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escrever teste dos dois caminhos de erro (RED)</name>
  <files>tests/auth/signin-network-error.test.ts</files>
  <behavior>
    - Erro de rede: quando `signInWithPassword` resolve com `{ error: new AuthRetryableFetchError("fetch failed", 0) }`, `signInAction` retorna `{ error: "Não foi possível conectar. Verifique sua internet e tente novamente." }`.
    - Regressao anti-enumeracao: quando `signInWithPassword` resolve com `{ error: new AuthApiError("Invalid login credentials", 400, "invalid_credentials") }`, `signInAction` retorna `{ error: "Email ou senha inválidos" }` (mensagem generica inalterada).
  </behavior>
  <action>
    Criar `tests/auth/signin-network-error.test.ts` como teste UNITARIO totalmente mockado (deterministico, sem rede real — diferente dos testes de integracao em `signup.test.ts` que batem no Supabase remoto). Importar `AuthApiError` e `AuthRetryableFetchError` de `@supabase/supabase-js` (ja re-exportados de `@supabase/auth-js` — verificado; nenhuma dependencia nova).

    Estrutura de mocks:
    - Mockar `next/navigation` com um `redirect` que lanca (mesmo padrao dos outros testes de auth: `throw new Error("NEXT_REDIRECT:" + url)`), para o caso de sucesso nunca ser exercitado aqui.
    - Mockar `@/lib/supabase/server` para que `createClient` retorne um client falso cujo `auth.signInWithPassword` e um `vi.fn()` controlavel. Usar `vi.hoisted(() => ({ signInWithPassword: vi.fn() }))` para declarar o mock e referencia-lo dentro do factory de `vi.mock` (vi.mock e hoisted; o holder de `vi.hoisted` e a forma suportada de compartilhar o spy). O factory deve retornar `{ createClient: async () => ({ auth: { signInWithPassword: <o spy> } }) }`.
    - Importar `signInAction` de `@/lib/auth/actions` DEPOIS das chamadas `vi.mock`.

    Dois casos de teste, cada um montando um `FormData` com email valido (ex.: `revendedor@gmail.com`) e senha nao-vazia (o `signInSchema` exige email valido + senha com min 1 char — ver `src/lib/validation/auth.ts` — entao a validacao passa e a execucao chega ao `signInWithPassword` mockado):
    1. `signInWithPassword.mockResolvedValue({ error: new AuthRetryableFetchError("fetch failed", 0) })` → esperar `{ error: "Não foi possível conectar. Verifique sua internet e tente novamente." }`.
    2. `signInWithPassword.mockResolvedValue({ error: new AuthApiError("Invalid login credentials", 400, "invalid_credentials") })` → esperar `{ error: "Email ou senha inválidos" }`.

    Este teste DEVE falhar agora (RED): o caso 1 recebera "Email ou senha inválidos" (o codigo atual colapsa tudo) em vez da mensagem de rede.

    Referencia de root cause: `.planning/debug/login-network-error-message.md`.
  </action>
  <verify>
    <automated>npx vitest run tests/auth/signin-network-error.test.ts 2>&1 | grep -qiE "fail|✗|×" && echo "RED OK (teste de rede falha antes do fix)"</automated>
  </verify>
  <done>O teste existe, roda no vitest e falha (RED) no caso de rede porque o fix ainda nao foi aplicado; o caso anti-enumeracao ja passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Diferenciar erro de rede em signInAction (GREEN)</name>
  <files>src/lib/auth/actions.ts</files>
  <behavior>
    - `isAuthRetryableFetchError(error)` verdadeiro → retorna a mensagem de rede distinta.
    - Qualquer outro `error` truthy (inclui `AuthApiError`) → mantem a mensagem generica "Email ou senha inválidos".
  </behavior>
  <action>
    Editar `src/lib/auth/actions.ts`:
    1. Adicionar o import no topo do arquivo: `import { isAuthRetryableFetchError } from "@supabase/supabase-js";` (helper de type-narrowing oficial, ja disponivel via a dependencia existente — sem nova instalacao).
    2. Dentro de `signInAction`, no bloco `if (error) { ... }` que hoje retorna sempre a mensagem generica: checar `isAuthRetryableFetchError(error)` PRIMEIRO e, se verdadeiro, retornar `{ error: "Não foi possível conectar. Verifique sua internet e tente novamente." }`. Manter, logo abaixo, o `return { error: 'Email ou senha inválidos' };` existente INALTERADO como fallback para todos os demais erros (credencial real invalida, rate limiting de auth, etc.).

    Nao alterar `signUpAction`, `signOutAction`, `generateStoreSlug` nem a validacao zod. Nao afrouxar a mensagem generica: ela deve permanecer identica byte-a-byte para o caso `AuthApiError` (preserva a mitigacao anti-enumeracao T-01-08 — falha de rede nao vaza existencia de conta, entao diferencia-la e seguro). Atualizar o doc-comment de `signInAction` para registrar a excecao de rede (a mensagem generica cobre falhas de credencial; falhas de rede recebem mensagem propria).
  </action>
  <verify>
    <automated>npx vitest run tests/auth/signin-network-error.test.ts 2>&1 | grep -qiE "fail|✗|×" && (echo "AINDA RED — fix incompleto" && exit 1) || echo "GREEN OK"</automated>
  </verify>
  <done>`signInAction` retorna a mensagem de rede para `AuthRetryableFetchError` e a mensagem generica para `AuthApiError`; `tests/auth/signin-network-error.test.ts` passa (GREEN) e a suite completa (`npm test`) permanece verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente nao autenticado → Server Action de login | Entrada nao confiavel (email/senha) cruza para o servidor; a mensagem de erro retornada e observavel por um atacante |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07-01 | Information Disclosure | signInAction (mensagem de erro) | high | mitigate | A mensagem generica "Email ou senha inválidos" permanece byte-a-byte identica para toda falha `AuthApiError` — nao enumera existencia de conta. Apenas `AuthRetryableFetchError` (falha de rede, sem informacao sobre a conta) recebe mensagem distinta; teste de regressao garante que credencial invalida nao vaza |
| T-01-07-02 | Tampering | import de helper do supabase-js | low | accept | `isAuthRetryableFetchError` vem do `@supabase/supabase-js` ja instalado (re-export de `@supabase/auth-js`) — nenhuma dependencia nova, nenhuma superficie de supply-chain adicionada |
</threat_model>

<verification>
- `npx vitest run tests/auth/signin-network-error.test.ts` passa (GREEN): caso de rede retorna mensagem de conexao; caso de credencial invalida retorna a mensagem generica.
- `npm test` (suite completa) permanece verde — os testes de integracao existentes de `signInAction` (em `tests/auth/signup.test.ts`) continuam recebendo a mensagem generica para credencial invalida.
- Verificacao manual opcional (end-of-phase UAT): desligar o wifi, tentar logar em `/login`, confirmar a mensagem de conexao em vez de "Email ou senha inválidos".
</verification>

<success_criteria>
- Gap do teste 5 fechado: login offline mostra mensagem de rede honesta.
- Padrao anti-enumeracao preservado: `AuthApiError` continua com a mensagem generica inalterada.
- Nenhuma dependencia nova; import via `@supabase/supabase-js` existente.
- Teste unitario dos dois caminhos commitado e verde.
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-conta-e-isolamento-multi-tenant/01-07-SUMMARY.md` when done
</output>
