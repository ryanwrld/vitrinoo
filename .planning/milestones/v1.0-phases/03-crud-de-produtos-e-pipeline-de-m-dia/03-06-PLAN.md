---
phase: 03-crud-de-produtos-e-pipeline-de-m-dia
plan: 06
type: execute
wave: 6
depends_on: [03-02, 03-04, 03-05]
files_modified:
  - src/lib/products/list.ts
  - src/app/(admin)/produtos/page.tsx
  - src/app/(admin)/produtos/product-list.tsx
  - src/app/(admin)/produtos/product-toolbar.tsx
  - tests/products/list-filter-sort.test.ts
autonomous: false
requirements: [PROD-06, PROD-07]
must_haves:
  truths:
    - "O revendedor busca produtos por nome, filtra por status/marca/solado e ordena por mais recente/nome/preço no painel"
    - "Os filtros/busca/ordenação vivem em searchParams da URL (abrir a URL filtrada reproduz a mesma visualização)"
    - "Cada linha mostra a disponibilidade derivada (EXISTS sobre product_sizes): Disponível ou Esgotado"
    - "Empty states distinguem 'nenhum produto ainda' de 'nenhum resultado para o filtro'"
  artifacts:
    - "src/lib/products/list.ts (queryProducts testável)"
    - "src/app/(admin)/produtos/page.tsx (lê searchParams)"
    - "src/app/(admin)/produtos/product-toolbar.tsx"
    - "tests/products/list-filter-sort.test.ts"
  key_links:
    - "product-toolbar (useDebouncedValue) -> router.push(searchParams) -> page.tsx -> queryProducts"
    - "queryProducts -> products select filtrado/ordenado + rollup de disponibilidade via product_sizes"
---

<objective>
Fechar a fase com a fatia de organização do painel: o revendedor busca por nome, filtra por status/marca/solado e ordena por mais recente/nome/preço, com o estado dos filtros persistido na URL (searchParams). A lista mostra a disponibilidade derivada de cada produto e distingue os dois empty states.

Purpose: PROD-06 — sem busca/filtro/ordenação, um catálogo com dezenas de modelos vira uma parede impossível de gerenciar. O padrão searchParams no Server Component (03-RESEARCH.md Pattern 3) é a mesma filosofia que a Fase 4 vai aplicar à vitrine pública (filtros em query params), aqui aplicada ao painel, reusando o `useDebouncedValue` já existente. Este plano também inclui o checkpoint humano do fluxo completo de CRUD (UI hint: yes) em dispositivo móvel.
Output: painel de produtos buscável/filtrável/ordenável, com verificação humana do fluxo completo.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-PATTERNS.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-05-SUMMARY.md
@src/lib/hooks/use-debounce.ts
@src/app/(admin)/produtos/page.tsx
@src/app/(admin)/produtos/product-list.tsx
@src/lib/products/constants.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teste de integração (RED) — busca, filtro e ordenação</name>
  <files>tests/products/list-filter-sort.test.ts</files>
  <read_first>
    - tests/products/create-product.test.ts (helpers de seed)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Pattern 3 searchParams query)
  </read_first>
  <behavior>
    - queryProducts(storeId, { q }) filtra por nome (ilike, case-insensitive, parcial)
    - queryProducts com { status } / { brand } / { sole } filtra pelos respectivos campos
    - queryProducts com { sort: 'nome' | 'preco' | 'recente' } ordena corretamente (nome asc, preço asc, recente = created_at desc)
    - o resultado inclui a disponibilidade derivada por produto (true se existe algum product_sizes.available=true)
    - queryProducts só retorna produtos da própria loja (nunca de outra)
  </behavior>
  <action>
    Criar `tests/products/list-filter-sort.test.ts` reusando os helpers de seed. Seedar uma loja com 3-4 produtos de marcas/solados/status/preços/nomes distintos e alguns com tamanhos disponíveis e outros esgotados. Chamar a função server-side `queryProducts` (Task 2) diretamente com combinações de params e afirmar o conjunto e a ordem retornados, além do flag de disponibilidade derivada. Incluir um caso de isolamento (produtos de outra loja não aparecem). Começa VERMELHO (queryProducts não existe).
  </action>
  <verify>
    <automated>npx vitest run tests/products/list-filter-sort.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Após a Task 2, `npx vitest run tests/products/list-filter-sort.test.ts` passa: busca por nome, filtros status/marca/solado, ordenação recente/nome/preço, disponibilidade derivada e isolamento.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: queryProducts (server-side testável) + page.tsx lê searchParams</name>
  <files>src/lib/products/list.ts, src/app/(admin)/produtos/page.tsx</files>
  <read_first>
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Pattern 3 código do Server Component + sort map)
    - src/app/(admin)/produtos/page.tsx (versão base do Plan 03-02 a ser refatorada)
  </read_first>
  <action>
    Criar `src/lib/products/list.ts` com `queryProducts(supabase, storeId, params)` onde params = { q?, status?, brand?, sole?, sort? }. Montar a query: `from("products").select(...)` filtrando por store_id; se q, `.ilike("name", "%"+q+"%")`; se status/brand/sole, `.eq(...)` respectivo; ordenar por um mapa { recente: created_at desc, nome: name asc, preco: price asc } com fallback 'recente'. Trazer a disponibilidade derivada por produto — via um select relacionado a product_sizes (ex.: `product_sizes(available)`) e computar em JS o flag disponivel = existe algum available=true, OU uma segunda query agregada; escolher a abordagem e documentar. Retornar a lista já com { ...produto, disponivel, coverPath }. Extrair como função pura server-side justamente para ser testável fora do Server Component (chamada direta no teste da Task 1).

    Refatorar `page.tsx` para: `searchParams: Promise<{...}>`; await searchParams; requireCompletedOnboarding; resolver a store; chamar queryProducts. Passar os produtos + os params atuais para `<ProductToolbar />` (Task 3) e `<ProductList />`. Continuar sem "use cache". Exibir o resumo "{N} produtos" (singular "1 produto"). Escolher os dois empty states (nenhum produto vs nenhum resultado) com base em: existem produtos na loja mas o filtro não achou nada -> "Nenhum produto encontrado"; nenhum produto na loja -> "Nenhum produto cadastrado ainda" (03-UI-SPEC.md Copywriting).
  </action>
  <verify>
    <automated>npx vitest run tests/products/list-filter-sort.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `tests/products/list-filter-sort.test.ts` passa; `npx tsc --noEmit` passa. page.tsx lê searchParams e chama queryProducts; a lista reflete filtros/ordenação; disponibilidade derivada por produto; resumo de contagem correto.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Toolbar de busca/filtro/ordenação + rollup de disponibilidade na lista</name>
  <files>src/app/(admin)/produtos/product-toolbar.tsx, src/app/(admin)/produtos/product-list.tsx</files>
  <read_first>
    - src/lib/hooks/use-debounce.ts (useDebouncedValue — reusar sem reimplementar)
    - src/app/(admin)/configuracoes/slug-editor.tsx (padrão de input controlado + debounce)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md (§Product list page toolbar; §Copywriting filtros/sort; tokens; mobile flex-wrap)
    - src/lib/products/constants.ts (BRANDS, SOLES para os selects de filtro)
  </read_first>
  <action>
    Criar `product-toolbar.tsx` (client component): input de busca com ícone Search (placeholder "Buscar por nome…"), controlado localmente e passado por `useDebouncedValue` (já existe — NÃO escrever novo hook) antes de refletir em `router.push` atualizando o query param `q`; selects de filtro status ("Todos"/"Publicado"/"Rascunho"), marca ("Todas as marcas" + BRANDS), solado ("Todos os solados" + SOLES); dropdown de ordenação ("Mais recente"/"Nome"/"Preço"). Cada mudança faz router.push preservando os demais params (construir a partir do searchParams atual, recebido por prop). No mobile, empilhar em flex-wrap gap-2 — nunca uma tira de scroll horizontal que esconde controles. Botões/inputs com touch target adequado.

    Estender `product-list.tsx`: exibir em cada linha o indicador de disponibilidade derivada (o flag `disponivel` vindo de queryProducts) — "Disponível" (dot verde + texto) ou "Esgotado" (dot cinza + texto, sem strikethrough no nível de rollup); manter thumbnail da capa (coverPath -> URL pública via next/image; ImageOff quando sem foto), nome/marca/preço e badge de status, além dos botões Pencil/Trash2 do Plan 03-05. Renderizar os dois empty states distintos conforme decidido na Task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc --noEmit` e `npm run lint` passam. A toolbar filtra/busca/ordena atualizando a URL; abrir a URL filtrada reproduz a visualização; a busca é debounced; a lista mostra disponibilidade derivada e a capa; os dois empty states aparecem nos casos corretos.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [CHECKPOINT] Verificação humana do fluxo completo de CRUD no mobile</name>
  <action>Verificação humana bloqueante que encerra a fase — a automação (todos os plans 03-01..03-06) já entregou o CRUD; o executor NÃO codifica nada nesta task. Apresentar os passos de &lt;how-to-verify&gt; ao usuário em um dispositivo móvel real (caso de teste Nike/Mercurial/FG) e aguardar o sinal de retomada. Antes de apresentar, garantir que `npm test` (suíte completa) está verde.</action>
  <what-built>Fluxo completo de CRUD de produtos no painel: cadastrar (campos + tamanhos + fotos), listar com busca/filtro/ordenação, editar, publicar/despublicar, excluir — tudo com toast, mobile-first.</what-built>
  <how-to-verify>
    1. Em um DISPOSITIVO MÓVEL, cadastrar um produto real usando o caso de teste Nike / Mercurial / FG: nome, marca Nike, linha Mercurial, solado FG, categoria Chuteira, modalidade, preço em BRL (ex.: 899,90), tamanhos e ao menos 1 foto — salvar e confirmar o toast "Produto salvo!" e o produto na lista.
    2. Marcar alguns tamanhos como disponível e outros esgotado; usar "Marcar tudo como esgotado" e confirmar o toast.
    3. Publicar o produto e confirmar o badge "Publicado" + toast; voltar para rascunho e confirmar.
    4. Na lista: buscar por "Mercurial", filtrar por marca Nike, por solado FG, por status Publicado; ordenar por Preço e por Nome — confirmar que a URL muda e que reabrir a URL reproduz a visualização.
    5. Editar o produto (trocar preço/foto) e salvar — confirmar persistência.
    6. Excluir o produto pelo Trash2 -> "Sim, excluir" — confirmar o toast "Produto excluído.", o sumiço da lista, e (se possível) que as fotos saíram do bucket.
    7. Verificar o empty state correto quando não há produtos vs quando o filtro não acha nada.
    8. Confirmar que tudo funciona sem layout quebrado no mobile.
  </how-to-verify>
  <resume-signal>Digite "aprovado" se o fluxo completo (cadastrar/tamanhos/fotos/publicar/buscar/filtrar/ordenar/editar/excluir) funcionou no mobile com toasts corretos; senão descreva o que quebrou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (toolbar, searchParams) -> Server Component (queryProducts) | Parâmetros de filtro vindos da URL; a query é sempre escopada por store_id resolvido do dono, os params só restringem dentro da própria loja |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Information Disclosure | manipular searchParams para tentar ver produtos de outra loja | high | mitigate | queryProducts sempre aplica .eq("store_id", storeId) do dono resolvido por auth; RLS de products é a rede final; isolamento testado (Task 1) |
| T-03-14 | Denial of Service | busca `ilike` com input arbitrário | low | accept | consulta parametrizada pelo cliente Supabase (sem concatenação de SQL); volume por loja é pequeno; sem mitigação adicional necessária |
</threat_model>

<verification>
- `npx vitest run tests/products/list-filter-sort.test.ts` verde
- `npx tsc --noEmit` e `npm run lint` passam
- Checkpoint humano: fluxo completo de CRUD no mobile com toasts e filtros por URL
- Suíte completa `npm test` verde antes de encerrar a fase
</verification>

<success_criteria>
- Buscar por nome, filtrar por status/marca/solado, ordenar por recente/nome/preço (PROD-06)
- Filtros persistidos em searchParams (URL compartilhável reproduz a visão)
- Disponibilidade derivada por produto na lista; dois empty states distintos
- Fluxo completo de CRUD validado por humano no mobile
</success_criteria>

<output>
Create `.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-06-SUMMARY.md` when done
</output>
