---
phase: 03-crud-de-produtos-e-pipeline-de-m-dia
plan: 04
type: execute
wave: 4
depends_on: [03-02]
files_modified:
  - package.json
  - package-lock.json
  - src/app/(admin)/produtos/photo-uploader.tsx
  - src/app/(admin)/produtos/product-form.tsx
  - src/lib/products/actions.ts
  - tests/products/photo-upload.test.ts
autonomous: false
requirements: [PROD-03, PROD-07]
must_haves:
  truths:
    - "O revendedor faz upload de até 5 fotos por produto; a 6ª é rejeitada com mensagem amigável"
    - "Cada foto é comprimida no cliente (Web Worker) com orientação EXIF corrigida antes de sair do celular"
    - "O servidor revalida magic bytes + limite de 5MB por foto e reconta existentes+novas antes de aceitar (defesa em profundidade)"
    - "A foto na posição 1 é a capa (D-11); reordenar por drag-and-drop (mouse/touch/teclado) atualiza só a coluna position (D-12); remover uma foto individual esvazia só aquele slot (D-13)"
  artifacts:
    - "src/app/(admin)/produtos/photo-uploader.tsx"
    - "src/lib/products/actions.ts (validatePhotoFile, upload loop, updatePhotoOrder, removePhoto)"
    - "tests/products/photo-upload.test.ts"
  key_links:
    - "photo-uploader (browser-image-compression) -> FormData photos[] -> saveProduct/addProductPhotos -> bucket product-images -> product_photos rows"
    - "updatePhotoOrder -> UPDATE product_photos.position (nunca renomeia arquivo no bucket)"
    - "removePhoto -> storage.remove([path]) + delete product_photos row"
---

<objective>
Entregar a fatia de mídia: o revendedor sobe até 5 fotos por produto, comprimidas automaticamente no cliente (com orientação EXIF correta), com feedback de progresso, limite rígido de 5MB por imagem, capa = posição 1 (D-11), reordenação por drag-and-drop touch-friendly (D-12) e remoção individual (D-13). O servidor valida magic bytes + tamanho + recontagem como defesa em profundidade.

Purpose: a foto é parte do valor central (o cliente final dispara o pedido no WhatsApp já com a foto). Upload rápido e confiável é alerta crítico #3 do PROJECT.md; o limite de 5MB com aviso antes do erro rígido é o bug #6. A compressão client-side (browser-image-compression, já pré-aprovada) evita subir arquivos brutos de câmera; o dnd-kit resolve reordenação acessível a toque (mandato mobile-first). Persistir ordem via coluna `position` (nunca renomear blob) é o que torna reordenar barato.
Output: pipeline de upload multi-arquivo com compressão, reordenação e remoção, validado no servidor.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-PATTERNS.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-02-SUMMARY.md
@src/lib/settings/actions.ts
@src/lib/products/actions.ts
@src/app/(admin)/produtos/product-form.tsx
@next.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teste de integração (RED) — validação e persistência de fotos</name>
  <files>tests/products/photo-upload.test.ts</files>
  <read_first>
    - tests/products/create-product.test.ts (helper de seed + asserções)
    - src/lib/settings/actions.ts (validateLogoFile — magic bytes, mesma forma de validatePhotoFile)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Pattern 2 upload; §Pitfall 6 limite de 5; §Pitfall 1 cleanup)
  </read_first>
  <behavior>
    - Upload de 3 fotos válidas (PNG/JPEG bytes reais) persiste 3 linhas em product_photos com position 0,1,2 e storage_path no formato {owner_id}/{product_id}/{uuid}.{ext}
    - Tentar subir uma 6ª foto (5 já existentes + 1) é rejeitado com { error } e NÃO insere (Pitfall 6 — recontagem server-side)
    - Um arquivo com magic bytes que não batem com o content-type é rejeitado (arquivo inválido)
    - Um arquivo > 5MB é rejeitado com mensagem de 5MB
    - updatePhotoOrder([{id,position}...]) atualiza as posições; cross-tenant afeta 0 linhas
    - removePhoto(photoId) remove a linha de product_photos (o cleanup de storage é best-effort, asserir a remoção da linha)
  </behavior>
  <action>
    Criar `tests/products/photo-upload.test.ts` reusando o helper de seed. Construir File/Blob de teste com bytes de assinatura reais (PNG: 0x89 0x50 0x4E 0x47; JPEG: 0xFF 0xD8 0xFF) para passar/falhar a checagem de magic bytes. Chamar as Server Actions reais (saveProduct com photos, ou uma action dedicada addProductPhotos — decidir na Task 2 e manter consistente). Cobrir: persistência de posições, rejeição da 6ª foto (recontagem), rejeição de magic bytes inválidos, rejeição de >5MB, updatePhotoOrder (incl. cross-tenant), removePhoto. As fotos aqui são pequenas (bytes sintéticos) — a compressão client-side NÃO roda no servidor/teste; o teste exercita a fronteira do servidor. Começa VERMELHO.
  </action>
  <verify>
    <automated>npx vitest run tests/products/photo-upload.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Após a Task 2, `npx vitest run tests/products/photo-upload.test.ts` passa: posições corretas, 6ª rejeitada, magic bytes e 5MB rejeitados, reorder e remove funcionam, cross-tenant isolado.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Server Actions de foto — validação, upload, reorder, remove</name>
  <files>src/lib/products/actions.ts</files>
  <read_first>
    - src/lib/settings/actions.ts (validateLogoFile linhas 12-54 — copiar como validatePhotoFile; logoExtension; upload para storage; getPublicUrl)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Pattern 2 loop de FormData.getAll; §Pitfall 1 cleanup no delete; §Pitfall 6 recontagem)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-PATTERNS.md (§Magic-byte + size file validation; §actions.ts)
  </read_first>
  <action>
    Em `src/lib/products/actions.ts`, adicionar `validatePhotoFile(file: File)` com a mesma forma de validateLogoFile (tabela de magic bytes PNG/JPEG/WebP, limite MAX 5*1024*1024, ordem type -> size -> assinatura), mensagens PT da 03-UI-SPEC.md §Copywriting Contract ("Essa foto passou do limite de 5MB.", "Envie apenas fotos em JPG, PNG ou WEBP."). Adicionar `photoExtension(contentType)` análogo a logoExtension.

    Estender o pipeline de upload: as fotos comprimidas chegam via `formData.getAll("photos")` (File[]). Antes de qualquer upload, RECONTAR product_photos existentes daquele produto + as novas recebidas e rejeitar se > 5 com "Você já atingiu o limite de 5 fotos por produto." (Pitfall 6). Para cada foto: validatePhotoFile; gerar path `${owned.userId}/${productId}/${crypto.randomUUID()}.${photoExtension(type)}`; upload para o bucket "product-images" (contentType); em erro de upload retornar mensagem amigável; inserir linha em product_photos (product_id, storage_path, position = índice sequencial contínuo às existentes). Decidir e documentar: fotos entram tanto no fluxo de saveProduct (criação) quanto numa action dedicada `addProductPhotos(productId, formData)` para a edição — implementar a que os testes da Task 1 assumem, mantendo a lógica de validação/recontagem compartilhada.

    Adicionar `updatePhotoOrder(order: {id: string; position: number}[]): Promise<ProductActionResult>`: getOwnedStore(); atualizar a coluna position de cada product_photos.id (nunca renomear/mover arquivo no bucket — a coluna position é exatamente para isso); RLS garante escopo por dono. Como (product_id, position) é UNIQUE, aplicar as atualizações de forma que não colidam (ex.: usar um passo intermediário/offset ou update em ordem segura) — documentar a estratégia escolhida.

    Adicionar `removePhoto(photoId: string): Promise<ProductActionResult>`: getOwnedStore(); buscar o storage_path da linha; `supabase.storage.from("product-images").remove([path])` (best-effort); delete da linha product_photos; retornar success/error.

    Adicionar `deleteProductPhotosStorage`-style helper reutilizável para o Plan 03-05 usar no delete do produto (buscar todos os storage_path e remover do bucket — Pitfall 1). Exportar o helper ou deixar a lógica pronta para reuso.
  </action>
  <verify>
    <automated>npx vitest run tests/products/photo-upload.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `tests/products/photo-upload.test.ts` passa; `npx tsc --noEmit` passa. Validação de magic bytes + 5MB + recontagem de 5 no servidor; upload gera path com UUID (nunca nome original); updatePhotoOrder só altera position; removePhoto limpa storage + linha.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: photo-uploader.tsx — compressão client, 5 slots, drag-and-drop, remover, capa</name>
  <files>package.json, package-lock.json, src/app/(admin)/produtos/photo-uploader.tsx, src/app/(admin)/produtos/product-form.tsx</files>
  <read_first>
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Standard Stack browser-image-compression + @dnd-kit; §Pattern 2 compressProductPhoto + handleDragEnd; §Pitfall 4 EXIF dupla rotação)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md (§Photo uploader — 5 slots, empty/filled/uploading, capa badge, drag handle, remover ×, touch targets)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-PATTERNS.md (§photo-uploader.tsx — file input, useTransition status, StatusPill)
    - src/app/(admin)/configuracoes/slug-editor.tsx (useTransition + toast; StatusPill lucide)
    - next.config.ts (images.remotePatterns já cobre o host público do Supabase Storage)
  </read_first>
  <action>
    Instalar as libs pré-aprovadas (03-RESEARCH.md §Package Legitimacy Audit — todas OK/Approved, sem postinstall, sem [SUS]): `npm install browser-image-compression @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`. Sem checkpoint humano de legitimidade (nenhum [SUS]).

    Criar `photo-uploader.tsx` (client component): grade de 5 slots quadrados (grid-cols-5 gap-2 mobile, aspect-square, rounded-lg). Slot vazio: borda tracejada #F5F5F3, ícone Plus + caption "Adicionar foto"; toque abre file picker (accept image/png,image/jpeg,image/webp, multiple). Ao selecionar arquivos: comprimir CADA File via `imageCompression(file, { maxSizeMB: 1, maxWidthOrHeight: 1600, useWebWorker: true })` — a correção de orientação EXIF é automática e NÃO deve ser refeita em nenhuma outra camada (Pitfall 4, A1); limitar a 5 no total (a 6ª mostra o erro "Você já atingiu o limite de 5 fotos por produto."). Slot preenchido: thumbnail (next/image ou <img> de object URL/URL pública), badge "Capa" (pill #00C46A branco text-xs) SOMENTE na posição 1 (D-11, apenas informativo), drag handle GripVertical (top-left, área de toque p-2 generosa — único affordance de reordenar no touch), botão remover "×" (top-right, p-2, ícone #FF4D4D em círculo branco) que esvazia só aquele slot e aceita novo upload imediatamente (D-13). Estado "Enviando…": overlay escuro semi-transparente + Loader2 + caption; um único estado unificado cobre compressão + upload (não mostrar dois progressos). Reordenar via @dnd-kit: DndContext + SortableContext (horizontalListSortingStrategy) sobre os slots preenchidos; onDragEnd usa arrayMove e, se as fotos já estiverem salvas (modo edição), persiste via updatePhotoOrder dentro de startTransition (otimista, toast.error só em falha — reorder é silencioso no sucesso, D-12). Slots vazios não são drop targets. Garantir 44x44px mínimo em todo controle tocável. Respeitar Pitfall 5 se a lista de fotos estiver sob useFieldArray (usar move(), nunca array paralelo).

    Integrar `<PhotoUploader />` na seção 5 (Fotos) do product-form.tsx (o placeholder do Plan 03-02). No modo criação, as fotos comprimidas vão no FormData (append "photos" em loop) e são enviadas com saveProduct; no modo edição, upload/remoção/reorder chamam as actions dedicadas. A legenda "Até 5 fotos. A primeira é a capa da sua vitrine." acima da grade.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    browser-image-compression e @dnd-kit/* em package.json; `npx tsc --noEmit` e `npm run lint` passam. A grade de 5 slots renderiza; selecionar imagem comprime e mostra "Enviando…"; badge "Capa" só na posição 1; drag handle reordena; "×" remove um slot; limite de 5 respeitado na UI.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [CHECKPOINT] Verificação humana do pipeline de fotos no mobile</name>
  <action>Verificação humana bloqueante — a automação (Tasks 1-3) já entregou o pipeline; o executor NÃO codifica nada nesta task. Apresentar os passos de &lt;how-to-verify&gt; ao usuário em um dispositivo móvel real e aguardar o sinal de retomada. EXIF, drag-and-drop por toque e limite de 5 não são cobríveis por teste automatizado headless — por isso este gate.</action>
  <what-built>Pipeline completo de fotos de produto: upload multi-arquivo com compressão client-side (browser-image-compression), até 5 slots, reordenação drag-and-drop (@dnd-kit), remoção individual e capa na posição 1 — no formulário /produtos/novo e /produtos/[id]/editar.</what-built>
  <how-to-verify>
    1. Abrir /produtos/novo em um DISPOSITIVO MÓVEL real (ou emulação de toque) — mandato mobile-first.
    2. Subir uma foto tirada com a câmera do celular em orientação retrato: confirmar que aparece na orientação CORRETA (não deitada/de cabeça para baixo) — valida a correção EXIF (Pitfall 4/A1).
    3. Subir 5 fotos e tentar uma 6ª: confirmar a mensagem "Você já atingiu o limite de 5 fotos por produto.".
    4. Tentar subir um arquivo > 5MB: confirmar "Essa foto passou do limite de 5MB." (a compressão deve normalmente evitar isso; testar um arquivo que não seja imagem para ver "Envie apenas fotos em JPG, PNG ou WEBP.").
    5. Arrastar a 3ª foto para a 1ª posição POR TOQUE: confirmar que reordena e que o badge "Capa" segue a posição 1.
    6. Remover uma foto pelo "×": confirmar que só aquele slot esvazia e aceita novo upload.
    7. Salvar o produto e reabrir na edição: confirmar que as fotos e a ordem persistiram.
  </how-to-verify>
  <resume-signal>Digite "aprovado" se orientação EXIF, limite de 5, drag-and-drop por toque, remoção e persistência de ordem funcionaram no mobile; senão descreva o que quebrou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (photo-uploader) -> Server Action | Arquivos vindos do dispositivo; o servidor nunca confia no file.type do browser nem no limite de 5 aplicado só na UI |
| Server Action -> Supabase Storage (bucket product-images) | Upload escopado por path {owner_id}/... e policies de storage owner-scoped (Plan 03-01) |
| npm install -> árvore de dependências | browser-image-compression + @dnd-kit entram na supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering / Elevation of Privilege | upload de arquivo malicioso disfarçado de imagem (extensão/mimetype falsos) | high | mitigate | validatePhotoFile checa magic bytes no servidor (Task 2), nunca só file.type; path com UUID, nunca nome original |
| T-03-03 | Tampering | bypass do limite de 5 fotos via chamada direta ao Server Action | medium | mitigate | recontagem server-side de existentes+novas antes de aceitar (Pitfall 6, Task 2) |
| T-03-10 | Information Disclosure / Tampering | upload/reorder/remove de foto de produto de outra loja | high | mitigate | getOwnedStore() + policies de storage (foldername[1]=auth.uid) + RLS de product_photos; cross-tenant testado (Task 1) |
| T-03-SC | Tampering | npm install browser-image-compression/@dnd-kit (supply chain) | high | mitigate | Package Legitimacy Audit concluído em 03-RESEARCH.md: todos OK/Approved, sem postinstall, milhões de downloads/semana, nenhum [SUS] — install sem checkpoint humano |
</threat_model>

<verification>
- `npx vitest run tests/products/photo-upload.test.ts` verde (validação, recontagem, reorder, remove, cross-tenant)
- `npx tsc --noEmit` e `npm run lint` passam
- Checkpoint humano: EXIF correto, limite de 5, drag-and-drop touch, remoção, persistência de ordem em dispositivo móvel
</verification>

<success_criteria>
- Até 5 fotos, compressão client-side + EXIF correta, limite rígido de 5MB, feedback de progresso (PROD-03)
- Capa = posição 1 (D-11), reorder drag-and-drop touch (D-12), remoção individual (D-13)
- Defesa em profundidade no servidor: magic bytes + 5MB + recontagem de 5
- Toast em cada ação de foto (PROD-07)
</success_criteria>

<output>
Create `.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-04-SUMMARY.md` when done
</output>
