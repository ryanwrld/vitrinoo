---
phase: 03-crud-de-produtos-e-pipeline-de-m-dia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0003_products_schema_rls.sql
  - src/lib/database.types.ts
  - tests/rls/product-isolation.test.ts
autonomous: false
requirements: [PROD-01, PROD-02]
must_haves:
  truths:
    - "As tabelas products, product_sizes e product_photos existem no Postgres com RLS habilitada"
    - "Um revendedor autenticado nunca lê nem escreve produtos/tamanhos/fotos de outra loja (isolamento por store_id -> owner_id)"
    - "O bucket product-images existe e é público (leitura por URL sem auth para a Fase 4)"
    - "src/lib/database.types.ts reflete as três novas tabelas (typegen rodou contra o schema vivo)"
  artifacts:
    - "supabase/migrations/0003_products_schema_rls.sql"
    - "src/lib/database.types.ts (regenerado)"
    - "tests/rls/product-isolation.test.ts"
  key_links:
    - "products.store_id -> stores(id) on delete cascade"
    - "product_sizes.product_id -> products(id) on delete cascade"
    - "product_photos.product_id -> products(id) on delete cascade"
    - "policy owner_full_access_* -> subquery stores.owner_id = auth.uid() (única fronteira de autorização)"
    - "bucket product-images path {owner_id}/{product_id}/{uuid}.{ext} -> storage.foldername(name)[1] = auth.uid()"
---

<objective>
Criar a fundação de dados desta fase: a migration `0003_products_schema_rls.sql` com as três tabelas relacionais (`products`, `product_sizes`, `product_photos`), RLS habilitada na MESMA migration por tabela, e o bucket de Storage `product-images` (público). Aplicar o [BLOCKING] push ao Postgres do Supabase, regenerar os tipos TypeScript, e provar o isolamento multi-tenant das três tabelas com duas contas reais seedadas.

Purpose: RLS é a ÚNICA fronteira de autorização do projeto (Padrão 4, 01-RESEARCH.md). Um esquecimento de RLS em tabela filha é vazamento de dados silencioso (03-RESEARCH.md Pitfall 2). Esta é a primeira fatia (dados) que sustenta todo o restante da fase — o schema aqui é consumido sem bypass pela vitrine pública (Fase 4) via `status = 'published'`, então o design de `status` já nasce previsto (D-10). A estrutura relacional (nunca JSON) é o que torna barato o comportamento decidido: tamanhos escolhidos individualmente (D-01), esgotado por padrão (D-03), atalho de esgotar tudo (D-04), capa = posição 1 (D-11), reordenação sem reenvio (D-12).
Output: migration aplicada ao banco remoto, tipos gerados, e teste de isolamento verde com duas contas.
</objective>

<phase_user_story>
**As a** revendedor de chuteiras importadas, **I want to** cadastrar, editar e organizar meus produtos com fotos e controle de estoque no painel, **so that** minha vitrine tenha um catálogo completo pronto para receber pedidos pelo WhatsApp.

(Nota do planejamento: o `**Goal:**` da Fase 3 no ROADMAP está em forma de resultado — "O revendedor consegue cadastrar, editar, excluir e organizar produtos..." — não na forma estrita "As a/I want to/so that". A user story acima é uma reafirmação fiel desse goal, sem inventar escopo novo, seguindo o precedente das Fases 1-2 deste projeto MVP. Esta primeira fatia entrega a fundação de dados; a primeira capacidade visível no navegador chega no Plan 03-02.)
</phase_user_story>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-PATTERNS.md
@supabase/migrations/0001_init_stores_rls.sql
@tests/rls/isolation.test.ts
@tests/setup/supabase-test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration 0003 (três tabelas + RLS + bucket product-images)</name>
  <files>supabase/migrations/0003_products_schema_rls.sql</files>
  <read_first>
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Pattern 1 — DDL exata das três tabelas + resolução da ambiguidade D-04; §Security Domain V4)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-PATTERNS.md (§migration — sequência create table -> enable row level security -> create policy; §Storage bucket + path-scoped RLS)
    - supabase/migrations/0001_init_stores_rls.sql (análogo exato: RLS na mesma migration, 4 policies de storage por bucket)
  </read_first>
  <action>
    Criar `supabase/migrations/0003_products_schema_rls.sql` seguindo LITERALMENTE o padrão de `0001_init_stores_rls.sql` (03-PATTERNS.md §migration) e a DDL de 03-RESEARCH.md Pattern 1. Ordem por tabela, sem exceção: `create table` -> `alter table ... enable row level security` -> `create policy ... for all using (...)` no MESMO arquivo (Pitfall 2 — nunca deixar RLS para uma migration posterior).

    Tabela `products`: id uuid pk default gen_random_uuid(); store_id uuid not null references stores(id) on delete cascade; name text not null; brand text not null; brand_other text; line text; sole text; category text; fulfillment text; price numeric(10,2) not null; description text; status text not null default 'draft' check (status in ('draft','published')); created_at timestamptz not null default now(). Policy owner_full_access_products: for all using (store_id in (select id from stores where owner_id = auth.uid())).

    DECISÃO DE SCHEMA (planner, resolve Open Question 1 de 03-RESEARCH.md sem checkpoint bloqueante): NÃO adicionar `check constraint` de enumeração em brand/sole/category/fulfillment — deixar como `text` nullable (exceto brand not null e status, que TEM check porque é o portão consumido pela Fase 4). A enumeração dessas listas fixas é validada só na camada de aplicação (constants.ts + Zod, Plan 03-02), de modo que ajustar a lista de `categoria`/marca no futuro nunca exija uma migration de correção. Documentar essa escolha em comentário no topo do arquivo.

    Tabela `product_sizes`: product_id uuid not null references products(id) on delete cascade; size smallint not null check (size between 36 and 45); available boolean not null default false (esgotado por padrão, D-03); primary key (product_id, size). RLS + policy owner_full_access_product_sizes: for all using (product_id in (select id from products where store_id in (select id from stores where owner_id = auth.uid()))).

    Tabela `product_photos`: id uuid pk default gen_random_uuid(); product_id uuid not null references products(id) on delete cascade; storage_path text not null; position smallint not null; created_at timestamptz not null default now(); unique (product_id, position). RLS + policy owner_full_access_product_photos com a mesma subquery aninhada de product_sizes.

    Bucket de Storage: `insert into storage.buckets (id, name, public) values ('product-images','product-images', true) on conflict (id) do nothing;` seguido das 4 policies (insert/select/update/delete) em storage.objects escopadas por `bucket_id = 'product-images' and (storage.foldername(name))[1] = auth.uid()::text` — cópia exata do padrão de store-assets em 0001 (o primeiro segmento do path é o owner_id, então a checagem foldername[1] funciona sem alteração). Bucket público porque a vitrine da Fase 4 lê as fotos por URL pública sem sessão (03-RESEARCH.md §Alternatives Considered — bucket público vs signed URL).

    Comentário no topo explicando a não-negociável (RLS na mesma migration) referenciando 03-RESEARCH.md em vez de 01.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0003_products_schema_rls.sql &amp;&amp; grep -q "enable row level security" supabase/migrations/0003_products_schema_rls.sql</automated>
  </verify>
  <acceptance_criteria>
    Arquivo existe; contém as três `create table` (products/product_sizes/product_photos), cada uma imediatamente seguida de `enable row level security` + `create policy`; contém `check (status in ('draft','published'))`; contém `insert into storage.buckets` para 'product-images' com public true e as 4 policies de storage.objects escopadas ao bucket + foldername[1]=auth.uid().
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar o schema ao Supabase + regenerar tipos</name>
  <files>src/lib/database.types.ts</files>
  <read_first>
    - .planning/phases/02-link-compartilh-vel-da-vitrine/02-03-PLAN.md (§Task do [BLOCKING] push — padrão de db push + SUPABASE_ACCESS_TOKEN + fallback manual + typegen)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Environment Availability — Supabase CLI 2.109.1, projeto remoto linkado presente em supabase/.temp)
  </read_first>
  <action>
    [BLOCKING — rodar DEPOIS da Task 1 e ANTES da Task 3.] Este é o schema push obrigatório: sem ele, build/typecheck passam (os tipos vêm de config, não do banco vivo) mas a verificação da fase seria um falso positivo, pois os testes de integração rodam contra o projeto Supabase remoto real (tests/setup/supabase-test.ts conecta via NEXT_PUBLIC_SUPABASE_URL/ANON_KEY de .env.local).

    Aplicar a migration ao projeto remoto linkado: rodar `supabase db push`. Se o comando precisar de auth neste ambiente não-interativo, exportar `SUPABASE_ACCESS_TOKEN` primeiro; se ainda assim exigir um prompt interativo que não pode ser suprimido, PARAR e sinalizar esta task para intervenção manual (este plano já é `autonomous: false`). Fallback se a CLI falhar: aplicar o SQL via SQL editor do Supabase Dashboard, depois ainda assim regenerar os tipos.

    Após o push bem-sucedido, regenerar `src/lib/database.types.ts` via `supabase gen types typescript --linked` (ou `--project-id vitrinoo`) e escrever a saída sobre o arquivo. Os tipos DEVEM vir do schema vivo — nunca editar database.types.ts à mão para simular as tabelas, pois é justamente o typegen que prova que o push aconteceu. Confirmar que os tipos gerados contêm `products`, `product_sizes` e `product_photos` em Tables.
  </action>
  <verify>
    <automated>grep -q "product_sizes" src/lib/database.types.ts &amp;&amp; grep -q "product_photos" src/lib/database.types.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `supabase db push` aplicou a migration ao banco remoto (ou fallback via Dashboard); `src/lib/database.types.ts` regenerado contém as três tabelas novas em Tables; `npx tsc --noEmit` passa. Se o push exigiu prompt interativo impossível de suprimir, a task foi sinalizada para intervenção manual em vez de ser marcada como concluída silenciosamente.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Teste de isolamento RLS das três novas tabelas</name>
  <files>tests/rls/product-isolation.test.ts</files>
  <read_first>
    - tests/rls/isolation.test.ts (análogo exato — seed de duas contas reais via seedAuthenticatedAccount, asserts de cross-tenant)
    - tests/setup/supabase-test.ts (helper seedAuthenticatedAccount)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-PATTERNS.md (§tests — Seed-two-tenants-and-assert-isolation)
  </read_first>
  <behavior>
    - Seed: criar Loja A e Loja B via seedAuthenticatedAccount; inserir uma `stores` row por conta (via client autenticado como cada dono, nunca service_role); inserir um `products` + `product_sizes` + `product_photos` por loja
    - Loja A autenticada SELECT em products/product_sizes/product_photos retorna apenas as linhas da Loja A (zero da Loja B)
    - Loja A tentando UPDATE/DELETE numa linha de products/product_sizes/product_photos da Loja B afeta 0 linhas (nunca sucesso silencioso cross-tenant)
  </behavior>
  <action>
    Criar `tests/rls/product-isolation.test.ts` copiando a estrutura de `tests/rls/isolation.test.ts`: seed de duas contas reais (Loja A, Loja B) com `seedAuthenticatedAccount`, inserir uma `stores` row por conta pelo client autenticado como cada dono, e então inserir uma linha em `products` (com store_id da respectiva loja), uma em `product_sizes` (product_id + size 40 + available true) e uma em `product_photos` (product_id + storage_path fake + position 0) por loja. Afirmar: (a) o client da Loja A lê apenas suas próprias linhas nas três tabelas (lista da Loja B vazia); (b) UPDATE de uma linha da Loja B pelo client da Loja A afeta 0 linhas; (c) DELETE idem. Este é teste de integração real contra o Supabase remoto — nunca mockar o client Supabase. Importar os tipos de src/lib/database.types.ts. Limpar (delete das stores seedadas) no afterAll, como o análogo faz.
  </action>
  <verify>
    <automated>npx vitest run tests/rls/product-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run tests/rls/product-isolation.test.ts` passa: todas as asserts de isolamento cross-tenant verdes para products, product_sizes e product_photos. O teste falharia (vermelho) se a RLS de qualquer uma das três tabelas estivesse ausente — provando que o push da Task 2 realmente aplicou a RLS.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| revendedor autenticado -> Postgres (RLS) | Cada revendedor só pode ler/escrever produtos/tamanhos/fotos da própria loja; a fronteira é a policy RLS, não código de aplicação |
| anon (vitrine Fase 4) -> Storage público | Fotos servidas por URL pública sem sessão; leitura pública é intencional, escrita continua owner-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure / Tampering | products / product_sizes / product_photos (policy RLS ausente em tabela filha) | critical | mitigate | RLS habilitada na MESMA migration de cada create table (Pitfall 2), policy via subquery store_id -> owner_id; provado por tests/rls/product-isolation.test.ts (Task 3) |
| T-03-02 | Elevation of Privilege | bucket product-images (escrita cross-tenant) | high | mitigate | 4 policies de storage.objects escopadas a bucket_id='product-images' AND foldername[1]=auth.uid()::text (Task 1) |
</threat_model>

<verification>
- `npx vitest run tests/rls/product-isolation.test.ts` verde (isolamento das 3 tabelas)
- `grep -q "product_sizes" src/lib/database.types.ts` e `product_photos` (typegen rodou contra o schema vivo)
- `npx tsc --noEmit` passa
- Migration contém RLS por tabela + bucket product-images + 4 storage policies
</verification>

<success_criteria>
- As três tabelas existem no banco remoto com RLS habilitada
- Isolamento multi-tenant provado com duas contas reais nas três tabelas
- Tipos regenerados refletindo o schema vivo
- Bucket product-images público criado com policies owner-scoped de escrita
</success_criteria>

<output>
Create `.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-01-SUMMARY.md` when done
</output>
