---
phase: 03-crud-de-produtos-e-pipeline-de-m-dia
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - package.json
  - package-lock.json
  - src/app/(admin)/produtos/size-grid.tsx
  - src/app/(admin)/produtos/product-form.tsx
  - src/lib/products/actions.ts
  - tests/products/availability.test.ts
autonomous: true
requirements: [PROD-02, PROD-04, PROD-07]
must_haves:
  truths:
    - "O revendedor escolhe explicitamente quais tamanhos (36-45) o produto tem; ao criar, 37-43 já vêm incluídos e esgotados por padrão, 36/44/45 ficam de fora até serem adicionados (D-01, D-02, D-03)"
    - "Cada pílula de tamanho cicla em um toque: não-incluído -> incluído/esgotado -> incluído/disponível -> não-incluído"
    - "Salvar o produto persiste exatamente os tamanhos escolhidos em product_sizes com o available correto"
    - "O atalho 'Marcar tudo como esgotado' põe todos os tamanhos incluídos como esgotado de uma vez (D-04) com toast"
  artifacts:
    - "src/app/(admin)/produtos/size-grid.tsx"
    - "src/lib/products/actions.ts (saveProduct persiste sizes; markProductEsgotado)"
    - "tests/products/availability.test.ts"
  key_links:
    - "size-grid (useFieldArray) -> product-form FormData sizes -> saveProduct -> product_sizes rows"
    - "markProductEsgotado -> UPDATE product_sizes SET available=false WHERE product_id (owner-scoped)"
---

<objective>
Entregar a fatia de estoque por tamanho: o revendedor escolhe quais tamanhos (36-45) o produto realmente tem e marca cada um como disponível ou esgotado, com a pré-seleção 37-43 esgotada por padrão ao criar (D-01/D-02/D-03), além do atalho "Marcar tudo como esgotado" (D-04). Ao salvar, os tamanhos escolhidos são persistidos em `product_sizes`.

Purpose: chuteiras raramente vêm em todo tamanho — o revendedor confirma o que tem em estoque em vez de desmarcar o que não tem (evita mostrar tamanho errado como disponível). A resolução de schema de 03-RESEARCH.md torna isto barato: cada tamanho é uma linha em product_sizes; o atalho de esgotar tudo é um único UPDATE em lote, sem coluna agregada no produto. A disponibilidade "vista" na vitrine (Fase 4) será derivada de EXISTS sobre product_sizes.
Output: grade de tamanhos interativa no formulário + persistência + atalho de esgotar tudo.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-PATTERNS.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-02-SUMMARY.md
@src/lib/products/actions.ts
@src/app/(admin)/produtos/product-form.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teste de integração (RED) — persistência e bulk-esgotar de tamanhos</name>
  <files>tests/products/availability.test.ts</files>
  <read_first>
    - tests/products/create-product.test.ts (helper de seed + padrão de asserção via client autenticado; criado no Plan 03-02)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Pattern 1 resolução D-04; §Grade de tamanhos com pré-seleção 37-43)
  </read_first>
  <behavior>
    - saveProduct com sizes=[{size:38,available:true},{size:40,available:false}] persiste exatamente essas 2 linhas em product_sizes com o available correto
    - saveProduct sem nenhum tamanho persiste 0 linhas em product_sizes (rascunho sem tamanho, D-10 — aparece esgotado por EXISTS falso)
    - markProductEsgotado(productId) põe available=false em TODAS as linhas de product_sizes daquele produto e não afeta produtos de outra loja
    - markProductEsgotado num produto de outra loja (cross-tenant) afeta 0 linhas (RLS)
  </behavior>
  <action>
    Criar `tests/products/availability.test.ts` reusando o helper de seed de tests/products/create-product.test.ts. Cobrir: (a) saveProduct com um array de sizes persiste as linhas corretas em product_sizes; (b) saveProduct sem sizes deixa product_sizes vazio para aquele produto; (c) markProductEsgotado zera available de todos os tamanhos do produto; (d) markProductEsgotado cross-tenant (client da Loja B tentando esgotar produto da Loja A) afeta 0 linhas. Passar os sizes no FormData da forma que o formulário vai enviar (ver Task 2 — decidir JSON string em um campo "sizes" e documentar) e afirmar via leitura direta de product_sizes. Começa VERMELHO (saveProduct ainda não persiste sizes; markProductEsgotado não existe).
  </action>
  <verify>
    <automated>npx vitest run tests/products/availability.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Após a Task 2, `npx vitest run tests/products/availability.test.ts` passa em todos os casos, incluindo o isolamento cross-tenant do bulk-esgotar.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Instalar clsx/tailwind-merge + size-grid + persistência de sizes + markProductEsgotado</name>
  <files>package.json, package-lock.json, src/app/(admin)/produtos/size-grid.tsx, src/app/(admin)/produtos/product-form.tsx, src/lib/products/actions.ts</files>
  <read_first>
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Standard Stack clsx/tailwind-merge; §Code Examples DEFAULT_SIZE_RANGE; §Pitfall 5 useFieldArray+dnd sync)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md (§Size grid — ciclo de 3 estados, tokens de cor, 44x44 touch target, legenda e copy)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-PATTERNS.md (§size-grid.tsx; §Field wrapper + error styling)
    - src/lib/settings/actions.ts (padrão de Server Action owner-scoped para markProductEsgotado)
  </read_first>
  <action>
    Instalar as libs pré-aprovadas no gate de legitimidade (03-RESEARCH.md §Package Legitimacy Audit — verdict OK/Approved, sem postinstall, sem [SUS]): `npm install clsx tailwind-merge`. Nenhum checkpoint humano é necessário (nenhum pacote foi [SUS]). Criar um helper `cn` (clsx + tailwind-merge) se conveniente, para compor className condicional das pílulas.

    Criar `size-grid.tsx` (client component) integrado ao react-hook-form do product-form via `useFieldArray` (name "sizes", itens { size, available }). Renderizar a grade de 10 pílulas 36-45 (grid-cols-5 gap-2 no mobile), cada pílula um `<button type="button">` com min 44x44px. Ciclo de 3 estados por toque (03-UI-SPEC.md §Size grid): não-incluído (outline #F5F5F3/texto #6B6B6B) -> incluído/esgotado (fill #F5F5F3, texto #6B6B6B, line-through) -> incluído/disponível (fill #00C46A, texto branco) -> não-incluído. Um tamanho não-incluído não está no array de fields; incluir = append({size, available:false}) (D-03: novo tamanho SEMPRE nasce esgotado); remover = remove(index). Usar clsx/tailwind-merge para as classes condicionais (primeiro componente do projeto com estados visuais suficientes — 03-RESEARCH.md). Ao criar um produto novo, inicializar os fields com DEFAULT_SIZE_RANGE (37-43) em estado incluído/esgotado (D-02/D-03); 36/44/45 começam fora. Abaixo da grade, o botão "Marcar tudo como esgotado" (outline #6B6B6B, não accent/destructive) — em modo criação ele apenas seta available=false em todos os fields incluídos (form state); em modo edição de um produto já salvo, chama a Server Action markProductEsgotado e mostra toast. Respeitar Pitfall 5: qualquer reordenação/alteração usa os métodos do useFieldArray, nunca um array paralelo. Adicionar a legenda de copy da UI-SPEC.

    Integrar `<SizeGrid />` na seção 4 (Tamanhos) do product-form.tsx (o placeholder deixado no Plan 03-02). No onSubmit, serializar os sizes escolhidos e anexar ao FormData no campo "sizes" (JSON string) — mesma convenção que o teste da Task 1 assume.

    Em `src/lib/products/actions.ts`: estender saveProduct para, após inserir o products row, parsear formData.get("sizes") (JSON), validar cada item (size inteiro 36-45, available boolean) e inserir as linhas em product_sizes com product_id do novo produto (só as escolhidas — nunca a grade inteira). Adicionar `markProductEsgotado(productId: string): Promise<ProductActionResult>`: getOwnedStore(); UPDATE product_sizes SET available=false WHERE product_id (a RLS via subquery garante que só afeta produtos da própria loja); retornar { success:true, id } ou { error }. Manter o return-shape union e a tradução de erro Postgres para PT.
  </action>
  <verify>
    <automated>npx vitest run tests/products/availability.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    clsx e tailwind-merge em package.json; `tests/products/availability.test.ts` passa; `npx tsc --noEmit` e `npm run lint` passam. A grade renderiza 36-45 com 37-43 pré-incluídos esgotados ao criar; o ciclo de 3 estados funciona por toque; salvar persiste só os tamanhos escolhidos; "Marcar tudo como esgotado" zera a disponibilidade.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (size-grid) -> Server Action | Array de tamanhos vindo do cliente; o servidor revalida size 36-45 e available boolean antes de gravar |
| Server Action -> Postgres (RLS) | markProductEsgotado e insert de product_sizes escopados por owner via subquery/getOwnedStore |
| npm install -> árvore de dependências | clsx/tailwind-merge entram na supply chain do build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | insert de product_sizes com size fora de 36-45 ou available não-boolean via chamada direta | medium | mitigate | validar cada item no servidor (inteiro 36-45, boolean) + check constraint size between 36 and 45 no schema (Plan 03-01) |
| T-03-09 | Tampering | markProductEsgotado em produto de outra loja | high | mitigate | getOwnedStore() + RLS via subquery product_id -> products -> stores.owner_id; afeta 0 linhas cross-tenant (testado na Task 1) |
| T-03-SC | Tampering | npm install clsx/tailwind-merge (supply chain) | high | mitigate | Package Legitimacy Audit de 03-RESEARCH.md concluído: ambos OK/Approved, sem postinstall, downloads em dezenas de milhões/semana, nenhum [SUS] — install sem checkpoint humano |
</threat_model>

<verification>
- `npx vitest run tests/products/availability.test.ts` verde (persistência de sizes + bulk-esgotar + isolamento)
- `npx tsc --noEmit` e `npm run lint` passam
- Grade de tamanhos: 37-43 pré-incluídos esgotados ao criar; ciclo de 3 estados; atalho de esgotar tudo
</verification>

<success_criteria>
- Tamanhos escolhidos explicitamente, 37-43 default esgotado ao criar (D-01/D-02/D-03)
- Persistência exata em product_sizes; produto sem tamanho fica sem linhas (D-10 via EXISTS)
- Atalho "Marcar tudo como esgotado" (D-04) com toast (PROD-07)
- PROD-02 (tamanhos) e PROD-04 satisfeitos
</success_criteria>

<output>
Create `.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-03-SUMMARY.md` when done
</output>
