---
phase: 03-crud-de-produtos-e-pipeline-de-m-dia
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/lib/currency/brl.ts
  - src/lib/validation/product.ts
  - src/lib/products/constants.ts
  - src/lib/products/actions.ts
  - src/app/(admin)/produtos/product-form.tsx
  - src/app/(admin)/produtos/novo/page.tsx
  - src/app/(admin)/produtos/page.tsx
  - src/app/(admin)/produtos/product-list.tsx
  - tests/products/create-product.test.ts
autonomous: true
requirements: [PROD-01, PROD-02, PROD-07]
must_haves:
  truths:
    - "O revendedor cadastra um produto com nome, marca (lista fixa + Outra), linha (texto livre, D-06), solado, categoria, modalidade, preço em BRL e descrição em uma tela única"
    - "Salvar com só nome+marca+preço funciona (rascunho); os demais campos são opcionais (D-09)"
    - "O preço digitado em formato BR (199,90) é persistido como 199.90, nunca truncado por parseFloat"
    - "O produto salvo aparece na lista /produtos com nome, marca e preço formatado; toast de sucesso/erro em cada save"
  artifacts:
    - "src/lib/currency/brl.ts"
    - "src/lib/validation/product.ts"
    - "src/lib/products/constants.ts"
    - "src/lib/products/actions.ts (saveProduct)"
    - "src/app/(admin)/produtos/product-form.tsx"
    - "src/app/(admin)/produtos/novo/page.tsx"
    - "src/app/(admin)/produtos/page.tsx"
    - "src/app/(admin)/produtos/product-list.tsx"
    - "tests/products/create-product.test.ts"
  key_links:
    - "product-form -> saveProduct (FormData) -> parseBRLPrice -> products insert"
    - "getOwnedStore() -> store_id via owner_id (RLS scoping)"
    - "produtos/page.tsx (Server Component) -> products select scoped por store_id"
---

<objective>
Entregar a primeira fatia visível no navegador: o revendedor abre `/produtos/novo`, preenche o formulário de tela única (D-08) com nome/marca/linha/solado/categoria/modalidade/preço/descrição, salva, e vê o produto listado em `/produtos` — com toast de sucesso. Só nome, marca e preço são obrigatórios (D-09); o resto é opcional, permitindo um rascunho rápido.

Purpose: este é o caminho end-to-end mínimo do CRUD de produtos (form -> Server Action -> Postgres -> lista). Estabelece a camada de dados de aplicação (parser de preço BRL dedicado, schema Zod, listas fixas, Server Action `saveProduct`) e a UI base que os slices seguintes (tamanhos, fotos, gerenciamento) vão estender. Tamanhos e fotos NÃO entram aqui — cada um é sua própria fatia (Plans 03-03 e 03-04).
Output: produto cadastrável e listável pela UI, com preço BRL correto e feedback visual.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-PATTERNS.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-CONTEXT.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md
@src/lib/settings/actions.ts
@src/app/(admin)/configuracoes/settings-form.tsx
@src/app/(admin)/configuracoes/page.tsx
@src/lib/validation/onboarding.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teste de integração (RED) — cadastro persiste campos e preço BRL</name>
  <files>tests/products/create-product.test.ts</files>
  <read_first>
    - tests/settings/store-settings-update.test.ts (análogo — mock de next/headers/next/navigation, seed via signUp+saveOnboarding, chamada de Server Action real)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Validation Architecture — testes de integração reais; §Pitfall 3 preço BRL)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-PATTERNS.md (§tests)
  </read_first>
  <behavior>
    - Seed conta + loja via signUp + saveOnboarding (mesmo helper do análogo); chamar saveProduct com FormData (name="Mercurial Vapor", brand="Nike", price="199,90")
    - Produto persiste: linha em products com name/brand corretos, price === 199.90 (número, nunca 199), status === 'draft'
    - Preço "1.299,90" (com separador de milhar) persiste como 1299.90
    - saveProduct sem name (ou sem brand, ou sem price) retorna { error: <string> } e NÃO persiste
    - saveProduct com campos opcionais (line, sole, category, fulfillment, description) persiste todos corretamente
  </behavior>
  <action>
    Criar `tests/products/create-product.test.ts` adaptando o mock e o helper de seed de `tests/settings/store-settings-update.test.ts` (mock de next/headers + next/navigation; signUp + saveOnboarding para ter uma loja com onboarding completo). Chamar a Server Action real `saveProduct` de `@/lib/products/actions` com FormData montado. Afirmar a persistência lendo de volta via um client anônimo autenticado (createAnonClient + signInWithPassword), como o análogo faz. Cobrir: happy path (nome+marca+preço), parsing de preço com vírgula decimal e com separador de milhar (Pitfall 3), rejeição de campos obrigatórios faltando (D-09), e persistência dos opcionais. Este teste começa VERMELHO (saveProduct ainda não existe) e fica verde após a Task 2. Limpar as stores seedadas ao final de cada caso.
  </action>
  <verify>
    <automated>npx vitest run tests/products/create-product.test.ts</automated>
  </verify>
  <acceptance_criteria>
    O arquivo existe e, após a Task 2, `npx vitest run tests/products/create-product.test.ts` passa em todos os casos: happy path, parsing 199,90 -> 199.90, 1.299,90 -> 1299.90, rejeição de obrigatórios, persistência de opcionais.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Camada de dados — parser BRL, schema Zod, constants e Server Action saveProduct</name>
  <files>src/lib/currency/brl.ts, src/lib/validation/product.ts, src/lib/products/constants.ts, src/lib/products/actions.ts</files>
  <read_first>
    - src/lib/settings/actions.ts (getOwnedStore linhas 63-84 — copiar verbatim; padrão de Server Action Zod -> normalize -> getOwnedStore -> mutation; tradução de erro Postgres)
    - src/lib/validation/onboarding.ts (forma do schema Zod + convenção de mensagens PT inline; DEFAULT_MESSAGE_TEMPLATE como exemplo de constante)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Code Examples productSchema; §Pitfall 3 parseBRLPrice; §Architecture Patterns Pattern 1)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md (§Fixed lists — enumerações de marca/solado/categoria/modalidade)
  </read_first>
  <behavior>
    - parseBRLPrice("199,90") === 199.90; parseBRLPrice("1.299,90") === 1299.90; parseBRLPrice("R$ 50") === 50; parseBRLPrice("abc") === null; parseBRLPrice("-10") === null (nunca negativo)
    - formatBRLPrice(199.9) === "R$ 199,90"; formatBRLPrice(1299.9) === "R$ 1.299,90"
    - productSchema exige name/brand/price (min 1); line/sole/category/fulfillment/description opcionais
    - saveProduct: Zod parse -> getOwnedStore -> parseBRLPrice (rejeita se null) -> insert em products com store_id, retorna { success: true, id } ou { error }
  </behavior>
  <action>
    Criar `src/lib/currency/brl.ts` com `parseBRLPrice(input: string): number | null` e `formatBRLPrice(value: number): string`. parseBRLPrice: remover "R$", espaços e pontos de milhar, trocar a vírgula decimal por ponto, converter; retornar null se o resultado não for um número finito positivo (NUNCA usar parseFloat cru no valor com vírgula — Pitfall 3). formatBRLPrice: usar Intl.NumberFormat pt-BR com style currency BRL (ou equivalente) para "R$ 1.234,56". Nenhum bloco de código fica em CLAUDE.md sobre isto — é helper novo (03-PATTERNS.md §No Analog Found).

    Criar `src/lib/validation/product.ts` com `productSchema` (Zod) exatamente como o draft de 03-RESEARCH.md §Code Examples: name/brand com min(1) e mensagens PT; brandOther/line/sole/category/description opcionais; fulfillment como enum opcional de sob_encomenda/pronta_entrega/ambos; price como string bruta (min 1) — o parsing acontece no servidor via parseBRLPrice, nunca z.number() sobre input com vírgula. Exportar `type ProductInput = z.infer<typeof productSchema>`. Comentário de doc explicando revalidação server-side, referenciando 03-PATTERNS.md.

    Criar `src/lib/products/constants.ts` com as listas fixas de 03-UI-SPEC.md §Fixed lists: `BRANDS` (Nike, Adidas, Puma, Mizuno, Under Armour, New Balance, Umbro) + o valor "Outra" (D-05); `SOLES` (FG, AG, TF, IC, MG, SG — D-07); `CATEGORIES` (Chuteira, Tênis, Chinelo, Outro); `FULFILLMENTS` como pares value/label (sob_encomenda/Sob encomenda, pronta_entrega/Pronta entrega, ambos/Ambos); `DEFAULT_SIZE_RANGE = [37,38,39,40,41,42,43]` e `SIZE_GRID = [36..45]` (usados no Plan 03-03). Marca/solado/categoria NÃO têm check constraint no banco (validação só aqui + Zod), conforme a decisão de schema do Plan 03-01.

    Criar `src/lib/products/actions.ts` com "use server" e `getOwnedStore()` copiado verbatim de src/lib/settings/actions.ts (03-PATTERNS.md permite duplicar; extrair para módulo compartilhado é opcional). Exportar `type ProductActionResult = { error: string } | { success: true; id: string }`. Implementar `saveProduct(formData: FormData): Promise<ProductActionResult>`: montar objeto a partir de formData.get para os campos do schema; productSchema.safeParse; se brand === "Outra" exigir brandOther não-vazio (senão retornar erro amigável); parseBRLPrice(price) — se null retornar { error: "Informe um preço válido, ex.: 199,90." }; getOwnedStore(); insert em products (store_id = owned.storeId; brand efetivo = brand_other quando "Outra"; campos opcionais como null quando vazios; status implícito 'draft'); traduzir erro do Postgres para mensagem PT amigável (nunca cru), retornar { success: true, id } com o id inserido. NÃO tocar em product_sizes/product_photos aqui — são slices posteriores.
  </action>
  <verify>
    <automated>npx vitest run tests/products/create-product.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `tests/products/create-product.test.ts` passa (todos os casos da Task 1); `npx tsc --noEmit` passa. saveProduct persiste com store_id correto, preço parseado, status draft; rejeita obrigatórios faltando e preço inválido com mensagem PT.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: UI — formulário de tela única + rota nova + listagem base + link de navegação</name>
  <files>src/app/(admin)/produtos/product-form.tsx, src/app/(admin)/produtos/novo/page.tsx, src/app/(admin)/produtos/page.tsx, src/app/(admin)/produtos/product-list.tsx</files>
  <read_first>
    - src/app/(admin)/configuracoes/settings-form.tsx (análogo exato do client form — useForm + zodResolver + useTransition + toast; forma do onSubmit montando FormData; wrapper de campo label+input+erro; botão submit disabled/pending)
    - src/app/(admin)/configuracoes/page.tsx (análogo do Server Component protegido — requireCompletedOnboarding, createClient, getUser, layout main bg-white max-w-md)
    - src/app/(admin)/dashboard/page.tsx (padrão de página do painel + link de navegação)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md (§Product form seções 1-3 e 6; §Product list page; §Copywriting Contract; tokens de cor/spacing/tipografia)
  </read_first>
  <action>
    Criar `product-form.tsx` (client component) espelhando settings-form.tsx: `useForm<ProductInput>` com zodResolver(productSchema), useTransition, toast (sonner). Renderizar as seções de tela única (D-08) da 03-UI-SPEC.md — SOMENTE as seções desta fatia: (1) Identificação: nome (input text, required), marca (`<select>` a partir de BRANDS com opção "Outra" que revela um input text brandOther via watch), linha (input text opcional); (2) Solado & Categoria: solado (`<select>` de SOLES, opcional, com opção vazia "—"), categoria (`<select>` de CATEGORIES), modalidade (`<select>` de FULFILLMENTS); (3) Preço: input type="text" inputMode="decimal" (NÃO type="number", Pitfall 3), placeholder "0,00", com "R$" como texto estático no padding; (6) Descrição: `<textarea>` rows={4} opcional. Usar os wrappers de campo (label + input + span de erro) com as classes/tokens exatos de settings-form.tsx. Deixar placeholders/estrutura para as seções Tamanhos (4) e Fotos (5) serem preenchidas pelos Plans 03-03/03-04 — NÃO renderizar size-grid nem photo-uploader agora. onSubmit: montar FormData com os campos, chamar saveProduct dentro de startTransition, toast.error(result.error) ou toast.success("Produto salvo!") e router.push("/produtos") no sucesso. Botão submit "Salvar produto" (accent, disabled={isPending}, label "Salvando…"). Aceitar props opcionais de defaultValues para reuso no Plan 03-05 (edição), mas nesta fatia só o modo criação é exercido.

    Criar `novo/page.tsx` (Server Component): `await requireCompletedOnboarding()`, layout main bg-white igual às páginas do painel, `<h1>` "Novo produto" (#0D3D2B), renderizar `<ProductForm />`, e um link "Voltar" para /produtos.

    Criar `page.tsx` (lista, Server Component): requireCompletedOnboarding; createClient; getUser; buscar a store por owner_id; `supabase.from("products").select("id, name, brand, brand_other, line, price, status").eq("store_id", store.id).order("created_at", { ascending: false })`. Renderizar `<h1>` "Produtos" + botão "Novo produto" (Link para /produtos/novo, accent, full-width no mobile) e `<ProductList products={...} />`. Se não houver produtos, o empty state "Nenhum produto cadastrado ainda" + corpo (copy da 03-UI-SPEC.md). NUNCA adicionar "use cache" (estoque/lista precisa refletir mudanças imediatamente).

    Criar `product-list.tsx`: renderizar cada produto como linha/card (thumbnail placeholder ImageOff por enquanto — fotos chegam no 03-04; nome em weight 500; marca/linha como caption #6B6B6B; preço via formatBRLPrice em Label size; badge de status Rascunho/Publicado). Filtros/busca/ordenação e botões editar/excluir NÃO entram aqui — são Plans 03-05/03-06; manter a lista simples e legível. Toolbar não é criada agora.

    Adicionar um link de navegação para /produtos a partir do dashboard (src/app/(admin)/dashboard/page.tsx) OU da /configuracoes, seguindo o padrão de Link já usado (borda #0D3D2B), para que a rota seja alcançável pelo painel. Escolher o dashboard como ponto de entrada principal.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc --noEmit` e `npm run lint` passam. /produtos/novo renderiza o formulário de tela única com as seções Identificação/Solado&Categoria/Preço/Descrição; submeter com nome+marca+preço salva e redireciona para /produtos exibindo o produto; toast de sucesso/erro aparece; a marca "Outra" revela o campo de texto livre; o preço aceita vírgula decimal. /produtos é alcançável por um link no dashboard.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (product-form) -> Server Action (saveProduct) | Input do revendedor cruza aqui; validação client é conveniência, a fronteira real é a revalidação Zod + parseBRLPrice no servidor |
| Server Action -> Postgres (RLS) | saveProduct escreve escopado por store_id resolvido de auth.uid() via getOwnedStore |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | campo preço (parseFloat cru trunca 199,90 -> 199; input não-numérico) | medium | mitigate | parser dedicado parseBRLPrice (rejeita null/negativo) + revalidação productSchema no servidor (Task 2, Pitfall 3) |
| T-03-06 | Elevation of Privilege | saveProduct sem owner-scoping gravaria em loja alheia | high | mitigate | getOwnedStore() resolve store_id de auth.uid() antes de qualquer insert; RLS de products (Plan 03-01) é a rede final |
| T-03-07 | Input Validation | campos obrigatórios/enum burlados via chamada direta ao Server Action | medium | mitigate | productSchema.safeParse revalidado no servidor; brand="Outra" exige brand_other; nunca confiar só no client |
</threat_model>

<verification>
- `npx vitest run tests/products/create-product.test.ts` verde
- `npx tsc --noEmit` e `npm run lint` passam
- Cadastro end-to-end pela UI: /produtos/novo -> salvar -> produto listado em /produtos com preço formatado
</verification>

<success_criteria>
- Produto cadastrável em tela única com só nome/marca/preço obrigatórios (D-08, D-09)
- Marca como lista fixa + "Outra" texto livre (D-05); solado/categoria/modalidade selects (D-07); linha texto livre (D-06)
- Preço BRL parseado corretamente (Pitfall 3); toast em cada save (PROD-07)
- Produto aparece na lista /produtos
</success_criteria>

<output>
Create `.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-02-SUMMARY.md` when done
</output>
