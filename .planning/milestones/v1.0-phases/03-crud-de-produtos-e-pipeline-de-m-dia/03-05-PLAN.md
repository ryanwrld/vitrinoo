---
phase: 03-crud-de-produtos-e-pipeline-de-m-dia
plan: 05
type: execute
wave: 5
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - src/app/(admin)/produtos/[id]/editar/page.tsx
  - src/app/(admin)/produtos/product-form.tsx
  - src/app/(admin)/produtos/product-list.tsx
  - src/lib/products/actions.ts
  - tests/products/edit-delete-product.test.ts
autonomous: true
requirements: [PROD-05, PROD-07]
must_haves:
  truths:
    - "O revendedor abre /produtos/[id]/editar com o formulário pré-preenchido (campos, tamanhos e fotos) e salva alterações"
    - "Excluir um produto remove as linhas (products + cascade sizes/photos) E os arquivos do bucket (sem órfãos), atrás de um diálogo de confirmação nativo"
    - "Publicar move o produto de rascunho para publicado (D-10); voltar para rascunho reverte — toggle manual, sem gate de completude"
    - "Cada ação (salvar, excluir, publicar, despublicar) dispara toast de sucesso/erro"
  artifacts:
    - "src/app/(admin)/produtos/[id]/editar/page.tsx"
    - "src/lib/products/actions.ts (updateProduct, deleteProduct, publishProduct, unpublishProduct)"
    - "tests/products/edit-delete-product.test.ts"
  key_links:
    - "editar/page.tsx -> product-form defaultValues (campos + sizes + photos pré-carregados)"
    - "deleteProduct -> storage.remove(paths) + delete products (cascade sizes/photos) (Pitfall 1)"
    - "publishProduct -> UPDATE products.status='published' (portão consumido pela Fase 4)"
---

<objective>
Entregar a fatia de gerenciamento: o revendedor edita um produto existente (formulário pré-preenchido com campos, tamanhos e fotos), exclui um produto (removendo linhas E arquivos do storage, atrás de confirmação), e publica/despublica (D-10 — rascunho vira publicado por ação explícita). Cada ação dá toast imediato.

Purpose: sem editar/excluir/publicar, o catálogo é write-only e nunca fica limpo. Publicar é o portão que a vitrine pública (Fase 4) consome via status='published' — decisão de schema do Plan 03-01, ação aqui. A limpeza de storage no delete evita arquivos órfãos (Pitfall 1 — cascade do Postgres NÃO apaga blobs). Exclusão é hard delete com diálogo nativo de confirmação (mesmo padrão do slug-editor da Fase 2).
Output: edição, exclusão com cleanup, e publish/draft toggle, todos com feedback visual.
</objective>

<execution_context>
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/workflows/execute-plan.md
@/Users/ryanlucas/Downloads/VITRINOO/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-PATTERNS.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md
@.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-04-SUMMARY.md
@src/lib/products/actions.ts
@src/app/(admin)/produtos/product-form.tsx
@src/app/(admin)/configuracoes/slug-editor.tsx
@src/app/(admin)/configuracoes/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teste de integração (RED) — editar, excluir com cleanup, publicar</name>
  <files>tests/products/edit-delete-product.test.ts</files>
  <read_first>
    - tests/products/create-product.test.ts e tests/products/photo-upload.test.ts (helpers de seed, construção de fotos, asserções de storage)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Pitfall 1 cleanup de storage; §Don't Hand-Roll hard delete)
  </read_first>
  <behavior>
    - updateProduct edita nome/marca/preço/opcionais de um produto existente e reescreve os product_sizes conforme o novo array
    - updateProduct de um produto de outra loja (cross-tenant) não altera nada
    - deleteProduct remove a linha de products (e por cascade product_sizes/product_photos) E chama storage.remove com os paths das fotos (sem órfãos) — asserir que a linha sumiu e que o(s) arquivo(s) não estão mais listados no bucket
    - deleteProduct cross-tenant não remove nada
    - publishProduct seta status='published'; unpublishProduct volta para 'draft'
  </behavior>
  <action>
    Criar `tests/products/edit-delete-product.test.ts` reusando os helpers. Seed produto com 1-2 fotos reais (bytes sintéticos válidos) e alguns tamanhos. Cobrir: updateProduct (campos + reescrita de sizes), updateProduct cross-tenant (0 efeito), deleteProduct (linha sumiu + storage limpo — verificar via storage.list/download que o path não existe mais), deleteProduct cross-tenant, publishProduct/unpublishProduct (status). Começa VERMELHO (essas actions ainda não existem). Limpar recursos ao final.
  </action>
  <verify>
    <automated>npx vitest run tests/products/edit-delete-product.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Após a Task 2, `npx vitest run tests/products/edit-delete-product.test.ts` passa: edição, reescrita de sizes, cleanup de storage no delete, isolamento cross-tenant, publish/unpublish.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Server Actions — updateProduct, deleteProduct (cleanup), publish/unpublish</name>
  <files>src/lib/products/actions.ts</files>
  <read_first>
    - src/lib/products/actions.ts (saveProduct, markProductEsgotado, validatePhotoFile, helper de cleanup de storage do Plan 03-04)
    - src/lib/settings/actions.ts (padrão de update + tradução de erro Postgres)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-RESEARCH.md (§Pitfall 1)
  </read_first>
  <action>
    Em `src/lib/products/actions.ts`:

    `updateProduct(productId: string, formData: FormData): Promise<ProductActionResult>`: mesma validação de saveProduct (Zod + brandOther + parseBRLPrice + getOwnedStore); UPDATE do products row .eq("id", productId) (o RLS garante escopo — cross-tenant afeta 0 linhas); reescrever product_sizes conforme o array recebido (estratégia simples e correta: delete das linhas atuais daquele product_id e insert das novas, ou diff — documentar a escolhida; delete+insert é aceitável dado o tamanho pequeno do conjunto). NÃO mexer nas fotos aqui (fotos têm suas próprias actions do Plan 03-04). Retornar success/error.

    `deleteProduct(productId: string): Promise<ProductActionResult>`: getOwnedStore(); PRIMEIRO buscar os storage_path de todos os product_photos daquele produto e chamar `storage.from("product-images").remove(paths)` (best-effort — logar mas não abortar se falhar) para NÃO deixar órfãos (Pitfall 1); DEPOIS delete da linha products (o on delete cascade remove product_sizes e product_photos). Reusar o helper de cleanup criado no Plan 03-04 se disponível. Retornar success/error.

    `publishProduct(productId)` e `unpublishProduct(productId)`: getOwnedStore(); UPDATE products SET status='published' (ou 'draft') .eq("id", productId). Toggle manual SEM gate de completude (03-RESEARCH.md Open Question 2 — decisão: não bloquear publicação por falta de foto/tamanho). Retornar success/error.

    Manter o return-shape union e a tradução de erro Postgres para PT em todas.
  </action>
  <verify>
    <automated>npx vitest run tests/products/edit-delete-product.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `tests/products/edit-delete-product.test.ts` passa; `npx tsc --noEmit` passa. updateProduct reescreve campos+sizes owner-scoped; deleteProduct limpa storage antes do cascade; publish/unpublish alternam status; tudo isolado por tenant.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: UI — rota de edição pré-preenchida, diálogo de exclusão, botões publish + na lista</name>
  <files>src/app/(admin)/produtos/[id]/editar/page.tsx, src/app/(admin)/produtos/product-form.tsx, src/app/(admin)/produtos/product-list.tsx</files>
  <read_first>
    - src/app/(admin)/configuracoes/slug-editor.tsx (padrão de <dialog> nativo de confirmação destrutiva — handleConfirm via onClick, estado pending "Excluindo…")
    - src/app/(admin)/configuracoes/page.tsx (Server Component protegido — carregar dados por id)
    - .planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-UI-SPEC.md (§Confirmation dialog delete; §Product form botão Publicar; §Product list page badges/botões; §Copywriting Contract)
    - src/app/(admin)/produtos/product-form.tsx (aceita defaultValues; modo edição)
  </read_first>
  <action>
    Criar `[id]/editar/page.tsx` (Server Component): requireCompletedOnboarding; carregar o produto por id escopado à store do dono (select products + product_sizes + product_photos daquele product_id); se não achar (ou for de outra loja), redirect para /produtos. Renderizar `<h1>` "Editar produto" e `<ProductForm defaultValues={...} productId={id} photos={...} sizes={...} mode="edit" />`.

    Estender `product-form.tsx` para o modo edição: pré-preencher os campos via defaultValues; a grade de tamanhos inicializa a partir dos sizes carregados (não do default 37-43); o photo-uploader inicializa com as fotos existentes (URLs públicas) e usa as actions dedicadas (addProductPhotos/updatePhotoOrder/removePhoto do Plan 03-04) para operações imediatas; o submit chama updateProduct(productId, formData) em vez de saveProduct. Renderizar, SÓ no modo edição, um botão secundário "Publicar" (quando status='draft') ou "Voltar para rascunho" (quando 'published') ao lado de "Salvar produto" — publicar é ação distinta de salvar (D-10); chamar publishProduct/unpublishProduct dentro de startTransition com toast ("Produto publicado!" / "Produto movido para rascunho."). Publish/unpublish e o atalho de esgotar tudo NÃO têm diálogo de confirmação (reversíveis, baixo risco — 03-UI-SPEC.md).

    Estender `product-list.tsx`: em cada linha, botões de ícone Pencil (Link para /produtos/[id]/editar) e Trash2 (abre o diálogo de exclusão), ambos com touch target 44x44. O diálogo de exclusão reusa o `<dialog>` nativo do slug-editor: título "Excluir {nome do produto}?", corpo "Isso vai remover o produto e todas as fotos da sua vitrine. Essa ação não pode ser desfeita.", botões "Cancelar" (outline #0D3D2B) + "Sim, excluir" (fill #FF4D4D, "Excluindo…" pending). Confirmar chama deleteProduct via onClick (não method="dialog" submit) dentro de startTransition, toast "Produto excluído." e router.refresh() no sucesso. Exibir o badge de status Rascunho/Publicado na linha (se ainda não exibido no Plan 03-02).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc --noEmit` e `npm run lint` passam. /produtos/[id]/editar abre pré-preenchido (campos+tamanhos+fotos); salvar aplica updateProduct; o botão Trash2 abre o diálogo e "Sim, excluir" remove o produto com toast; "Publicar"/"Voltar para rascunho" alterna o status com toast; um produto de outra loja não é editável (redirect).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Server Action (update/delete/publish) | productId vindo do cliente; o servidor sempre re-resolve owner via getOwnedStore e confia na RLS, nunca no id sozinho |
| Server Action -> Storage + Postgres | deleteProduct remove blobs do bucket owner-scoped e linhas via cascade |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Tampering / Information Disclosure | editar/excluir/publicar produto de outra loja passando productId alheio | high | mitigate | getOwnedStore() + RLS de products (subquery owner) faz UPDATE/DELETE afetar 0 linhas cross-tenant; testado (Task 1) |
| T-03-04 | Denial of Service (custo/quota) | arquivos órfãos no bucket após exclusão (cascade não apaga blobs) | medium | mitigate | deleteProduct busca storage_path e chama storage.remove ANTES do cascade (Pitfall 1, Task 2) |
| T-03-12 | Tampering | despublicar/publicar em massa via chamada direta | low | accept | ação reversível e de baixo impacto; owner-scoped por getOwnedStore/RLS; sem gate adicional |
</threat_model>

<verification>
- `npx vitest run tests/products/edit-delete-product.test.ts` verde (edição, cleanup, publish, isolamento)
- `npx tsc --noEmit` e `npm run lint` passam
- Edição pré-preenchida, diálogo de exclusão, publish/draft toggle funcionando na UI
</verification>

<success_criteria>
- Editar e excluir produtos (PROD-05); exclusão sem órfãos de storage (Pitfall 1)
- Publicar/despublicar como toggle manual (D-10), sem gate de completude
- Diálogo de confirmação nativo só na exclusão (destrutiva)
- Toast em cada ação (PROD-07)
</success_criteria>

<output>
Create `.planning/phases/03-crud-de-produtos-e-pipeline-de-m-dia/03-05-SUMMARY.md` when done
</output>
